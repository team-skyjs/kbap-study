---
tags: [meta, log]
생성일: 2026-07-08
---

# 📜 작업 로그

> Append-only. 볼트에 일어난 일을 시간순 한 줄씩. 형식: `- [날짜] 동사 | 내용`
> 동사: `setup`(구조) · `ingest`(새 노트) · `update`(기존 노트 갱신) · `move`(이동/이사) · `lint`(점검) · `fix`(수리)

---

- [2026-07-02] setup | 볼트 생성: 🏠 홈(MOC) + 📝 템플릿, 기존 EAS 노트 이름·위치 정리
- [2026-07-02] ingest | Obsidian 개념과 활용법
- [2026-07-02] ingest | Claude Code 작동원리: 🗺️ 지도 + 묶음 1~6 (learn-claude-code 20레슨, 에이전트 6개 병렬 분석)
- [2026-07-02] ingest | 로컬 빌드 vs EAS 클라우드 빌드 (K-Bap vs winemine) + EAS 노트에 역링크
- [2026-07-03] ingest | MCP 개념·원리·활용 (공식 문서 리서치) + 작동원리 6번 노트에 역링크
- [2026-07-03] ingest | 페이징 — Offset vs No-Offset (keyset/cursor 리서치)
- [2026-07-06] update | 페이징 노트에 MySQL·Postgres·Spring JPA 코드 예제 추가
- [2026-07-06] ingest | Metro 연결 실패 (dev 시드 → 리서치 완성: APIPA·Metro IP 선택·터널 원리)
- [2026-07-06] move | 볼트 iCloud 이사 (dev 원본은 백업 보존, 아이폰 동기화 확인)
- [2026-07-07] ingest | OCR 메뉴 스캔 — 박스 그룹핑 (dev 시드 → 완성: 최근접·헝가리안·클러스터링) / dev 시드 삭제
- [2026-07-08] fix | 홈의 OCR 링크 깨짐 수리 (파일명 불일치)
- [2026-07-08] setup | 카파시 LLM Wiki 비교 분석 → 📐 볼트 규칙 + 📜 작업 로그 신설, ingest 복리 단계 의무화
- [2026-07-08] lint | 린트 #1: 노트 16개 · 링크 83개 검사 — 깨진 링크 1(수리됨) · 고아 0 · 상태 방치 0 · 유령 링크 0
- [2026-07-08] ingest | 인증·보안(신규 주제): OAuth 2.0과 OIDC 개념 + Google vs Apple 비교 (2노트, RFC 9700·Apple 함정 리서치)
- [2026-07-08] update | MCP 노트 전송층 표에 OAuth 개념 노트 역링크 (복리 단계)
- [2026-07-09] ingest | 클라이언트 저장소 (K-Bap 시드 → kbap-fe 실코드 4파일 검증·인용: AsyncStorage·SecureStore·메모리, 결정 트리)
- [2026-07-09] ingest | 쿠키·세션 vs 토큰 인증 (저장소 시드 Q3 분리 → 인증·보안, 앱에 쿠키가 없는 구조적 이유)
- [2026-07-09] update | 복리: Google vs Apple 함정①에 K-Bap credentials.ts 실전 사례 + OAuth 개념 노트에 인증방식 링크
- [2026-07-09] ingest | OCR 라인 분류 — 휴리스틱의 한계와 대안 (_briefs 브리프 → classifyLine.ts 실코드 검증: 패치 이미 적용 확인, 분포변화·2단 판단·퍼지매칭·골든픽스처)
- [2026-07-09] update | 복리: 박스 그룹핑 노트 "진행 중" 문단 → 새 분류 노트로 연결(파이프라인: 분류→그룹핑) / 규칙 §4에 _briefs 워크플로우 추가
- [2026-07-09] setup | dev 시드 삭제(클라이언트 저장소) — iCloud 완성본 확인 후
- [2026-07-14] setup | kbap 백엔드 공부 하네스 구축 (dev study/.claude — 에이전트 4·스킬 4, 노트는 볼트 kbap 백엔드/ 폴더에 단계별 생성 예정)
- [2026-07-14] ingest | kbap 백엔드 1단계(기초): 서버 프로세스·2 bootJar / 스레드풀 vs 이벤트루프 / JVM 위 Kotlin·널안전 — 3노트, 상호 forward/back 링크 완비
- [2026-07-14] fix | 지도 1단계 항목 3개를 굵은글씨→[[wikilink]]로 전환(지도경유 등록·고아 해소) + 스레드 노트 in-note 앵커 GitHub슬러그→[[#heading]] 수리
- [2026-07-14] lint | 린트 #2(kbap 1단계): 노트3+지도+홈 링크 전수대조 — 세 노트 상호링크 5개 정확·고아 0(지도가 3노트 링크)·미등록 0·깨진링크 0(앵커 1건 수리됨). 눈높이 경미 2건은 보고. 복리: 1단계 순수기초라 기존노트 억지링크 없음(계획서 판단 준수)
- [2026-07-14] fix | QA 눈높이 보고 2건 반영: 스레드 노트에 애너테이션 한 줄 각주(2단계 예고), Kotlin 노트 NPE 첫 등장 풀이 추가
- [2026-07-14] fix | ⚠️ 볼트 유실 사고 복구: 7/13 19:20경 외부 기기발 삭제가 iCloud로 전파돼 노트 15개·템플릿·.obsidian 설정 소실(2단계 계획 중 아키텍트가 발견) → dev 백업에서 13개 + 세션 트랜스크립트에서 2개(쿠키·세션 vs 토큰 인증, 클라이언트 저장소) 전량 복원. 홈 실링크 17개 전수 확인 정상. 삭제 발원 기기(아이폰/새 맥) 확인 필요
- [2026-07-14] ingest | kbap 백엔드 2단계(Spring 핵심): IoC/Bean·컴포넌트스캔 / DI·생성자주입·계층(Controller→UseCase→도메인→영속) / 설정·프로필4종·@Transactional 첫걸음 — 3노트, 1단계로 back-link 완비
- [2026-07-14] fix | 볼트 전역 깨진 홈백링크 수리: 템플릿(📝 공부노트 템플릿)이 [[홈]]으로 배포돼 온 게 근본원인 — 실제 파일명 🏠 홈과 불일치. 템플릿+21개 노트(모든 주제)의 [[홈]] → [[🏠 홈]] 일괄 치환(별칭 없음, bare 토큰). 잔여 0(로그 제외)
- [2026-07-14] lint | 린트 #3(kbap 2단계): 노트3+지도+홈 링크 전수대조 — 상호/back링크 정확·고아 0(지도가 6노트 링크)·미등록 0·깨진링크 0(전역 홈백링크 21+템플릿 수리됨). 눈높이 경미 2건 보고. 복리: 유실복구로 인증(쿠키·세션)·페이징 노트 재존재하나 실접점은 5단계(JWT)·3단계(JPA)라 그때 연결(계획서 방침 유지)
- [2026-07-14] fix | QA 눈높이 보고 2건 반영: DI 노트 ArchUnit 한 줄 풀이, IoC 노트 WebMvcConfigurer 확장 지점 설명 추가
- [2026-07-14] ingest | kbap 백엔드 3단계(JPA·영속성): 영속성컨텍스트·엔티티매핑·변경감지/flush·BaseEntity / LAZY·N+1·fetch join·페이징충돌 2단계해법 / Flyway timestamp버전·out-of-order·Testcontainers — 3노트, 1·2단계 back-link 완비
- [2026-07-14] update | 복리(2단계 예약 이행): 페이징 노트(백엔드/) "관련 노트"에 [[LAZY 로딩과 N+1 문제]] 역링크 1줄 추가 — 이론상 keyset 커서가 kbap 실코드에선 fetch join과 2단계로 결합(FoodRepositoryAdapter.findFoodPage, kb-63). 양방향 완성
- [2026-07-14] lint | 린트 #4(kbap 3단계): 노트3+페이징+지도 링크 전수대조 — 상호/back링크 정확·페이징↔LAZY 양방향 확인·고아 0(지도가 9노트 링크)·미등록 0·깨진링크 0. 눈높이 경미 2건 보고
- [2026-07-14] fix | QA 눈높이 보고 2건 반영: LAZY 노트 JPQL 첫 등장 풀이(개념표), cascade·orphanRemoval 발췌 풀이 한 줄 추가
- [2026-07-14] ingest | 브리프 처리(iOS 앱용량·ML Kit): [[iOS 앱 용량 — 네이티브 의존성과 ML Kit 모델]] — 450MB 진단(스크립트당 38MB×5 정적링크)·링커/리소스 원리·patch-package 2파일 패치·CNG 생존 메커니즘·Android unbundled 대비. 실코드(패치·Podfile.lock) 검증 완료
- [2026-07-14] update | 복리: EAS Dev Build·로컬vs클라우드빌드·OCR 박스그룹핑·OCR 라인분류 4개 노트에 새 노트 역링크 추가 (양방향 완성)
- [2026-07-14] lint | 신규 노트(iOS 앱용량) 링크 5건 전수대조 — 깨진링크 0·고아 아님(인바운드 6: 홈+형제4+로그)·홈 등록 확인·상태 완료
- [2026-07-14] fix | QA 눈높이 1건(모듈러 노트 VO 풀이) + 추가 발견: 같은 노트의 레포 문서 상대링크 10건(../adr/ 등, 볼트 밖 → 옵시디언에서 깨짐)을 레포 경로 표기로 일괄 변환. 재발 방지 규칙을 하네스 스타일 스킬에 추가
- [2026-07-14] ingest | kbap 백엔드 4단계(아키텍처·방법론): 클린아키텍처·ports&adapters(도메인순수·runtimeOnly) / 모듈러모놀리스·DDD·바운디드컨텍스트·ArchUnit경계 / SDD·TDD·BehaviorSpec·에이전트팀하네스 — 3노트. 1~4단계(개념) 완주, 종합 성격이라 1~3단계 조각을 묶음
- [2026-07-14] update | 교차주제 복리: Claude Code 작동원리 [[5. 여러 에이전트의 협업]]·[[2. 행동을 다듬는 장치 — 훅·투두·서브에이전트·스킬]] 두 노트 "관련 노트"에 [[SDD와 TDD — 스펙과 테스트로 개발하기]] 역링크 각 1줄 — CC 에이전트팀 개념이 kbap SpecKit·TDD 하네스로 실전 적용되는 접점. 양방향 완성
- [2026-07-14] lint | 린트 #5(kbap 4단계): 노트3+CC 2노트+지도 링크 전수대조 — 4단계 상호/back링크 정확·교차주제 노트3↔CC5·노트3↔CC2 양방향 확인·고아 0(지도가 12노트 링크)·미등록 0·깨진링크 0. 눈높이 경미 1건 보고. (iOS 앱용량 노트는 타 세션 작업이라 린트 범위 제외)
- [2026-07-14] ingest | kbap 백엔드 5a(실코드 흐름): 온보딩 프로필 저장 흐름(Controller→UseCase→member 도메인→persistence, profile JSON) / 메뉴판 스캔 판정 흐름(정규화→정제→카탈로그 매칭→위험도, FE recall→BE precision) — 2노트, 1~4단계 개념 노트로 대량 back-link
- [2026-07-14] update | FE 교차 복리: [[OCR 메뉴 스캔 — 박스 그룹핑]]·[[OCR 라인 분류 — 휴리스틱의 한계와 대안]] 두 FE 노트 "관련 노트"에 [[메뉴판 스캔 판정 흐름 — 메뉴명이 위험도가 되기까지]] 역링크 각 1줄 — FE recall 후보가 BE precision으로 확정되는 뒷단. 양방향 완성
- [2026-07-14] lint | 린트 #6(kbap 5a): 흐름 2노트+FE 2노트+지도 링크 전수대조 — 개념 back-link·FE↔스캔 양방향 정확·고아 0(지도가 14노트 링크)·미등록 0·깨진링크 0. 상대링크 사고패턴 `](../` 검사 = 0(흐름 노트는 레포 경로를 인라인 코드로 표기). 눈높이 경미 1건 보고
- [2026-07-14] fix | QA 눈높이 1건 반영: 스캔 흐름 노트 @Modifying 두 옵션(flush/clearAutomatically) 풀이 — 영속성 컨텍스트 캐시 불일치 방지 장치로 설명 + 영속성 노트 링크
- [2026-07-15] ingest | kbap 백엔드 5b(실코드 흐름·JWT 인증 kb-118): 소셜 로그인 흐름(Firebase 검증→회원 조회/가입→자체 access·refresh JWT 발급→Redis 세션 저장→응답 본문 반환) / JWT 인증 필터와 세션 생명주기(매 요청 Bearer 검증·request attribute 2단 릴레이·rotation 재발급·로그아웃 멱등) — 2노트, 1~4단계·인증 개념 노트로 대량 back-link. Redis는 콜아웃으로만(개념 노트 후보 보고)
- [2026-07-15] update | 복리(2단계·5a 예약분 이행): 쿠키·세션 vs 토큰 인증 노트 하단에 두 흐름 노트 역링크(원리→kbap 실제 발급·검증) / OAuth 2.0·OIDC 노트에 노트1 역링크(Firebase가 ID 토큰 검증 대신) / Google vs Apple 노트에 노트1 역링크(릴레이 이메일·(iss,sub) 식별) / 5a 온보딩·스캔 노트 @AuthMemberId 지점에 노트2 역링크(2단 릴레이 빚 갚기, 기존 본문 불변) / 지도 ④ [x]·wikilink 전환·"쿠키 발급/재발급"→"응답 본문 Bearer 발급/재발급" 정정·진도 16/다음 홈(kb-111)
- [2026-07-15] lint | 린트 #7(kbap 5b): 흐름 2노트 wikilink 14종(union) ↔ 실파일명 전수대조 전부 글자일치·깨진링크 0 / 필수복리 양방향 확인(두 흐름↔쿠키·세션, 노트1↔OAuth·Google vs Apple, 노트2↔5a 온보딩·스캔) / 고아 0(지도 ④+개념 역링크)·미등록 0(지도경유)·상태 완료 / 상대링크 사고패턴 ](../ 검사=0. iOS 앱용량 줄(타 세션)은 범위 제외. 눈높이 경미 1건 보고
- [2026-07-15] fix | 오케스트레이터 직접 검수 2건: JWT 필터 노트 응답 JSON 3곳의 키 오기 data/error → payload/message 수리(BaseResponse.kt:3-7 실측 — success/payload/message가 실제 필드, 노트1·5a와도 통일) / 소셜 로그인 노트 jjwt 첫 등장에 한 줄 풀이(JVM용 JWT 발급·검증 라이브러리) 추가
- [2026-07-15] move | 사용자 요청: kbap 백엔드 16노트 파일명에 공부 순서 번호 접두(1.~16., 지도 순서) — 사이드바 가나다순에서 학습 순서가 안 보이는 문제 해결. 리네임+볼트 전체 wikilink 일괄 갱신(H1 제목 포함, 갱신 25파일: kbap 16+지도+CC 2+OCR 2+페이징+인증·보안 3), 잔여 구링크 0(로그 제외). 이후 신규 노트도 번호 이어붙임(하네스 규칙 추가)
- [2026-07-15] ingest | kbap 백엔드 5c(실코드 흐름·홈 조회 kb-111): 17. 홈 화면 조회 흐름 — GET /home 단일 @Transactional(readOnly=true) fan-in으로 기피성분·인기음식5·최근스캔10 세 섹션을 프로필 언어로 모음, @AuthMemberIdOrNull 선택 인증(비회원 빈배열·위조토큰 401), 최근스캔 batch조회 N+1 회피 — 1노트, 개념8+흐름3 링크. 비회원=빈배열 실측을 > [!warning] 스펙과 다름으로 반영
- [2026-07-15] update | 복리(홈 read↔write 원천 3짝): 13 온보딩 노트에 17 역링크(기피성분·앱언어 저장→홈에서 읽힘) / 14 스캔 노트에 17 역링크(ScanHistory 저장→홈 최근스캔 원천, US3 write↔read) / 16 JWT 노트에 17 역링크(@AuthMemberId 선택버전 @AuthMemberIdOrNull) — 각 하단 append, 본문 불변. 페이징 노트 복리는 부적합 판정(홈은 고정크기 fan-in·커서 없음)이라 미실시. 지도 ③ [x]·wikilink 전환·진도 17/다음 회원 랭킹(kb-123)
- [2026-07-15] lint | 린트 #8(kbap 5c): 노트17 wikilink 11종(개념8+흐름3) ↔ 새 번호 파일명 전수대조 전부 글자일치·깨진링크 0 / 복리 3짝 양방향 확인(17↔13·14·16) / 고아 0(인바운드 4: 지도+13·14·16)·미등록 0(지도경유)·상태 완료·파일명 17. 번호규칙 준수 / 상대링크 사고패턴 ](../ =0. 볼트 전역 구링크 스캔: 라이브 노트 0(작업로그 3건은 append-only 이력이라 범위 밖). 눈높이 경미 1건 보고
- [2026-07-15] fix | QA 눈높이 1건 반영: 노트 17의 변경 감지(dirty checking) 등장 지점에 [[7. 영속성 컨텍스트와 엔티티 매핑]] 링크 병기. + 로그 5c 3줄이 기존 fix·move 줄 앞에 삽입돼 시간순 어긋난 것을 재배열 수리(append-only 원칙 복원)
- [2026-07-15] ingest | kbap 백엔드 5d(실코드 흐름·회원 랭킹 kb-123): 18. 회원 랭킹 흐름 — write(스캔에 얹힌 increaseScanCount 원자적 벌크 UPDATE·lost update 방지)↔read(GET /me/ranking, Ranking 불변 값객체가 카운트로 점수·등급 조회시점 계산)를 한 노트에서 맞물림 — 1노트, 개념9+흐름3 링크. 리뷰·다양성 항상 0(스캔만 기여) 실측을 > [!warning] 스펙과 다름으로 반영
- [2026-07-15] update | 복리(랭킹 접점 3짝): 14 스캔 노트에 18 역링크(increaseScanCount forward-예고 갚기, write↔read) / 7 영속성 노트에 18 역링크(변경 감지 ↔ 원자적 벌크 UPDATE 대비) / 2 스레드 노트에 18 역링크(동시성 lost update ↔ DB 원자적 +1 해법) — 각 하단 append, 본문 불변. 홈(17) 복리는 부적합 판정(랭킹 미조회·접점 없음)이라 미실시. 지도 ⑤ [x]·wikilink 전환·진도 18/다음 배치 LLM(kb-53, 마지막)
- [2026-07-15] lint | 린트 #9(kbap 5d): 노트18 실링크 12종(개념9+흐름3) ↔ 새 번호 파일명 전수대조 전부 글자일치·깨진링크 0 / 복리 3짝 양방향 확인(18↔14·7·2) / 고아 0(인바운드 4: 지도+14·7·2)·미등록 0(지도경유)·상태 완료·파일명 18. 번호규칙 준수 / 상대링크 사고패턴 ](../ =0 / in-note 앵커 [[#흐름 A — 증가(write)]] 헤딩 존재 확인·`[[링크]]`는 백틱 인라인코드라 미렌더(오탐 아님)·17은 의도적 미링크(본문 언급만). 눈높이 경미 1건 보고
- [2026-07-15] fix | QA 눈높이 1건 반영: 노트 18의 @SQLRestriction 등장 지점에 소프트 삭제 자동 필터 한 줄 풀이 + [[7. 영속성 컨텍스트와 엔티티 매핑]] 링크 병기
- [2026-07-16] ingest | 브리프 처리(S3 presigned URL): [[S3 presigned URL과 클라이언트 시크릿]](백엔드/) — 번들 시크릿 노출 원리·SigV4 서명 위임·PUT vs POST policy·key 신뢰경계·IAM 최소권한·CORS(모바일 무관)·키 폐기 원칙·BE발급 vs Cognito 비교. Swagger 재검증(발급 API 미배포·profileImageUrl 그릇 확인)
- [2026-07-16] update | 복리: 온보딩(13)·소셜로그인(15)·JWT필터(16)·Google vs Apple·OCR 라인분류 5개 노트에 역링크 — "비밀 서명은 서버로"·"FE는 주장, BE가 검증" 원리로 연결 (양방향 완성)
- [2026-07-16] lint | 신규 노트(S3 presigned) 링크 6건 전수대조 — 깨진링크 0·고아 아님(인바운드 6)·홈(백엔드) 등록 확인·상태 완료
- [2026-07-16] ingest | kbap 백엔드 5e(실코드 흐름·배치 LLM 스코어링 kb-53, 커리큘럼 마지막): 19. 배치 실행 골격(두 번째 bootJar·ApplicationRunner 게이팅·조사 대기열 청크 순회·batch flyway 미의존) / 20. LLM 스코어링 파이프라인(프롬프트 압축계약→가상스레드 3모델 fan-out→파싱→Consensus Ensemble 집계 1~100, 3모델 전부 성공해야 확정) — 2노트. 저장 안 함(kb-54)·위험도 미산출(kb-9)·부분실패=전부 FAILED 실측 반영
- [2026-07-16] update | 복리(배치 접점 4짝): 1 서버프로세스에 19 역링크(두 번째 bootJar 예고 갚기) / 9 Flyway에 19 역링크(batch flyway 미의존·스키마 소유 web 한 곳) / 2 스레드에 20 역링크(가상 스레드=세 번째 동시성 모델) / 14 스캔에 20 역링크(신뢰도=위험도의 먼 원천, 저장 미구현이라 과장 없이) — 각 하단 append 본문 불변. 19↔20 본문 상호링크 확인. 지도 ⑥ [x]·wikilink 전환·frontmatter 상태 완료·진도 완주(20노트)·이후 확장 3후보 추가
- [2026-07-16] fix | QA 깨진링크 수리: 노트 19의 wikilink 13개가 전부 백틱 인라인코드로 감싸져(`[[...]]`) 옵시디언에서 링크·그래프엣지로 렌더 안 되던 것을 백틱 제거해 정상 링크 복원(계획서 링크맵 표기가 본문에 그대로 복사된 것이 원인, 노트 20은 정상). 19→20 및 개념 9종 역참조 전부 복구
- [2026-07-16] lint | 린트 #10(kbap 5e·커리큘럼 완주): 노트19 링크 10종·노트20 링크 9종 ↔ 번호 파일명 전수대조 전부 글자일치·깨진링크 0(백틱 13건 fix 후) / 19↔20 양방향·복리 4짝 양방향 확인 / 고아 0(각 인바운드 4)·미등록 0(지도경유)·상태 완료·파일명 19.·20. 번호규칙 준수 / 상대링크 ](../ =0. 지도 상태 진행중→완료 반영. 타 세션 S3 presigned 줄은 범위 제외. 눈높이 보고 0건(두 노트 용어 전부 링크·용어집으로 커버)
- [2026-07-16] ingest | kbap 백엔드 확장①(개념·캐싱&Redis): 21. 캐싱과 키-값 저장소 — 왜 캐시하나(비용 회피)→키-값 저장소→Redis 4성질(인메모리·TTL·단일스레드원자·프로세스밖공유)→kbap 실측(Redis=refresh 세션 저장소, @Cacheable/@EnableCaching 없음). "캐시 아님"을 통찰로. React Query·페이징은 계획대로 미링크. 본편(1~20) 뒤 확장 노트
- [2026-07-16] update | 복리(캐싱 접점): 7 영속성 노트에 21 역링크(영속성 컨텍스트=1차 캐시의 상위 개념) / Expo 클라이언트저장소 노트 관련노트에 21 한 줄(FE SecureStore↔BE Redis 세션 양끝) / 15·16 노트 Redis 콜아웃에 21 링크 병기(문구 유지·링크만, 계획 지정 예외 — 16은 "아직 개념 노트 없어"가 낡아 dated 병기로 갱신) — 21↔7·15·16·Expo 양방향. 지도 "이후 확장" ① [x] wikilink 승격·진도요약 확장노트 1줄(지도 상태 완료 유지)
- [2026-07-16] lint | 린트 #11(kbap 확장①): 노트21 링크 11종(1·2·6·7·8·10·11·15·16·17 + Expo 클라이언트저장소) ↔ 파일명 전수대조 전부 글자일치·깨진링크 0·백틱래핑 0(19 사고 재발 없음 확인)·상대링크 ](../ =0 / 복리 5짝 양방향(21↔7·15·16·Expo·지도) / 고아 0(인바운드 5)·미등록 0(지도경유)·상태 완료·파일명 21. 번호규칙 준수. 눈높이 보고 0건(개념 노트라 용어 전부 개념표·용어집·FE 비유로 자체 정의)
- [2026-07-20] ingest | 브리프 처리(Android 구글 로그인 2단계 장애): [[Android 구글 로그인 — SHA-1·OAuth 클라이언트·DEVELOPER_ERROR]](Expo & React Native/) — 신뢰 체인 다이어그램·OAuth 클라이언트 타입 1/2/3·status code 표(10~17)·SHA 자동생성/전파지연/Play App Signing 함정·APK v2 서명 블록·진단 절차. 실코드 검증: KB-196 수정이 이미 반영됨(useSocialAuth.ts:72-73, 브리프의 "처리 중"보다 코드가 진화) · google-services.json은 여전히 Web(type 3)뿐(재다운 불필요 실증)
- [2026-07-20] update | 리서치 정정: 2차 벽("accessToken cannot be empty")의 원인을 브리프 해석("Android 네이티브가 accessToken 요구")에서 정밀화 — RNFB v25 브리지가 accessToken 부재를 ''로 매핑(OAuthCredential.ts)하고 Firebase Android SDK만 빈 문자열 거부(iOS SDK는 허용)가 진짜 인과. upstream 이슈 #9081·수정 커밋 d800caf8로 확증(로컬 node_modules 소스 실측 + RNFB e2e 회귀 테스트). 리서치 에이전트의 "Android getTokens는 accessToken 항상 null" 주장은 네이티브 소스로 반증(getTokens는 반환함, K-Bap 우회책 유효)
- [2026-07-20] update | 복리 4짝: OAuth 2.0과 OIDC(idToken/accessToken 실전 사례) · Google vs Apple("모범생"의 예외=Android 클라이언트 등록) · 로컬 빌드 vs EAS(서명=설치 허가+로그인 신원) · kbap 15 소셜 로그인(흐름의 앞 구간) — 각 하단 append 본문 불변, 양방향 링크. 홈(Expo & React Native) 등록
- [2026-08-21] ingest | 브리프 처리(프록시·Proxyman MITM): [[프록시와 MITM 디버깅 (Proxyman)]](네트워크/ — **신규 폴더**) — forward/reverse/transparent 비교표·CONNECT 터널(봉투만) vs SSL Proxying(TLS 2세션 분할) 시퀀스·신뢰 체인·iOS 설치≠완전신뢰(Apple 공식 "isn't automatically trusted for SSL")·트러블슈팅 9행·피닝 트레이드오프(NSPinnedDomains/로테이션 위험)·Android 대비표·대안 도구 5종·치트시트. 공식 문서 검증: Proxyman docs(llms-full)·Apple 102390·Apple Identity Pinning·RFC 9110·RN DevTools
- [2026-08-21] update | 리서치 정밀화 2건: ①`999`는 HTTP 코드가 아니라 Proxyman **내부 코드**(메인테이너 이슈 #922 "999 is an internal code") → "Possible Certificate Pinning" 힌트는 추측일 뿐이라는 함정 섹션 신설(감별법: Safari로 교차확인) ②옛 인증서가 무용지물이었던 이유 = CA가 **설치본/기기마다 새로 생성**됨(공식 "does not use pre-generated or shared certificates"). 실코드 검증: kbap-fe .env(dev.kbap.site)·api/client.ts는 fetch(→NSURLSession이 시스템 프록시 자동 사용, 앱 수정 불필요)·피닝 의존성 0(999의 원인이 피닝일 수 없었음 확증)·RN 0.85.3이라 DevTools Network 탭(0.83+) 이미 사용 가능
- [2026-08-21] update | 복리 3짝: Metro 연결 실패(같은 en0 IP·서브넷·DHCP 지식의 재활용, 다음 벽은 IP가 아니라 신뢰) / Android 구글 로그인(패키지명+SHA-1 대조 = 인증서 체인 대조와 동형, 안드로이드는 사용자 CA 기본 불신) / OAuth 2.0과 OIDC(Bearer 토큰이 평문으로 보인다 = HTTPS는 신뢰된 CA 전제) — 각 하단 append 본문 불변, 양방향. 홈에 **네트워크** 섹션 신설·등록
- [2026-08-26] archive | kbap 백엔드 재구축 R0(아카이브): 2026-07 커리큘럼 21노트+지도가 현재 코드와 크게 어긋나 **개작이 아닌 전면 재집필**로 결정 — kbap-server의 모듈 개편(3모듈 :common·:api·:batch, ADR-0016·0018)·계층 개편(UseCase 계층 소멸, ADR-0017)·JPA 연관관계 전면 금지(ArchUnit)·배치 Spring Batch 재작성·LLM 3모델 fan-out 제거(KB-320)·콘텐츠 채움 kbap-langchain 이관(KB-301)이 사유. 실행: `kbap 백엔드/` → `kbap 백엔드 (old)/` **폴더째 이동(파일명 22개 전부 불변)** · 22파일 최상단에 `> [!warning] 📦 보관 노트` 배너 삽입(낡은 이유 + 옛 번호→새 번호 대체 매핑, 새 노트 미존재라 위키링크 대신 일반 텍스트로 표기해 유령 노드 0) · frontmatter `상태: 완료`→`보관` 22건 · 옛 지도는 배너+서문 한 줄만 추가하고 체크박스·진도 기록은 당시 기록으로 보존
- [2026-08-26] update | 아카이브 부수 정리: 홈 `### kbap 백엔드` 를 2줄로(새 커리큘럼 집필 중 안내 + 옛 지도 📦 보관 줄 — 고아 방지) · 볼트 규칙 §5에 `상태: 보관` 값 정의 추가, §6 린트 "`상태: 진행중` 방치" 항목에 보관 제외 명시. 새 커리큘럼은 **29노트 + 새 지도(2026-08)** 계획(옛 19 배치 실행 골격·20 LLM 스코어링 파이프라인은 구조 자체가 제거돼 **주제 폐기**, 대체 노트 없음)
- [2026-08-26] lint | R0 검증: 볼트 전체에서 옛 21노트+지도를 가리키는 위키링크 전수 대조 — 파일명을 한 글자도 바꾸지 않아 **깨진 링크 0건**(old 폴더 내부 상호 링크·외부 인바운드 전부 유효, 옵시디언은 경로가 아니라 파일명으로 해석) / 새로 삽입한 배너 22개에 위키링크 0개 = **유령 노드 0** / `kbap 백엔드/` 경로 소멸·`kbap 백엔드 (old)/` 22파일 확인
- [2026-08-26] ingest | kbap 백엔드 재구축 R1(2단계 Spring 핵심): 새 지도([[🗺️ kbap 백엔드 공부 지도 (2026-08)]] — 29노트 목차 + R0~R10 진도표) + 4. IoC 컨테이너와 의존성 주입(컨테이너·생성자 주입·@Bean 조립·싱글턴/스레드 안전, 프로덕션 @Autowired 0 vs 테스트 269) / 5. kbap의 계층(Controller→(Facade)→Service→JpaRepository, UseCase 소멸 사유 ADR-0017·0014 — **이후 24노트가 물려 쓸 용어 정본**) / 6. 설정과 프로필, 트랜잭션 경계 / 7. API 계약의 모양(BaseResponse 봉투·ErrorCode 채번·X-API-Version 헤더 버저닝, FE client.ts와 짝지어 읽기) — 4노트, 상호링크 완비
- [2026-08-26] update | R1 실측 정정 5건 — 계획서·브리프가 틀리고 **코드가 맞았던** 건들(레포 재확인 완료): ① 프로필은 4종(local·dev·staging·prod)이되 api·batch가 각자 4개씩 가져 yml 8개 + 테스트용 2개 = **10개 파일** ② batch 의 flyway "off"는 설정이 아니라 **의존성 부재** — `batch/build.gradle.kts` 에 flyway 0줄(api는 3줄), batch resources 의 flyway 언급은 주석 한 줄뿐. 스프링 자동구성은 클래스패스에 있을 때만 도니 끌 것도 없다 ③ `JwtTokenProperties` 는 `@ConfigurationProperties` 홀더가 **아니다** — 애너테이션 없는 순수 data class 이고 `AuthConfig.kt:18-23` 이 `@Value` 로 조립. 레포 전체에서 `@ConfigurationProperties` 를 단 클래스는 **딱 2개**(`FoodVectorProperties`·`LlmModelProperties`) ④ self-invocation 함정의 실물 — `MemberService.completeOnboarding`(`@Transactional`, :22)이 같은 클래스의 `getMember`(`@Transactional(readOnly = true)`, :130)를 내부 호출한다. 프록시를 안 거쳐 `readOnly` 가 적용되지 않고, 그 덕에 저장이 정상 동작한다 ⑤ 문서↔코드 불일치 5번째 — `.env.example` 머리말은 "Spring Boot 는 .env 를 자동으로 읽지 않는다(spring-dotenv 필요)"라지만 `api/src/main/resources/application.yml:17-18` 의 `spring.config.import: optional:file:.env[.properties]` 로 **지금은 읽힌다**
- [2026-08-26] update | 복리 3짝(전부 하단 append·본문 불변, 양방향 완성): [[S3 presigned URL과 클라이언트 시크릿]] 에 6 역링크(발급 정책 3값 만료·형식·크기가 `kbap.upload.*` 설정 → `ImageUploadProperties` 로 묶여 재빌드 없이 바뀐다) / [[쿠키·세션 vs 토큰 인증 — 왜 앱은 JWT인가]] 에 7 역링크(§3의 "짧은 액세스+리프레시 회전"이 돌려면 401 하나를 `AUTH-004`/`AUTH-006` code 로 쪼개야 한다) / [[프록시와 MITM 디버깅 (Proxyman)]] 에 7 역링크(헤더 버저닝이라 경로만 봐선 어느 계약인지 모른다 — SSL Proxying 으로 `X-API-Version` 을 펼치는 게 유일한 확인 수단). 홈 `### kbap 백엔드` 첫 줄을 🚧 집필 중 안내문 → 새 지도 위키링크로 승격(옛 지도 📦 보관 줄은 유지)
- [2026-08-26] lint | 린트 #12(R1): 볼트 전역 54노트 위키링크 ↔ 실파일명 전수대조 — **라이브 노트 깨진 링크 0**(작업 로그 3건은 append-only 알려진 예외, 옵시디언·볼트 규칙·홈의 `[[링크]]`·`[[대괄호]]` 류는 문법 설명용 텍스트라 대상 아님) / 새 4노트·지도에서 `kbap 백엔드 (old)/` 로 새는 링크 **0** / **유령 노드 0**(지도는 4·5·6·7만 위키링크, 미집필 25개는 일반 텍스트 유지) / 백틱 래핑 `` `[[...]]` `` **0**(2026-07-16 노트19 사고 재발 없음) / 상대링크 `](../` 0 / frontmatter 상태 4노트 완료·지도 진행중(4/29 집필 중이라 정상) / 파일명 = H1 제목 = 링크 대상 일치 / 고아 0(인바운드 4:5개·5:6개·6:8개·7:5개·지도:4개) / 홈 등록 확인. 눈높이·정합성 지적 3건 보고 + 직접 수리 1건(노트13 §4-13 의 "리플렉션"이 정의 없이 등장 → 인라인 정의 + 정의가 있는 [[8. 영속성 컨텍스트와 엔티티 매핑 — 엔티티가 곧 도메인 모델]] 링크)
- [2026-08-26] fix | R1 QA 직접 수리 2건(노트 4, 인용 행번호 실측 대조): `AuthConfig` 발췌 설명의 "8~12줄"이 파일 절대 행번호가 아닌 발췌 상대 번호였던 것을 **30~34줄**로 정정(같은 코드를 노트 6은 `AuthConfig.kt:30-34` 로 인용 — 두 노트 표기 일치) / `AuthService` 생성자 설명의 인터페이스 목록 "20·23·24·25줄"에서 22줄(`TokenIssuer`)이 빠져 있던 것을 **20·22·23·24·25줄** 로 보정하고 `common.port.*` → `common.port.auth.*` 로 좁힘(다섯 타입 모두 `common/port/auth/` 실측 확인)
- [2026-08-26] ingest | kbap 백엔드 재구축 R2(3단계 JPA·영속성): [[8. 영속성 컨텍스트와 엔티티 매핑 — 엔티티가 곧 도메인 모델]](작업대·변경 감지·flush≠commit·BaseEntity/`@SQLRestriction` 소프트삭제·`ddl-auto: validate`·ADR-0012/0014로 변환 계층을 걷어낸 이력과 그 대가·Kotlin+JPA 함정 `kotlin-jpa`/`allOpen`) / [[9. 연관관계를 쓰지 않는 설계 — N+1을 애초에 만들지 않기]](**R2 앵커** — N+1을 고치는 게 아니라 만들지 않기로 한 설계, 연관 애너테이션 0개를 ArchUnit이 강제, 관계 표현 3수단 id참조+JSON컬럼+쿼리조인, 2단계 키셋 페이징, 트레이드오프 7행) / [[10. Flyway 마이그레이션 — 스키마를 코드로 관리하기]](timestamp 버전 49개·git 커밋로그 비유·`out-of-order: true`가 timestamp의 필연적 짝이라는 것과 그 대가인 "파일 한 장은 스스로 완결"·스키마 owner=api가 `BATCH_*`까지 소유·테스트도 같은 마이그레이션) — 3노트
- [2026-08-26] fix | 거짓 서술 수리(볼트 규칙 §3-4 모순 플래그): [[페이징 — Offset vs No-Offset(Keyset·Cursor)]] 머리말의 kbap 실전 줄이 **"keyset 커서가 fetch join과 충돌해 2단계로 결합된다(`FoodRepositoryAdapter.findFoodPage`)"** 였는데 2026-08-26 실측으로 **근거 둘 다 부재** — `join fetch` 레포 전체 0건, `FoodRepositoryAdapter` 클래스 소멸(모듈·계층 개편), 애초에 JPA 연관관계가 0개라 fetch join을 쓸 일이 없다. **"커서로 id만 페이징(`findFoodPageIds`) → 그 id로 본체 조회(`findByIdIn`)"** 로 교체하고 좌표를 `common/src/main/kotlin/com/kbap/common/domain/food/FoodJpaRepository.kt:107-117` 로 갱신, 링크 대상은 옛 `8. LAZY 로딩과 N+1 문제` → 새 9번. 하단에 날짜 붙인 `> [!note]` 로 **무엇이 왜 바뀌었는지 + "2단계라는 뼈대는 그대로 맞다"** 를 남김(사용자가 "내가 잘못 배웠나?" 할 때의 답)
- [2026-08-26] fix | R2 QA 직접 수리 1건(노트 8 치트시트): `@Version` 주석의 "kbap 은 Food 만" 을 **"Food·Review 둘뿐"** 으로 정정 — 같은 노트 8절이 `Food.kt:73-75`·`Review.kt:42-44` 둘을 들고 있어 노트 안에서 모순이었다(실측: `@jakarta.persistence.Version` 프로덕션 2곳)
- [2026-08-26] update | 복리 2짝(하단 append·본문 불변, 양방향 완성): [[페이징 — Offset vs No-Offset(Keyset·Cursor)]] ↔ 9 (위 fix 와 한 블록으로 처리 — 이론 노트의 keyset이 실코드에서 어떤 모양인지) / [[클라이언트 저장소 — AsyncStorage·SecureStore·메모리 (K-Bap 실전)]] ↔ 10 (AsyncStorage 구조 변경과 스키마 마이그레이션의 갈림길은 **되돌리기** — 폰은 사용자마다 한 벌이라 지우고 받으면 되지만 DB는 서버 전체가 공유하는 딱 한 벌). 지도 8·9·10 `[x]` + 위키링크 승격·진도표 R1 ✅/R2 ✅/R3 ⬅ 다음·완료 4→7. 홈 kbap 줄 `(집필 중: 4/29)` → `(집필 중: 7/29)`
- [2026-08-26] lint | 린트 #13(R2): 볼트 전역 위키링크 전수대조 — **라이브 노트 깨진 링크 0**(미해결 13건은 전부 `[[링크]]`·`[[대상]]`·`[[대괄호]]`·`[[...]]` 류 문법 설명 텍스트 + 작업 로그 3건 append-only 알려진 예외) / 새 3노트에서 `kbap 백엔드 (old)/` 로 새는 링크 **0** / **유령 노드 0**(지도 위키링크는 4~10 + 홈뿐, 미집필 22개는 `- [ ]` 일반 텍스트 유지) / 백틱 래핑 `` `[[...]]` `` **0** / 상대링크 `](../` **0** / 동명 파일 **0**(old↔new 제목 충돌 없음) / frontmatter 3노트 전부 `상태: 완료`·파일명 = H1 = 링크 대상 일치 / 고아 0(인바운드 8:6개·9:9개·10:5개) / 홈·지도 등록 확인. 실측 재확인: 마이그레이션 49개·`@Entity` 22개·`@Version` 2개·`join fetch` 0건·`FoodRepositoryAdapter` .kt 0건(specs 문서에만 잔존)
- [2026-08-26] fix | R2 QA 지적 3건 처리(오케스트레이터 직접): 노트8 "리플렉션"이 정의 없이 3회 등장 → 첫 등장에 인라인 정의(JS `Object.keys`/`obj[key]` 대비, 단 생성자·private 필드까지 닿음) + 용어집 항목 신설 / 노트10 인용 SQL 주석의 `:app:batch` 옛 좌표 → "마이그레이션은 적용 후 수정 금지(checksum)라 낡은 주석이 남는다"는 설명 콜아웃 추가(원문 편집 대신) / 머리말 서식 두 갈래(4·5는 화살표 없음, 7·10은 `📂 kbap 백엔드 (new)` 표기)를 6·8·9의 형태로 4노트 통일
- [2026-08-26] update | 하네스 수정(재발 방지): `study-note-style` 스킬에 **머리말 두 줄 고정 서식** 규칙 추가 — 🏠홈·📂주제=지도 링크 / ⬅️앞·➡️다음 / 🔗곁(선택), 미집필 노트는 `제목(집필 예정)` 일반 텍스트로 쓰고 완성 회차에 vault-keeper가 링크 승격. 서식 갈림이 3회차 연속 QA에 오르는 것을 막기 위해 규칙화(남은 22노트에 적용)
- [2026-08-26] ingest | kbap 백엔드 재구축 R3(4단계 아키텍처·방법론): [[11. 모듈러 모놀리스와 DDD — 3모듈과 도메인 패키지]](모놀리스/MSA/모듈러 3선택지 비교·바깥 선=Gradle 3모듈(어기면 컴파일 실패)·안쪽 선=도메인 패키지 14개(어기면 테스트 실패)·**모듈 8→16→7→3 궤적**과 그때마다 잃은 것·"성분" 삼형제로 본 바운디드 컨텍스트) / [[12. 포트와 어댑터 — 외부 시스템을 갈아끼우는 자리]](**R3 앵커** — 계약`common.port`/구현`*.infra`/조립`config` 3분할·포트 전수 7영역 16개 실측·어댑터 4종 해부·"DB는 왜 포트가 아닌가"(ADR-0012·0014로 걷어낸 이력)) / [[13. 경계를 지키는 테스트 — ArchUnit이 강제하는 규칙들]](`ModuleBoundaryTest.kt` 규칙 13종을 무엇을/왜/어기면 3단으로 전수 해설·26 vs 13의 정체·`allowEmptyShould` 대칭·**빌드/테스트/사람 3층 강제**·실패 메시지 읽는 법) / [[14. SDD와 TDD — 스펙과 테스트로 개발하기]](SpecKit 4단계·헌법 5원칙·`tasks.md`의 `[Red]` 딱지·Kotest BehaviorSpec·Testcontainers·커버리지 제외 규약) — 4노트, 상호링크 완비
- [2026-08-26] fix | 거짓 서술 수리 2건(볼트 규칙 §3-4 모순 플래그) — Claude Code 작동원리 [[2. 행동을 다듬는 장치 — 훅·투두·서브에이전트·스킬]]·[[5. 여러 에이전트의 협업]] 두 노트가 kbap 을 "test-writer→implementer 팀으로 TDD 를 굴리는 하네스"의 실사례로 인용하고 있었으나 **그 하네스는 2026-08-14 제거**됐다(레포 `CLAUDE.md` 첫 절: 사유 "구현 단계가 느려지고 과한 코드가 나오는 문제", 지금은 메인 세션 직접 구현 + `kbap-code-review`·`kbap-db-review` 스킬 명시 호출, 헌법 원칙 I 은 그대로 유효). 삭제가 아니라 **교체 + 이력 보존** — 머리말 🔗줄의 옛 `[[12. SDD와 TDD — 스펙과 테스트로 개발하기]]`(old 폴더) 링크를 새 [[14. SDD와 TDD — 스펙과 테스트로 개발하기]] 로 갱신하고, 하단에 날짜 붙인 `> [!note]` 로 "무엇이 언제 왜 바뀌었나" + 각 노트의 본문 논지에 붙인 교훈(노트2: 서브에이전트 맥락 격리의 대가/규칙과 강제 수단의 분리, 노트5: 남이 쓴 테스트를 받는 분업이 Green 의 '최소' 판단을 막는다/"나눌 수 있나"가 아니라 "나눠서 뭘 버나") 을 append. 본문 나머지 불변
- [2026-08-26] update | R3 실측 정정 2건: ① **문서↔코드 불일치 6번째 확정** — 레포 `CLAUDE.md:13`·`:30` 이 외부 시스템 목록에 `Kakao` 를 적지만 코드에 카카오는 한 줄도 없다(KB-350, 커밋 `9cfbe180`, 2026-08-19 에 카카오 로컬 → Google Places API (New) 전환하며 `KakaoLocalApi.kt`·`KakaoPlaceSearchClient.kt` 삭제). 포트 영역도 문서는 5개(`llm,storage,auth,mq,place`)라 적지만 실제 디렉터리는 **7개**(`scan`·`exchange` 추가) — 노트 12 는 디렉터리 실측(파일 22·인터페이스 16)을 정본으로 싣고 문서 낡음을 콜아웃으로 명시 ② **오케스트레이터 자료 오류를 작성자가 정정** — "전환 커밋에서 컨트롤러·서비스 무변경"은 스쿼시 머지 커밋(`921a70cb`)의 잘린 stat 오독이었고, 실제 전환 커밋 `9cfbe180` 은 16파일 +484/−442 로 `PlaceService`·`PlaceController`·`PlaceSearchRequest/Response` 가 전부 바뀌었다(카카오의 페이징 개념 `page`·`hasNext` 가 계약에 스며 있어 포트째 잘렸기 때문). 노트 12 는 **"벤더 기술은 어댑터에 갇혔지만 벤더의 개념은 계약을 타고 번졌다 — 포트의 값어치는 '변경이 없다'가 아니라 '변경이 어디까지 번지는지 예측 가능하다'"** 는 정확한 서술로 실려 있다
- [2026-08-26] update | 복리 2짝(하단 append·본문 불변, 양방향 완성) + CC 2노트(위 fix 와 한 블록): [[S3 presigned URL과 클라이언트 시크릿]] ↔ 12 (이 노트의 "클라가 직접 올리되 서명은 서버가" 결론에서, 서명하는 코드가 레포에서 **파일 하나**로 갇혀 있다는 실물 — 갈아끼울 때 고쳐야 할 파일 수를 미리 정해 두는 설계) / [[쿠키·세션 vs 토큰 인증 — 왜 앱은 JWT인가]] ↔ 12 (§3 "JWT 는 즉시 폐기가 어렵다"를 refresh 서버 저장으로 푸는데, 정작 서버 코드에서 Redis 라는 단어가 나오는 파일은 어댑터 하나뿐 — 계약이 규정한 건 저장소가 아니라 **조회+삭제의 원자성**이고 회전의 재사용 구멍이 거기 달렸다. 노트 12 §6-1 에도 이 노트로 가는 한 문장 추가). 지도 11·12·13·14 `[x]` + 위키링크 승격·진도표 R2 ✅/R3 ✅ 방금/R4 ⬅ 다음·완료 7→11. 홈 kbap 줄 `(집필 중: 7/29)` → `(집필 중: 11/29)`
- [2026-08-26] lint | 린트 #14(R3): 볼트 전역 위키링크 전수대조 — **라이브 노트 깨진 링크 0**(미해결은 작업 로그 3건 append-only 알려진 예외 + `[[링크]]`·`[[대괄호]]`·`[[wikilink]]` 류 문법 설명 텍스트뿐) / 새 4노트·CC 2노트에서 `kbap 백엔드 (old)/` 로 새는 링크 **0**(CC 두 노트가 old 12번을 가리키던 인바운드 2건은 새 14번으로 옮겼다. 다른 주제 폴더 → old 인바운드 11건은 대체 노트가 아직 미집필이라 그대로 — R4·R5 에서 15~17·19 가 나오면 옮길 자리다) / **유령 노드 0**(지도 위키링크는 4~14 + 홈뿐, 미집필 18개(1~3·15~29)는 `- [ ]` 일반 텍스트 유지) / 백틱 래핑 `` `[[...]]` `` **0** / 상대링크 `](../` **0** / **머리말 두 줄 고정 서식 11~14 전부 준수**(방금 규칙화한 🏠홈·📂지도 / ⬅️앞·➡️다음 / 🔗곁 — 직접 수정 0건, 14 의 `➡️ 다음: 15…(집필 예정)` 도 규약대로 일반 텍스트) / frontmatter 4노트 전부 `상태: 완료`·파일명 = H1 = 링크 대상 일치 / 고아 0(인바운드 11:4개·12:6개·13:4개·14:4개) / 홈·지도 등록 확인. 눈높이·정합성 지적 3건 보고 + 직접 수리 1건(노트13 §4-13 의 "리플렉션"이 정의 없이 등장 → 인라인 정의 + 정의가 있는 [[8. 영속성 컨텍스트와 엔티티 매핑 — 엔티티가 곧 도메인 모델]] 링크)
- [2026-08-26] fix | R3 QA 지목 3건 처리(오케스트레이터 직접): 노트14 머리 블록을 폴더 표준으로 통일(인라인 `> **한 줄 요약**` → `> 작성·맥락 / > 목표` + `## 0. 한 줄 요약`, 절 번호 0부터) / 노트13이 같은 ArchUnit 규칙을 `:105-138`·`:125-138` 두 좌표로 인용하던 것을 실측대로 `:125-138`(허용 맵 정의 `:108-123`) 단일화 / 노트12의 `seam` 첫 등장(§3-2 표)에 "=포트, 갈아끼우는 이음매" 한 마디 삽입 — 정의는 202줄 §4 표에 있어 첫 등장과 68줄 떨어져 있었음
- [2026-08-26] update | 하네스 수정 2차: `study-note-style` 스킬에 **머리 블록 고정 규칙** 추가 — H1 아래 `> 작성·맥락 / > 목표` + `---` + `## 0. 한 줄 요약`(절 번호 0부터, 인라인 한 줄 요약 금지). R1 머리말 서식에 이은 두 번째 서식 드리프트라 규칙화(남은 18노트 적용)
- [2026-08-27] ingest | kbap 백엔드 재구축 R4(5단계 읽기·인증 4흐름): [[15. 온보딩 프로필 저장 흐름 — 요청이 DB에 닿기까지]](가장 얇은 세로 슬라이스 — `save()` 없이 저장되는 변경 감지, 같은 경로에 `X-API-Version` 으로 갈리는 두 핸들러, `null` = "서버가 정하라" 신호, self-invocation 이 **우연히** 흐름을 살리는 실물, KB-297 프로필 JSON→컬럼 평탄화를 3개 마이그레이션으로 쪼갠 이유) / [[16. 소셜 로그인 흐름 — Firebase 토큰이 자체 JWT가 되기까지]](**앱에서 시작하는 유일한 흐름 노트** — `useSocialAuth.ts` 에서 서버 `AuthController` 까지 한 요청의 양쪽 끝을 잇는다. "왜 Firebase 를 쓰면서 자체 JWT 를 또 발급하나"를 옛 설계 KB-109 폐기 이력으로 답함·신원 키가 `(provider, provider_uid)` 인 이유·동시 로그인을 DB unique 인덱스가 심판·`UnavailableSocialAuth` 폴백·탈퇴가 Firebase→DB 순인 이유) / [[17. JWT 인증 필터와 세션 생명주기 — 매 요청 검증·재발급·로그아웃]](요청 하나가 아니라 **모든 요청의 가로줄** — 톰캣 필터 ↔ Express 미들웨어 대응표, 필터→DispatcherServlet→인터셉터→리졸버 5칸, `token_type` 이 같은 시크릿의 세 서랍을 가르는 라벨, `GETDEL` 원자 소비가 회전의 심장, 로그아웃 후에도 access 가 최대 30분 사는 대가) / [[18. 홈 화면 조회 흐름 — 여러 서비스를 한 응답으로 모으기]](읽기 fan-in — 서비스 **6곳**·쿼리 **9방**(비회원 3방)·읽기 트랜잭션 **3개**를 세어 보인다. BFF 트레이드오프 7행 표, 연관관계를 지운 대가로 id 를 직접 모으는 2패턴, 비회원 200·위조토큰 401 의 갈림) — 4노트, 15↔16↔17↔18 상호링크 + 외부 5노트 양방향 완비
- [2026-08-27] move | ⭐ 옛→새 번호 링크 이관 **9건 / 5노트**(대체 노트가 생겨 실행. 옛·새가 **제목이 같고 번호만 다른** 쌍이라 문자열 단위로 갈아끼우고 대상 파일 존재를 검증): [[OAuth 2.0과 OIDC — 소셜 로그인의 원리]]:182 · [[Google vs Apple 소셜 로그인 비교]]:130 · [[쿠키·세션 vs 토큰 인증 — 왜 앱은 JWT인가]]:143(옛15→새16)·:144(옛16→새17) · [[Android 구글 로그인 — SHA-1·OAuth 클라이언트·DEVELOPER_ERROR]]:10·:61(옛15→새16) · [[S3 presigned URL과 클라이언트 시크릿]]:9(옛15→새16)·:10(옛13→새15)·:87(옛16→새17). 매핑: 옛13→새15 / 옛15→새16 / 옛16→새17 / 옛17→새18(제목도 "세 섹션"→"여러 서비스"로 바뀜, 다만 이 노트로 가는 **외부 위키링크는 원래 0건**이었다)
- [2026-08-27] fix | 이관하며 거짓이 된 서술 1건 수리 — [[쿠키·세션 vs 토큰 인증 — 왜 앱은 JWT인가]]:144 의 "**매 요청** `Authorization: Bearer` 헤더를 필터가 검증하고" 를 "**보호 경로마다**"로 고쳤다. kbap 의 JWT 필터는 전역이 아니라 `WebConfig.kt:80-100` 의 19개 URL 패턴에만 붙기 때문(아래 실측 정정 ① 참조). 원문 수정 + 날짜 붙인 `> [!note]` 로 무엇이 왜 틀렸는지 남김
- [2026-08-27] update | 복리 5짝(하단 append·본문 불변, 양방향 완성): [[OAuth 2.0과 OIDC — 소셜 로그인의 원리]] ↔ 16 (이 노트는 "ID 토큰을 어떻게 검증하나"까지인데, kbap 은 **검증 성공한 그 토큰을 계속 쓰지 않는다** — memberId 내장·수명 통제·강제 로그아웃 가능 세 이유로 자체 토큰에 갈아탄다. OIDC 검증은 로그인 순간 딱 한 번) / [[Google vs Apple 소셜 로그인 비교]] ↔ 16 (§5-1 의 "복합 식별" 원리가 `uk_member_provider_uid` DDL 과 픽스처 문자열 `"firebase-uid-should-not-be-stored"` 테스트로 못 박힌 자리) / [[쿠키·세션 vs 토큰 인증 — 왜 앱은 JWT인가]] ↔ 17 (필터가 전역이 아니라는 정정 + §3 "짧은 액세스 + 리프레시 회전"의 **미구현 절반**인 재사용 탐지) / [[Android 구글 로그인 — SHA-1·OAuth 클라이언트·DEVELOPER_ERROR]] ↔ 16 (**이음매** — 이 노트의 종착점인 "앱이 마침내 idToken 을 쥔다"가 16번의 출발점 첫 줄이다. 서버로 가는 건 구글이 준 토큰이 아니라 Firebase 가 재포장한 토큰) / [[S3 presigned URL과 클라이언트 시크릿]] ↔ 15 (업로드가 끝난 경로가 DB 에 앉는 모양 — `validatedImagePath` 가 `http(s)://` 로 시작하는 값을 거절해 **objectKey 만** 저장하고, 조회 때 `ImageUrls.resolve` 가 도메인을 붙인다). 지도 15·16·17·18 `[x]` + 위키링크 승격·진도표 R3 ✅/R4 ✅ 방금/R5 ⬅ 다음·완료 11→15. 홈 kbap 줄 `(집필 중: 11/29)` → `(집필 중: 15/29)`
- [2026-08-27] update | R4 실측 정정 5건(`WebConfig.kt` 재확인 — 노트 15·17·18 에 반영됨): ① **JWT 필터는 전역이 아니다** — `WebConfig.kt:80-100` 의 `addUrlPatterns` **19개 경로에만** 붙는다. `/api/home`·`/api/auth/{login,refresh,logout}`·`/api/app-version` 은 필터가 **아예 안 돈다**. 그 경로의 인증은 `AuthMemberIdOrNullArgumentResolver` 가 `Authorization` 헤더를 **직접 재파싱**해서 한다(attribute 를 읽지 않는다) ② **게스트 예외는 2단계** — URL 패턴은 서블릿 규격이라 HTTP 메서드를 못 가리므로 넓게 걸고 `shouldNotFilter` 로 뺀다. 필터가 도는 경로 중 통과하는 건 GET 3개뿐(`community/posts`·`community/posts/\d+`·`reviews`). 부수 효과로 **게시글은 게스트가 읽는데 댓글(`posts/{id}/comments`)은 401** ③ `ApiPaths.API = "/api"` — **버전리스**다(`/api/v1` 아님). 버전은 `X-API-Version` 헤더 ④ **refresh 재사용 탐지 없음** — `GETDEL`(`consume`)로 옛 토큰은 죽지만, 재사용 시 세션 계보를 끊지 않고 `AUTH-005` 401 만 준다(`AuthService.kt:56-57`). 탈취를 막진 못하되 조용히 영구화되지도 않는다. **이 선택을 기록한 ADR·스펙 문장은 레포에 없다**(합류 후 확인할 질문으로 노트 §9 에 명시) ⑤ **`@AuthMemberIdOrNull` 은 이름과 다르다** — 만료·위조 토큰에 `null` 이 아니라 **예외를 던져 401**. `null` 이 되는 건 헤더 없음·`Bearer ` 아님·관리자 토큰 셋뿐
- [2026-08-27] lint | 린트 #15(R4): 볼트 전역 65노트·위키링크 719건 전수대조(NFC 정규화 후 파일명 대조) — **라이브 노트 깨진 링크 0**(미해결 3건은 전부 작업 로그의 append-only 알려진 예외 `:45`·`:53`·`:56`, 그 외는 `[[링크]]`·`[[대괄호]]`·`[[예시]]` 류 문법 설명 텍스트) / 새 4노트 → `kbap 백엔드 (old)/` **유입 0** / **`kbap 백엔드 (old)/` 로 들어가는 외부 인바운드 잔여 4건**(R5 이후 이관 대상): [[OCR 메뉴 스캔 — 박스 그룹핑]]·[[OCR 라인 분류 — 휴리스틱의 한계와 대안]] → 옛 `14. 메뉴판 스캔 판정 흐름`(새 19번 대기) · [[클라이언트 저장소 — AsyncStorage·SecureStore·메모리 (K-Bap 실전)]] → 옛 `21. 캐싱과 키-값 저장소`(새 29번 대기) · [[🏠 홈]] → 옛 `🗺️ kbap 백엔드 공부 지도`(**의도된 📦보관 등록**이라 이관 대상 아님). R3 대비 11 → 4 로 줄었다 / **유령 노드 0**(지도 위키링크는 4~18 + 홈뿐, 미집필 14개(1~3·19~29)는 `- [ ]` 일반 텍스트 유지) / 백틱 래핑 `` `[[...]]` `` **0** / 상대링크 `](../` **0** / **머리말 두 줄 서식 + 머리 블록 15~18 전부 준수**(🏠홈·📂지도 / ⬅️앞·➡️다음 / 🔗곁, H1 아래 `> 작성·맥락`+`> 목표` → `---` → `## 0. 한 줄 요약`. 18 의 `➡️ 다음: 19…(집필 예정)` 도 규약대로 일반 텍스트 — 직접 수정 0건) / frontmatter 4노트 전부 `상태: 완료`·파일명 = H1 = 링크 대상 일치(지도는 `진행중` 유지, 15/29 라 정상) / **고아 0**(인바운드 15:6회·4노트, 16:13회·8노트, 17:13회·6노트, 18:4회·3노트) / 홈·지도 등록 확인. 눈높이·정합성 지적 3건 보고 + 직접 수리 2건(노트15 흐름표의 `MDC` 가 정의 없이 첫 등장 → 인라인 풀이 + 17번 정본 표시 / 노트18 참고 URL 4개를 폴더 관행대로 `<>` 로 감쌈)
- [2026-08-27] ingest | kbap 백엔드 재구축 R5(5단계 스캔·이미지 3흐름): [[19. 메뉴판 스캔 판정 흐름 — 메뉴명이 위험도가 되기까지]](v1 경로 — **FE 가 요청 본문을 직접 쓰는 첫 API**. FE recall ↔ BE precision 이음매, `matchKey` = NFC + `가`~`힣` 만 남기는 세 줄, 못 찾은 이름은 그 자리에서 조사 대기 행으로 등록, 성분 매칭이 조인이 아니라 JSON 컬럼 위 `filter` 한 줄인 이유(ADR-0009), **`scan()` 에 `@Transactional` 이 없고 아래로 독립 트랜잭션 5개가 흩어지는** 트레이드오프, v1 응답이 9필드→7필드로 **동결**된 계약) / [[20. 스캔 v2와 사용량 티켓 — 서버 OCR·무료 한도·중복 요청 막기]](같은 URL 을 `X-API-Version` 이 가르는 두 계약 — 티켓(JWT)→Redis 예약(Lua 원자)→비전 LLM→성공 시에만 DB 카운트, 실패 시 예약 해제와 **해제마저 실패했을 때** 무엇이 남나, 한도 상태가 사는 저장소 세 곳, 해금은 리뷰가 코드 0줄로) / [[21. 이미지 업로드 흐름 — presigned URL과 업로드 완료 확정]](발급→S3 직접 PUT→**확정** 3단계. 각 단계에서 서버가 아는 것/모르는 것, objectKey 를 서버가 짓는 게 곧 보안, `complete` 를 소비처마다 다른 강도로 요구한다는 실측표) — 3노트, 19↔20↔21 상호링크 + 외부 4노트 양방향 완비
- [2026-08-27] move | ⭐ 옛→새 번호 링크 이관 **2건 / 2노트**(R4 가 남긴 잔여 3건 중 2건 상환. 옛 14번과 새 19번이 **제목이 같고 번호만 다른** 쌍이라 줄 단위로 대조해 갈아끼움): [[OCR 메뉴 스캔 — 박스 그룹핑]]:10 · [[OCR 라인 분류 — 휴리스틱의 한계와 대안]]:11 — 둘 다 옛 `14. 메뉴판 스캔 판정 흐름` → 새 [[19. 메뉴판 스캔 판정 흐름 — 메뉴명이 위험도가 되기까지]]. 계획서 §4-2 권고대로 **20번을 병기**하고 "FE 가 OCR 결과를 보내는 건 v1 경로"를 그 줄에 못 박았다(서버 OCR v2 등장으로 "앱이 OCR 한다"가 더는 유일한 경로가 아니다). 남은 미상환 1건은 [[클라이언트 저장소 — AsyncStorage·SecureStore·메모리 (K-Bap 실전)]] → 옛 `21. 캐싱과 키-값 저장소`(새 29번, R10 대기)
- [2026-08-27] fix | 이관하며 낡아버린 FE 노트 서술 수리 2노트 — [[OCR 메뉴 스캔 — 박스 그룹핑]]:10 의 `POST /api/v1/scans` 를 **`POST /api/scans`** 로 교정(kbap 은 URL 버전리스, `X-API-Version` 헤더로 가른다). 두 노트 하단에 날짜 붙인 `> [!note]` 로 실측 대조를 남김: ① 필드명 `itemId`(FE) = `idx`(서버 DTO) ② **서버가 사진을 비전 LLM 으로 다시 읽는다** — FE OCR 은 힌트일 뿐이고 응답 기준은 비전 추출 메뉴라 **응답 배열 길이 ≠ 요청 items 길이, `idx` 로 짝을 맞춰야 한다**(kb-320) ③ [[OCR 라인 분류 — 휴리스틱의 한계와 대안]] §5 가 예고한 **카탈로그 퍼지 매칭(Levenshtein 3단계)은 채택되지 않았다** — 서버는 편집거리도 형태소 분석도 없이 `matchKey` 세 줄로 정확 일치만 본다. 대신 **정제를 앞으로(비전 모델) 옮겨 뒤를 단순하게** 만든 것. 그 노트의 결론("카탈로그 매칭 = 최고의 분류기")은 살아남았고 위험하다던 편집거리 구간은 아예 안 밟았다. §5 제목의 `(BE 작업 중)` 도 `(2026-07 예고안 — 실제 구현은 다르게 풀렸다)` 로 교체
- [2026-08-27] update | 복리 3짝(하단 append·본문 불변, 양방향 완성): [[S3 presigned URL과 클라이언트 시크릿]] ↔ 21 (이 노트가 *"발급 API 미배포"* 로 끝나 있던 그 API 가 배포됐다. 원리편 → 구현편. 설계가 코드에서 달라진 곳은 하나 — **2단계가 아니라 3단계**다. 서버는 서명만 하고 업로드 성공을 모르므로 클라가 `complete` 로 신고해야 `uploaded_image` 행이 생기고, 그 행이 곧 소유 증명이 되어 리뷰·커뮤니티·주문이 대조한다) / [[쿠키·세션 vs 토큰 인증 — 왜 앱은 JWT인가]] ↔ 20 (**JWT 를 "너 누구냐" 말고 "이거 해도 되냐"에 쓰는 두 번째 용례** — 스캔 티켓. 서명 키까지 access 토큰과 같고 `token_type` 클레임 하나로 가른다. 실패가 401 이 아니라 **400** 인 이유: 티켓 검사 시점엔 신원이 이미 통과했다) / [[iOS 앱 용량 — 네이티브 의존성과 ML Kit 모델]] ↔ 20 (**450MB 사건의 뿌리가 사라지는 자리** — v2 는 앱이 글자를 안 읽으니 ML Kit 의존성 자체가 빠진다. 다만 오프라인 불가 + 호출당 LLM 비용이라 **앱 용량을 서버 비용과 맞바꾼 것**. 그 노트 「열린 질문」 1번은 v2 로 넘어가면 질문 자체가 없어지는 종류). 지도 19·20·21 `[x]` + 위키링크 승격·진도표 R4 ✅/R5 ✅ 방금/R6 ⬅ 다음·완료 15→18. 홈 kbap 줄 `(집필 중: 15/29)` → `(집필 중: 18/29)`
- [2026-08-27] update | R5 실측 정정·확정 6건(노트 19·20·21 에 반영됨): ① **위험도는 "4단계"가 아니다** — enum 값은 `SAFE`·`CAUTION`·`DANGER`·`UNKNOWN` 4개지만 severity 0·1·2 로 **서열이 있는 건 셋뿐**이고 `UNKNOWN` 은 `-1` 로 척도 **밖**(판정 불가)이라 `aggregate` 가 `maxBy` 이전에 따로 가로챈다. 임계값 `10`·`60` 은 **하드코딩**(설정도 DB 값도 아님). 그리고 **기피 성분을 아예 등록 안 한 회원은 전 항목이 `SAFE`** — "안전 확인됨"과 "확인할 게 없었음"이 같은 값 ② **스캔 v2 는 이미지 바이트를 받지 않는다** — 본문은 `imagePath` 문자열 하나뿐이고 `^(?!https?://).*` 로 전체 URL 을 막는다. 비전 모델조차 서버가 내려받아 base64 로 싣지 않고 **CDN 에서 직접 가져간다**(`image-base-url`) — 그래서 사진이 공개 URL 로 접근 가능해야 한다는 제약이 21번과 물린다 ③ **고아 객체 회수 장치가 없다**(2026-08-27 실측) — 업로드만 되고 `complete` 가 안 오면 S3 에 파일이 남는데, `StorageObjectStore.delete` 호출은 검증 실패 두 줄뿐이고 정리 잡도 `iac/terraform/` 의 S3 수명주기 규칙도 없다. **의도된 미구현**(`specs/kb-145-presigned-url/research.md:80` 이 범위 밖으로 명시). 서버가 발급 원장을 안 남기는 설계라 DB 대조도 불가 — 답은 버킷 수명주기 규칙뿐 ④ **티켓이 JWT 인 이유는 무상태**다 — 발급 기록을 아무 데도 저장하지 않아 A 서버가 발급한 걸 B 서버가 검증하고, 만료가 토큰 안에 있어 TTL 관리 코드가 0줄. `jti`(UUID)가 그대로 Redis 예약 키가 된다. 서명 키를 access 토큰과 **공유**하므로 `token_type` 클레임 검사가 필수 안전장치 ⑤ **Redis Lua 는 원자성만 산다** — 만료청소→중복검사→한도검사→자리잡기 11줄 사이에 아무도 못 끼어들게 해 check-then-act 의 틈을 없앤다. DB 비관 잠금은 **수 초짜리 LLM 호출을 잠금 안에 가두므로** 기각됐고, 조건부 UPDATE 선점은 보상 전 크래시 시 무료 1회가 영구 유실돼 기각됐다(`research.md:24,33`). **다만 Lua 는 트랜잭션이 아니다 — 롤백이 없다** ⑥ **v1 도 비전 LLM 을 부른다** — 한도가 v2 전용인 건 비용이 아니라 **호환성** 때문(옛 앱은 티켓 API 를 모른다). 즉 v1 은 비용은 그대로 나가면서 한도 밖이다. 노트가 명시적으로 정정한 사실 2건이 ①(옛 노트의 "위험도 4단계")과 ⑥("v2 만 LLM 을 쓴다")이다
- [2026-08-27] lint | 린트 #16(R5): 볼트 전역 68노트·위키링크 833건 전수대조(NFC 정규화 후 파일명 대조) — **라이브 노트 깨진 링크 0**(미해결 3건은 전부 작업 로그 2026-07-14 줄의 append-only 알려진 예외 `:45`·`:53`·`:56`, 그 외는 `[[링크]]`·`[[대괄호]]`·`[[예시]]` 류 문법 설명 텍스트와 `[[대상\|별칭]]` 파이프 이스케이프) / 새 3노트 → `kbap 백엔드 (old)/` **유입 0** / **`kbap 백엔드 (old)/` 로 들어가는 외부 인바운드 잔여 1건**(R4 의 3건에서 2건 상환): [[클라이언트 저장소 — AsyncStorage·SecureStore·메모리 (K-Bap 실전)]] → 옛 `21. 캐싱과 키-값 저장소`(새 29번, **R10 대기**). [[🏠 홈]] → 옛 지도는 **의도된 📦보관 등록**이라 이관 대상 아님 / **유령 노드 0**(지도 위키링크는 4~21 + 홈뿐, 미집필 11개(1~3·22~29)는 `- [ ]` 일반 텍스트 유지) / 백틱 래핑 `` `[[...]]` `` **0** / 상대링크 `](../` **0** / **머리말 두 줄 서식 + 머리 블록 19~21 전부 준수**(🏠홈·📂지도 / ⬅️앞·➡️다음 / 🔗곁, H1 아래 `> 작성·맥락`+`> 목표` → `---` → `## 0. 한 줄 요약`. 21 의 `➡️ 다음: 22…(집필 예정)` 도 규약대로 일반 텍스트 — **직접 수정 0건**) / frontmatter 3노트 전부 `상태: 완료`·파일명 = H1 = 링크 대상 일치(지도는 `진행중` 유지, 18/29 라 정상. 볼트 전역 `상태: 진행중` 은 지도와 템플릿 둘뿐) / **고아 0**(인바운드 19:4노트·20:7노트·21:3노트) / 홈·지도 등록 확인. 품질 검수: 19↔20 경계 **상충 0**(v1 도 LLM 을 부른다·`requireDetectedMenu` 플래그·판정 엔진 공유가 양쪽 동일 서술), 20번 754줄은 **분할 불필요**(18절 중 최대 91줄, 한 요청의 한 흐름으로 읽힌다), Lua·원자성·check-then-act 는 §1 개념표에 FE 대응물과 함께 선정의됨 — 지목 3건 보고 + **직접 수리 1건**: 노트20 용어집의 `degraded` 가 "LLM 정제 실패를 알리는 플래그"로 살아 있는 것처럼 적혀 노트19 §8-3("하드코딩된 죽은 필드, 항상 `false`")과 어긋났다 → 실측대로 고치고 19번 정본으로 링크
- [2026-08-27] ingest | kbap 백엔드 재구축 R6(5단계 쓰기 ① 2흐름): [[22. 리뷰 작성 흐름 — 쓰기 경로와 랭킹 원장]](737줄 — **첫 다중 테이블 쓰기 트랜잭션**. `POST /api/reviews` 하나가 읽기 3테이블(food·scan_history·uploaded_image) + 쓰기 3테이블(member·food_review·member_ranking_event)을 한 트랜잭션으로 움직인다. **랭킹 원장** = 잔액(카운트 컬럼)과 별개의 거래 내역 테이블 · `countByMemberIdAndFoodId == 1L` 이 성립하는 근거는 Hibernate auto-flush · `^/api/reviews$` 게스트 예외의 `^`·`$` · `ReviewRepositoryCustomImpl` 이 존재하는 이유는 정렬 5종 × 커서 2종 × 필터 4종의 조합 폭발 · `latest` 커서만 숫자 하나인 건 하위 호환 · 차단/신고는 **뷰어 필터**라 집계와 남의 화면은 안 건드림 · `listOf(-1L)` 센티널 · `ON DUPLICATE KEY UPDATE` 를 쓰는 진짜 이유는 "트랜잭션 안 `save()` 예외 폴백이 rollback-only 마킹으로 불가" · 50건 목록 = 쿼리 5개 고정) / [[23. 회원 랭킹 — 원자적 카운트와 조회 시점 점수 계산]](568줄 — 주제는 랭킹이 아니라 **lost update**. 반례 코드 → 3단계 시나리오 → 해법 셋(원자 UPDATE·비관·낙관) 비교 → "현재 값을 안 봐도 되는 갱신이면 원자 UPDATE" 한 줄 규칙. `score`·`tier` 컬럼은 **없고** 매 조회마다 `Ranking` 값 객체가 계산 · `@Modifying` 반환값은 **바뀐 행 수** 라 0이면 예외 · 하한 가드 `and m.reviewCount > 0` 을 코드가 아니라 WHERE 절에 둔 이유 · `flush(전)→UPDATE→clear(후)` 짝의 청구서는 "같은 트랜잭션의 1차 캐시가 날아간다") — 2노트, 22↔23 상호링크(22 §6 = 원장 적재 정본 / 23 §8 = 왜 필요한가) + 외부 3노트 양방향 완비
- [2026-08-27] update | ⭐ R6 실측 정정 — **랭킹 점수에 무엇이 기여하는가**(두 작성자 교차 확인): 옛 노트(`kbap 백엔드 (old)/18. 회원 랭킹 흐름`)와 KB-123 스펙은 *"리뷰 도메인이 없어 reviews·diversity 는 항상 0, 점수는 스캔만 반영"* 이라고 적었다. **지금은 셋 다 살아 있다.** `ReviewService.createReview` 가 `:62 increaseReviewCount`·`:80 increaseUniqueReviewedFoodCount`, `deleteReview` 가 `:119 decreaseReviewCount`·`:122 decreaseUniqueReviewedFoodCount` 를 부른다(레포 재확인 완료). 가중치는 `Ranking.kt:21-23` 의 **리뷰 10점 · 다양성 5점 · 스캔 2점**, 등급 7단계 `RankingTier.kt`. 즉 **점수 기여 3요소 전부 가동**이고, 스캔은 `ScanFacade.kt:32`(v1)·`:49`(v2)에서 계속 들어온다. 낡은 건 **문서뿐**이고 코드는 정상 — 낡은 자리 3곳을 노트 23 §13 이 표로 못 박았다: ① `MemberApi.kt:167`(springdoc → **Swagger 에 그대로 노출**, "리뷰 기능 도입 전이라…" 문장 실물 확인) ② `specs/kb-123-member-ranking/spec.md` FR-011·Edge Cases ③ 같은 스펙의 contract 문서가 경로를 `/api/v1/members/me/ranking` 으로 적음(실제는 버전리스 `/api`). 흥미로운 건 FR-011 이 *"리뷰 기능이 추가되면 산정 규칙을 고치지 않고도 값이 반영돼야 한다"* 를 요구했고 실제로 `Ranking` 은 **한 줄도 안 고쳐졌다**는 것 — 호출자만 늘었다
- [2026-08-27] update | R6 실측 정정·확정 4건(노트 22·23 에 반영됨): ① **리뷰 자격 조건이 생겼다**(`specs/kb-369-review-eligibility/`, 2026-08-21) — 아무나 못 쓴다. `scanHistoryRepository.existsByMemberIdAndFoodId` 가 참이어야 하고 아니면 `REVIEW-004`/**403**. 음식 매칭 실패(`food_id` 미충전)·삭제된 스캔 이력(`@SQLRestriction`)도 자격 없음. **`PATCH` 수정은 재검증 안 한다**(이미 쓴 리뷰가 스캔 이력 삭제로 수정 불가가 되면 이상하니까) / 검사 순서가 음식 → 자격이라 없는 음식은 `FOOD-001` 이 먼저 / 작성 후 403 UX 를 막으려 음식 상세에 `reviewEligible` boolean 동봉 ② **삭제는 원장 행을 지우지 않는다 — 상쇄(보상) 이벤트다.** `REVIEW_CREATED +1/+1` 행은 그대로 두고 `REVIEW_DELETED -1/0` 행을 **추가**한다(`MemberRankingEvent.kt:39-55`). 리뷰 본체도 소프트 삭제. 델타를 전부 더하면 잔액과 맞고, 그 정합성을 `ReviewControllerTest.kt:818-829` 가 JDBC 직접 SELECT 로 검증한다 ③ **`uq_member_ranking_event(review_id, event)` 는 원자성과 별개인 멱등 장치다.** `SET c = c + 1` 이 완벽히 원자적이어도 그 문장이 **두 번 실행되면** 카운트는 여전히 틀린다 — 원자성은 "동시에 와도 둘 다 반영"을 보장할 뿐 "같은 사건이 두 번 반영되면 안 된다"엔 아무 말도 안 한다. `> 0` 하한 가드도 못 막는다(리뷰 5개 회원의 삭제가 두 번 반영되면 3이 되고 3은 음수가 아니다 — **틀렸지만 그럴듯한 값**). `event` 를 키에 포함한 건 같은 리뷰가 CREATED 1행 + DELETED 1행을 가져야 해서 — 막는 건 "같은 리뷰의 재등장"이 아니라 "**같은 사건**의 재등장" ④ 중복 삭제 방어는 **3겹**(소프트삭제 조회 차단 400 → 원장 exists 가드 400 → `@Version` + unique 409)이고 `ReviewControllerTest.kt:897-920` 이 `CountDownLatch` 로 동시 삭제를 쏴서 **`(400, 409)` 중 아무거나 허용하되 카운트는 1번만 깎일 것**을 검증한다 — 그 느슨함 자체가 요구사항의 정확한 표현
- [2026-08-27] update | 복리 3짝(하단 append·본문 불변, 양방향 완성) + 승격 3건: [[페이징 — Offset vs No-Offset(Keyset·Cursor)]] ↔ 22 (**8절 5번 "복합 정렬이면 커서에 tie-breaker(id) 포함"의 실물** — 정렬 5종마다 지표 SQL 조각이 갈리고 커서 조건이 `(지표 비교) or (지표 동점 and r.id <)` 로 조립된다. 인코딩도 `latest` 만 숫자, 나머지는 `"지표_id"` base64url 불투명 토큰이라 **정렬을 바꾸면 커서를 버려야 한다** — 원리 노트에 없던 운영 제약) / [[20. 스캔 v2와 사용량 티켓 — 서버 OCR·무료 한도·중복 요청 막기]] ↔ 23 (**같은 동시성 문제의 반대편 해법** — 20 의 Redis Lua 는 "현재 값을 보고 판단"해야 해서 읽기·쓰기를 감쌀 도구가 필요했고, 값을 볼 필요 없는 `+1` 에는 DB 원자 UPDATE 한 방이면 끝. 20 §6 의 `scan_unlocked` 동승 UPDATE 가 바로 그것) / [[21. 이미지 업로드 흐름 — presigned URL과 업로드 완료 확정]] ↔ 22 (**`complete` 한 이미지의 소비처** — `uploaded_image` 행을 `verifyImageOwnership` 이 되찾아 "내 것 · REVIEW 용도"를 대조하고 아니면 `REVIEW-003`. 21 이 말한 "고아 객체 회수 장치 없음"의 실물도 여기 — 리뷰 트랜잭션이 롤백돼도 S3 파일은 트랜잭션 밖이라 남는다). 승격: 20 `:400`·`:759`·21 `:9` 의 `22. …(집필 예정)` 일반 텍스트 **3건 → 위키링크**, 20 흐름 노트 줄에 23 추가. 지도 22·23 `[x]` + 위키링크 승격·진도표 R5 ✅/R6 ✅ 방금/R7 ⬅ 다음·완료 18→20. 홈 kbap 줄 `(집필 중: 18/29)` → `(집필 중: 20/29)`
- [2026-08-27] fix | R6 QA 직접 수리 2건 — ① **노트22 의 응답 봉투 필드명이 틀렸다**: §9-1·§9-2 JSON 예시가 `"data": { … }` 였는데 실제는 `"payload"` 다(`api/core/BaseResponse.kt:5` 재확인). [[7. API 계약의 모양 — 공통 봉투·에러 코드·헤더 버저닝]] 정본·노트23 §11 예시와도 어긋났고, **FE 가 그대로 베끼면 `undefined` 가 나오는** 종류의 오류라 2곳 교정 ② 노트22 §6-4 의 `TOCTOU` 가 정의 없이 괄호로만 첫 등장했다(용어집 정의는 440줄 뒤) → 첫 등장 자리에 "time-of-check to time-of-use, 검사 시점과 사용 시점이 어긋나는 경합" 인라인 풀이 삽입
- [2026-08-27] lint | 린트 #17(R6): 볼트 전역 70노트·위키링크 918건 전수대조(NFC 정규화 + 코드펜스 제외 후 파일명 대조) — **라이브 노트 깨진 링크 0**(미해결 3건은 전부 작업 로그 2026-07-14 줄의 append-only 알려진 예외, 그 외는 `[[링크]]`·`[[대괄호]]`·`[[예시]]` 류 문법 설명 텍스트) / 새 2노트 → `kbap 백엔드 (old)/` **유입 0** / **`kbap 백엔드 (old)/` 외부 인바운드 잔여 1건**(R5 대비 변동 없음): [[클라이언트 저장소 — AsyncStorage·SecureStore·메모리 (K-Bap 실전)]] → 옛 `21. 캐싱과 키-값 저장소`(새 29번, **R10 대기**). [[🏠 홈]] → 옛 지도는 의도된 📦보관 등록이라 이관 대상 아님 / **유령 노드 0**(지도 위키링크는 4~23 + 홈뿐, 미집필 9개(1~3·24~29)는 `- [ ]` 일반 텍스트 유지) / 새 노트 백틱 래핑 **0** · 상대링크 `](../` **0**(잔여는 전부 메타 문서·old 폴더의 문법 설명) / **머리말 두 줄 서식 + 머리 블록 22·23 준수**(🏠홈·📂지도 / ⬅️앞·➡️다음 / 🔗곁, H1 아래 `> 작성·맥락`+`> 목표` → `---` → `## 0. 한 줄 요약`. 23 의 `➡️ 다음: 24…(집필 예정)` 도 규약대로 일반 텍스트 — **서식 직접 수정 0건**) / frontmatter 2노트 `상태: 완료`·파일명 = H1 = 링크 대상 일치(지도는 `진행중` 유지, 20/29 라 정상) / **고아 0**(인바운드 22: 5노트 · 23: 3노트) / 홈·지도 등록 확인. 품질 검수: **22↔23 경계 상충 0**(원장 적재는 22 §6 정본 / 점수 계산·"왜 원장이 필요한가"는 23 §8 — 양쪽이 서로를 정본으로 명시해 넘긴다. `uq_member_ranking_event` 서술도 각도가 다름: 22 는 TOCTOU·3겹 방어, 23 은 "원자성 ≠ 멱등"), **20↔23 상충 0**(양쪽 다 "비관 락은 수 초짜리 LLM 호출을 잠금 안에 가둬서 기각" 동일 서술), 23 의 실측 단정 5건 레포 재확인 완료(마이그레이션 49개에 `score`·`tier` 컬럼 0 · `leaderboard` 0건 · `PESSIMISTIC`/`@Lock` 0건 · `@Version` 은 `Review`·`Food` 둘뿐 · `MemberRankingEventJpaRepository` 메서드 1개), **22 의 737줄은 분할 불필요**(16절 중 최대 90줄대, 신고·차단이 §8 한 절로 눌려 있고 "그 행이 남의 조회 결과를 바꾼다"는 한 줄이 동거 이유를 정당화한다 — 쪼개면 `GET /api/reviews` 의 제외 목록 설명이 두 노트로 갈린다) — 직접 수리 2건(위 `fix` 줄), 작성자 반려 0건
- [2026-08-27] ingest | kbap 백엔드 재구축 R7(5단계 쓰기 ② 2흐름): [[24. 주문 흐름 — 스캔 결과가 주문 이력이 되기까지]](697줄 — **9번(연관관계 없는 설계)의 실전판**. 주문→항목→음식→이미지 네 단계가 전부 `Long` id 로만 이어져 있고 조합을 `OrderService` 가 손으로 한다. `loadFoodsById` 네 줄(id 모으기 → `distinct` → 빈 리스트 가드 → `findAllById().associateBy`)이 §3-1 손동작 그대로 · **상세도 목록도 쿼리 정확히 3번**이고 항목·주문 개수와 무관 · `imagePath` 하나가 소유권 증명·중복 방지 키·응답 사진 URL 3역 · 역지오코딩이 컨트롤러에 있는 이유는 **HTTP 왕복을 트랜잭션 밖으로** · `order_id` 에는 FK 가 있고 `food_id` 에는 없다 = "DB FK 와 JPA 연관은 별개") / [[25. 커뮤니티 흐름 — 포스팅·댓글·커서 페이징]](570줄 — 엔드포인트 9개, 로직 전부 `CommunityService` 한 파일. **자기참조 컬럼 `parent_id` 하나로 만든 1depth 대댓글**: `resolveTopLevelParentId` 의 `parent.parentId ?: parent.id` 한 줄이 "3depth 를 막는 검사"가 아니라 **"3depth 가 만들어질 수 없는 저장 형태"** · 댓글 조립 쿼리 3번(최상위 `size+1` / 대댓글 `IN` 일괄 / 작성자 `toSet` 일괄), 피드 조립 4번(글 수 무관) · 게스트 게이트는 DB 질의 없는 널 체크 두 줄 · 탈퇴자는 **글은 숨기고 댓글은 익명화**) — 2노트, 24↔25 상호링크(24 §12 비교표 ↔ 25 §4·§6) + 외부 1노트 양방향 완비
- [2026-08-27] update | ⭐ R7 조사 과제 **종결 — "게스트는 글을 읽는데 댓글은 401" 은 누락이 아니라 스펙 명시 의도**다(R4 에서 노트17 이 발견해 R7 로 넘긴 숙제). 근거 6개를 노트25 §7 이 표로 못 박았다 — `specs/kb-292-community-comments/spec.md:89` FR-010("게스트에게는 댓글 내용을 제공하지 않는다 — 목록 조회는 **인증 오류로 거부**하고, **댓글 개수만** 피드·글 상세 응답으로 노출한다. 블러 처리는 FE 책임") · 같은 spec `:46` 수용 시나리오 · `contracts/comments-api.md:5`("4개 엔드포인트 전부 회원 전용, `@AuthMemberIdOrNull` 사용처 없음") · 같은 문서 `:67`("**피드와 달리 게스트 첫 페이지도 불가**") · `CommunityApi.kt:139` swagger · `CommentReadControllerTest.kt:208-213` 이 게스트 401 을 테스트로 고정. 설계 의도는 **"보이되 못 읽는다"** — 글과 댓글 수까지 보여줘 활기를 전하고 대화 내용은 로그인 유도의 미끼로 쓴다. 노트17 의 콜아웃은 이미 `[!warning]` → `[!note]` 로 낮춰 수정됐고, 이번에 그 줄의 `25. …(집필 예정)` 텍스트를 위키링크 §7 로 승격했다. **남는 옥에 티는 에러 코드 불일치** — 같은 "로그인하면 볼 수 있어요" 상황인데 피드 2페이지는 `COMMUNITY-005`(서비스 로직이 판정), 댓글 목록은 `AUTH-003 INVALID_ACCESS_TOKEN`(필터가 던짐)이다. 스펙이 "기존 인증 오류"로 의도했으니 버그는 아니되 FE 분기가 갈린다. **교훈: 코드만으로는 "버그처럼 보이는 의도"를 구분할 수 없다** — 정규식이 `/comments` 를 안 잡는 게 아니라 잡으면 안 되는 것이었고, 그 판단은 스펙에만 적혀 있었다
- [2026-08-27] update | R7 실측 정정·확정 8건(노트 24·25 에 반영됨): ① **주문 상세·목록 쿼리 = 각 3번 고정**(`OrderService` 리포지토리 호출 줄 실측: 상세 `findById`+`findByOrderIdOrderByIdAsc`+`findAllById`, 목록 `findPageByMemberId`+`findByOrderIdInOrderByIdAsc`+`findAllById`). 연관관계를 썼다면 상세 `1+N`, 목록 30건×5항목이면 `1+30+150 = 181번`. `publicImageUrlOf` 가 **DB 를 안 가는 순수 문자열 함수**(`ImageUrls.resolve`)라야 이게 성립한다 ② **`saveAll` 은 배치 INSERT 가 아니다** — `hibernate.jdbc.batch_size` 설정이 레포 `application*.yml` 에 **0건**이고 `BaseEntity` 키 전략이 `GenerationType.IDENTITY` 라 Hibernate 가 id 회수를 위해 행마다 INSERT 를 따로 보낸다. 항목 3개면 INSERT 3번(상한 50개라 실전 문제는 아니지만 "`saveAll` = 쿼리 1번"은 틀린 암기) ③ **중복 주문 방어 두 겹의 역할이 다르다** — `existsByImagePath` 는 시간차 재주문용 **정중한 에러 메시지**이고 동시 요청은 둘 다 통과시킨다. 진짜 보증은 `uq_orders_image_path`. 그래서 `save` 가 아니라 **`saveAndFlush`** 여야 제약 위반이 `try` 블록 **안에서** 터져 `ORDER-003` 409 로 번역된다(`save` 면 커밋 시점에 터져 500 으로 샌다) ④ **그 제약 이름이 두 곳에 하드코딩**돼 있다(레포 재확인: `V2026.08.20.16.53.09__order_tables.sql:16` 과 `OrderService.kt:140` 의 `IMAGE_PATH_UNIQUE_KEY`). `DataIntegrityViolationException` 이 "무슨 제약인지"를 구조화해 주지 않아 **예외 원인 체인을 훑어 문자열을 찾는** 구조라, 마이그레이션에서 이름을 바꾸면 컴파일·테스트는 통과하고 실전 더블탭만 500 이 된다 ⑤ **소프트 삭제와 유니크 제약이 다른 세계를 본다** — `@SQLRestriction("status='ACTIVE'")` 때문에 JPA 조회는 `ACTIVE` 만 보는데 `UNIQUE` 는 `DELETED` 행까지 전부 본다. 지금은 주문 삭제 API 가 없어(`OrderService` 에 `delete` 계열 0개) 잠재적이다 ⑥ **준비중(비 `READY`) 음식도 주문된다** — 커밋 `a2c339f9` 가 그 거절을 걷어냈고 `ORDER-001` 은 지금 `ErrorCode.kt` 에 없다. 대신 `takeIf { it.isReady() }` 로 **사진만 기본 대체 이미지**로 보낸다(`f5fd3c6e`) ⑦ **남의 주문은 403 이 아니라 404** — `findById(...).filter { it.memberId == memberId }` 가 소유자 불일치를 '없음'과 같은 갈래로 접는다. id 를 훑어 "403 이 뜨는 id = 실재하는 남의 주문"을 알아내는 걸 막는 의도 ⑧ **커뮤니티에는 차단 필터가 없다**(2026-08-26 실측) — 스펙(`kb-291.../spec.md:9`)이 "차단·삭제 필터를 이후 태스크에서 적용할 수 있도록 조회 경로를 한 곳으로 모은다"고 했고 **자리는 마련됐지만**(`CommunityService.kt:207` 주석) `viewerMemberId` 는 `assemble` 에 **전달조차 되지 않고** 게스트 판정에만 쓰인다. `CommunityService` 생성자 의존 6개에 `MemberBlockService` 없음. 차단·신고 필터가 실제로 도는 곳은 **리뷰뿐**(`ReviewService.kt:177-183`). 부수 관찰: 그래서 커뮤니티 피드는 **지금은 캐시를 붙일 수 있는 상태**다(뷰어별 목록은 캐시 키가 회원 수만큼 갈린다 — 29번으로 이어진다)
- [2026-08-27] update | 복리 1짝(하단 append·본문 불변) + 승격 5건 + 등록: [[페이징 — Offset vs No-Offset(Keyset·Cursor)]] ↔ 24·25 (R6 의 22번 역링크가 8절 5번 *"복합 정렬이면 tie-breaker 포함"* 의 실물이었다면 이번엔 **반대편** — 주문·커뮤니티는 정렬이 `id` 하나뿐이라 동점이 없고 커서가 **숫자 하나**로 끝난다. "언제 복합 커서가 필요한가"의 답은 **정렬 키에 동점이 생기느냐** 하나. 원리 노트에 없던 운영 디테일 셋: ① **정렬 방향이 반대면 커서 부등호도 뒤집힌다**(최신순 `id < :cursor` vs 등록순 `id > :cursor`) ② `size` 는 400 거절이 아니라 `coerceIn(1,30)` **조용한 클램프** ③ `totalCount` 부재가 FE 계약으로 그대로 샌다). 승격 5건: 노트17 `:207`(게스트 댓글 401 콜아웃 → 25 §7) · 노트19 `:395`(스캔 `image_path` 표 → 24) · 노트22 `:649-650`(§12 "같은 패턴이 두 번 더" 2줄 → 24·25) · 노트23 `:9`(머리 블록 `➡️ 다음` → 24) · 노트9 `:447`(§9 "뒤의 흐름 노트들에서 나온다" → 24 §8 + 25 §6-3 추가). 지도 24·25 `[x]` + 위키링크 승격·진도표 R6 ✅/R7 ✅ 방금/R8 ⬅ 다음·완료 20→22. 홈 kbap 줄 `(집필 중: 20/29)` → `(집필 중: 22/29)`
- [2026-08-27] fix | R7 QA 직접 수리 2건 — ① **24↔25 상충**: 노트24 §12 비교표의 커뮤니티 칸이 `조합 = 뷰어별 차단 필터` 였는데, 같은 회차 노트25 §5 가 **"커뮤니티에 차단 필터는 없다"** 를 실측으로 못 박았다(위 `update` ⑧). 22번에서 물려받은 낡은 서술이라 `단일 조립 지점(차단 필터는 아직 없다 — 25 §5)` 으로 교정 ② 같은 뿌리를 노트22 `:645` 에서도 잘라냈다 — "`order`·`community` 도 쓰기 트랜잭션 + 커서 페이징 + **`viewerMemberId` 차단 필터** + 소유자 검증 판박이" 문장에서 차단 필터를 빼고 "리뷰에만 있다"를 명시. 승격 편집과 같은 줄이라 함께 처리. **응답 봉투 필드명은 이번 2노트 모두 `payload` 로 정상**(R6 노트22 의 `data` 오기 전례가 있어 `BaseResponse.kt:5` 재확인 후 24 §9-1~9-3·25 §8 전수 대조 — 오기 0건)
- [2026-08-27] lint | 린트 #18(R7): 볼트 전역 72노트·위키링크 1,001건 전수대조(NFC 정규화 + 코드펜스 제외 후 파일명 대조) — **라이브 노트 깨진 링크 0**(미해결은 전부 작업 로그 2026-07-14 줄의 append-only 알려진 예외 3건과 `[[링크]]`·`[[대괄호]]`·`[[예시]]` 류 문법 설명 텍스트·`[[대상\|별칭]]` 파이프 이스케이프) / 새 2노트 → `kbap 백엔드 (old)/` **유입 0** / **`kbap 백엔드 (old)/` 외부 인바운드 잔여 1건**(R5 이후 변동 없음): [[클라이언트 저장소 — AsyncStorage·SecureStore·메모리 (K-Bap 실전)]]`:10` → 옛 `21. 캐싱과 키-값 저장소`(새 29번, **R10 대기**). [[🏠 홈]]`:26` → 옛 지도는 의도된 📦보관 등록이라 이관 대상 아님 / **유령 노드 0**(지도 위키링크는 4~25 + 홈뿐, 미집필 7개(1~3·26~29)는 `- [ ]` 일반 텍스트 유지) / 백틱 래핑 **0** · 상대링크 `](../` **0** / **머리말 두 줄 서식 + 머리 블록 24·25 준수**(🏠홈·📂지도 / ⬅️앞·➡️다음 / 🔗곁, H1 아래 `> 작성·맥락`+`> 목표` → `---` → `## 0. 한 줄 요약`. 25 의 `➡️ 다음: 26…(집필 예정)` 과 `:186` 의 `29. 캐싱…(집필 예정)` 도 규약대로 일반 텍스트 — **서식 직접 수정 0건**) / frontmatter 2노트 `상태: 완료`·파일명 = H1 = 링크 대상 일치(지도는 `진행중` 유지, 22/29 라 정상) / **고아 0**(인바운드 24: 7노트 · 25: 6노트) / 홈·지도 등록 확인. 품질 검수: **22↔24↔25 커서 중복 0** — 원리는 [[페이징 — Offset vs No-Offset(Keyset·Cursor)]], 복합 keyset 정본은 22 §7-2, 단순 id 정본은 24 §7-1, 25 §4 는 *"커뮤니티에서만 다른 두 가지"* 만 남기고 앞 셋으로 넘긴다(중복 설명 0줄) / **24 는 9번의 실전판 성립**(§8 이 주문→항목→음식 3단 배치 로딩을 코드로 보여주고 9 §3-1 을 줄 단위로 대응시킨다) / **25 대댓글 트리 N+1 없음 확인**(쿼리 3번, 다만 **대댓글은 페이징하지 않아** 최상위 하나에 300개 달리면 300개가 다 온다는 의도적 단순화를 노트가 스스로 밝힘) / 에러 코드 실측 재확인(`ErrorCode.kt` — `AUTH-003 = INVALID_ACCESS_TOKEN`, `COMMUNITY-001~007` 표와 일치) — 직접 수리 2건(위 `fix` 줄), **작성자 반려 0건**
- [2026-08-27] ingest | kbap 백엔드 R8(5단계 배치·비동기, 3노트 2,328줄): [[26. 배치 앱의 골격 — Spring Batch 잡·트리거·스케줄]](886줄 — 볼트 최장. 두 번째 실행 파일 `:batch`, Job·Step·청크, JobRepository 를 **JDBC 로 명시 조립하지 않으면 기본이 인메모리라 이력이 조용히 사라진다**, 트리거 3종(HTTP `/internal/batch` 202+폴링 · 인앱 cron · 컨테이너), `RunIdIncrementer` 가 없으면 잡이 **딱 한 번만 성공**하는 이유, 컴포넌트 스캔은 좁게·엔티티는 넓게) / [[27. 아웃박스와 SQS — 다른 시스템에 일을 넘기는 법]](791줄. 이중 쓰기(dual write)가 원리적으로 안 묶이는 이유 → 아웃박스, 상태 `PENDING→SENT→COMPLETE` 전이 4쿼리, **SQS 호출은 트랜잭션 밖**(테스트가 `isActualTransactionActive() shouldBe false` 로 단언), at-least-once + 멱등, 같은 앱 안 `@EventListener`+`@Async` 와 다른 시스템 큐의 12행 대비표) / [[28. 임베딩과 벡터 검색 — 뜻으로 음식 찾기]](650줄. 임베딩=뜻을 좌표로(RGB 비유와 **비유가 깨지는 자리**까지), 256차원·코사인, 벡터 저장소는 MySQL 이 아니라 **AWS DocumentDB**, `embeddingHash` 로 임베딩 호출 스킵, **적재는 살아 있는데 읽는 코드가 없다**)
- [2026-08-27] ingest | R8 실측 요지 — ① **배치 잡은 코드에 2개, 기본 설정에서 실제 뜨는 건 1개**(`foodVectorSyncJob` 은 `@ConditionalOnExpression` 으로 `kbap.vector.enabled` and `kbap.llm.embedding.enabled` 둘 다 true 여야 하고 둘 다 기본 `false` → 매시 30분 `UnknownJob` 로그만 찍힘, 에러 아님) ② **재시도·DLQ 는 두 아웃박스가 정반대**다 — `food_content_outbox` 는 시도 상한도 백오프도 kbap 쪽 DLQ 도 **없고** `attempts` 컬럼은 세기만 하고 **어떤 분기에도 안 쓰인다**(`WHERE` 에 조건 0건), `SENT` 인 채 결과가 안 오면 자동 복구 없이 관리자 대시보드 건수로만 보인다 / `food_vector_outbox` 는 `MAX_ATTEMPTS = 5` 초과 시 `FAILED` 격리 + `last_error`(500자) + `retry()` 복구. **경계를 넘느냐(랭체인 책임)가 상태 기계를 갈랐다** ③ 벡터 저장소 실체는 **AWS DocumentDB**(MongoDB 호환, `kbap.foods` 컬렉션, cosine, `$search`+`vectorSearch` — Atlas 의 `$vectorSearch` 와 다른 문법), MySQL 에 안 넣은 건 ANN 벡터 인덱스가 없어서 ④ **임베딩 벤더 교체(Bedrock Titan 1024 → OpenAI `text-embedding-3-small` 256) 때 재색인 배치를 하나도 안 만들었다** — 인덱스는 처음부터 256이었고(Titan 은 Spring AI 2.0.0 `TitanEmbeddingRequest` 가 `dimensions` 를 직렬화 못 해 256을 *보내려다 실패*한 잠복 결함, 차원 검증 `check` 한 줄이 잡아냄), `embeddingHash = sha256(모델명|차원|원문)` 이라 **모델명이 바뀌는 순간 전 문서 해시가 불일치 → 다음 배치가 자동 재임베딩**. 대신 그 재임베딩은 공짜가 아니고(음식 수 × 단가), 관리자 화면의 enqueue 버튼은 `not exists(operation=UPSERT)` 라 **"전량 재색인" 이 아니라 "미등록분 백필"** 이다
- [2026-08-27] ingest | R8 **이름과 실제가 다른 자리** — 스펙 폴더명이 `kb-328-food-vector-outbox` 라 27번의 `food_content_outbox` 처럼 **SQS 큐를 탈 것처럼 보이지만, 벡터 동기화는 큐를 타지 않는다.** 배치가 DB 행을 직접 읽어 OpenAI 임베딩을 만들고 DocumentDB 에 직접 적재한다 — SQS 도 다른 레포도 없다. 아웃박스 패턴에서 **"이중 쓰기를 트랜잭션 밖으로 미룬다"** 는 발상만 빌리고 큐 이야기(at-least-once·부하 평탄화·느슨한 결합)는 해당되지 않는다. 작성자 둘이 교차 확인해 27 §9·§18 과 28 §5 에 반영. **아웃박스 = 큐 라고 외우면 틀리는 자리다.** 같은 맥락의 실측 하나 더 — 28번 파이프라인은 **읽기 쪽 소비자가 0개**다(`FoodVectorSearcher` 빈은 만들어지고 아무도 주입받지 않는다). 스캔의 유사 음식 폴백이 2026-08-19 KB-343 으로 제품 결정에 의해 걷혔고 **적재 파이프라인만 의도적으로 존치**(다음에 켤 때 재색인 없이 바로 쓰려고). 임계값 기본값이 `0.0`(= 항상 채택)이었다는 것도 함께 기록 — 벡터 검색은 `WHERE` 와 달리 "못 찾음"을 반환하지 않고 **k=1 이면 무엇이든 하나를 돌려준다**
- [2026-08-27] update | R8 복리 — **승격 7건**(기존 노트가 `(집필 예정)` 평문으로 예고하던 자리를 위키링크로): 19`:187`·20`:484`·20`:759`(각주 목록)·12`:378` → 27번 / 11`:398`·21`:494`·25`:9`(머리 블록 ➡️다음) → 26번. **역링크 append 9건**(하단 `> [!note] 2026-08-27 역링크`, 본문 불변): [[6. 설정과 프로필, 트랜잭션 경계]](경계를 애너테이션이 아니라 `TransactionTemplate` 로 긋는 쪽의 실물 + 두 배치 잡의 스타일이 실제로 갈려 있다는 점) / [[7. API 계약의 모양 — 공통 봉투·에러 코드·헤더 버저닝]](**이 계약을 안 지키는 컨트롤러가 하나 있다** — `/internal/batch` 는 봉투도 버전 헤더도 인증 필터도 없다. 갈림의 기준은 내부/외부가 아니라 `/api` 인가 `/internal` 인가) / [[8. 영속성 컨텍스트와 엔티티 매핑 — 엔티티가 곧 도메인 모델]](변경 감지가 `save()` 없이 일하는 실물 vs 네이티브 UPDATE 한 문장으로 짠 정반대편, 갈라 쓰는 기준) / [[9. 연관관계를 쓰지 않는 설계 — N+1을 애초에 만들지 않기]](8절이 "별도 설계(벡터 검색 등)" 라고만 적어둔 그 설계의 실물 — 다만 읽는 코드가 걷혔다는 결말까지) / [[10. Flyway 마이그레이션 — 스키마를 코드로 관리하기]](**다른 앱이 쓰는 `BATCH_*` 9테이블을 api 마이그레이션이 만든다**, `:batch` 는 flyway 가 꺼진 게 아니라 의존성이 0줄) / [[12. 포트와 어댑터 — 외부 시스템을 갈아끼우는 자리]](3-2절 벤더 교체의 **데이터 쪽 후일담** + 같은 벡터 기능인데 저장소는 포트가 아니라 `common.domain.food.vector` 인 경계 사례) / [[13. 경계를 지키는 테스트 — ArchUnit이 강제하는 규칙들]](Gradle 이 막는 경계 vs ArchUnit vs 런타임 단언 — 3층의 순서) / [[23. 회원 랭킹 — 원자적 카운트와 조회 시점 점수 계산]](같은 "두 번 해도 한 번"을 unique 제약 아닌 **갱신 행 수 판정**으로 푼 실물, 409 를 실패로 읽으면 안 된다) / 볼트 밖 [[페이징 — Offset vs No-Offset(Keyset·Cursor)]](**커서를 쓰는 이유가 성능이 아니라 정합성**인 자리 — 도는 중에 결과 집합이 움직인다. 실패 행을 그 실행에서 다시 안 보는 파생 성질까지)
- [2026-08-27] fix | R8 품질 검수에서 **응답 봉투 필드명 오기 1건 수리** — 27번 §13-② 의 성공 응답을 `{ "success": true, "data": null, "error": null }` 로 적어 뒀는데, kbap 의 봉투는 `success`·`payload`·`message`·`code` 네 필드다(`api/core/BaseResponse.kt:3-8`). `data`·`error` 라는 필드는 존재하지 않는다. 적재 API 는 `ResponseEntity<BaseResponse<Unit>>` 에 `BaseResponse.ok(Unit)` 이므로 `{ "success": true, "payload": {}, "message": null, "code": null }` 로 교정하고 근거 한 줄을 붙였다(전역 `NON_NULL` 설정이 없어 null 필드가 그대로 직렬화된다는 것도 확인). **R6 노트22 에 이어 두 번째 `data` 오기** — 봉투 JSON 을 적는 자리는 매 회차 `BaseResponse.kt` 를 열어 대조할 것
- [2026-08-27] update | R8 등록 — 지도(`🗺️ kbap 백엔드 공부 지도 (2026-08)`) 배치·비동기 3항목 `[ ]` → `[x]` + 위키링크 승격, 진도표 R7 `✅ 방금` → `✅` / R8 `⬅ 다음` → `✅ 방금` / R9 `⬅ 다음`, 완료 수 **22 → 25 / 29**. [[🏠 홈]] kbap 줄 `(집필 중: 22/29)` → `(집필 중: 25/29)`. 29번은 R10 몫이라 일반 텍스트 유지(28번 머리 블록의 `➡️ 다음: 29…(집필 예정)` 도 그대로)
- [2026-08-27] lint | 린트 #19(R8): 볼트 전역 **79노트·위키링크 1,166건 전수대조**(R9 의 1·2·3 완성 반영 후 재측정)(NFC 정규화 + 코드펜스·인라인코드 제외, `[[대상\|별칭]]` 파이프 이스케이프 처리) — **라이브 노트 깨진 링크 0**(미해결 6건은 전부 `📜 작업 로그` 안: 2026-07-14 줄의 append-only 알려진 예외 5건 + 린트 #14 줄의 `[[wikilink]]` 문법 설명 텍스트 1건) / **고아 0** / 새 3노트 → `kbap 백엔드 (old)/` **유입 0** / **`(old)` 외부 인바운드 잔여 1건**(R5 이후 변동 없음): [[클라이언트 저장소 — AsyncStorage·SecureStore·메모리 (K-Bap 실전)]]`:10` → 옛 `21. 캐싱과 키-값 저장소`(새 29번, **R10 대기**). [[🏠 홈]]`:26`(옛 지도 📦보관 등록)과 작업 로그 1건은 의도된 것이라 이관 대상 아님 / **유령 노드 0** — R9 가 동시 집필 중이라 `1.`·`2.`·`3.` 을 잠시 유령으로 봤으나 R9 완료로 자동 해소(26·27·28 은 애초에 1~3 을 위키링크로 걸지 않았다) / 백틱 래핑 **0** · 상대링크 `](../` **0** / **머리말·머리 블록 26·27·28 준수**(frontmatter `상태: 완료` 3/3, 파일명=H1 일치 3/3, 🏠홈·📂지도 / ⬅️앞·➡️다음 / 🔗곁 3줄, H1 아래 `> 작성·맥락`+`> 목표` → `---` → `## 0. 한 줄 요약`) — **직접 수정 0건, 어긋난 자리 없음**
- [2026-08-27] lint | R8 인수인계 — **R9 keeper 몫으로 남긴 승격 후보 10건**: 노트 1·2·3 이 방금 생겼으므로, 볼트 곳곳의 `2. 요청 하나에 스레드 하나 — JVM 동시성과 Node 이벤트루프(집필 예정)` 류 평문 예고가 이제 낡았다 — 26`:347`·27`:529`·11`:398`·20`:29`·20`:235`·20`:756`·21`:64`·23`:68`·24`:109`·4`:9`(머리 블록 ⬅️앞). R8 keeper 는 26·27 두 곳도 **건드리지 않고 남겼다**(1~3 은 R9 소관이라는 지시). 옛 19·20번(배치 실행 골격·LLM 스코어링 파이프라인)이 주제 폐기된 자리를 이번 26·27·28 이 대체했다는 사실도 함께 기록 — 옛 19는 "돌고 죽는 배치"를 전제했는데 지금 배치는 **상시 기동 + 트리거 실행**이고, 옛 20의 3모델 병렬 LLM 스코어링은 **레포에서 통째로 사라져** kbap-langchain(별도 레포)으로 이관됐다(KB-301). 그래서 R8 은 옛 노트를 갱신한 게 아니라 **새 주제 3개로 다시 썼다**
- [2026-08-27] ingest | kbap 백엔드 재구축 R10(확장·마무리): [[29. 캐싱과 키-값 저장소 — kbap이 Redis를 쓰는 두 가지 방식]](520줄 — 캐시의 세 질문(무엇을·어디에·언제 버리나)에서 출발해 **kbap 에 캐시 계층이 0개**라는 실측(`@Cacheable`·`@EnableCaching` 레포 전체 0건)으로 뒤집고, Redis 의 네 성질(프로세스 밖·TTL·원자적 실행·단순 키-값)을 refresh 세션(17번)·스캔 예약(20번) 두 용도에 대응시킨다. `GETDEL` 이 refresh 동시 회전 경합을 막는 자리, ShedLock 은 Redis 가 아니라 DB 라는 함정, `docker-compose` 에 Redis 볼륨이 없는 이유까지) — 커리큘럼의 마지막 노트
- [2026-08-27] update | R10 복리 — **재구축 시작부터 남아 있던 마지막 old 인바운드 상환**: [[클라이언트 저장소 — AsyncStorage·SecureStore·메모리 (K-Bap 실전)]] 머리말의 `🔗 서버 반대편` 이 옛 `21. 캐싱과 키-값 저장소` 를 가리키고 있던 것을 새 [[29. 캐싱과 키-값 저장소 — kbap이 Redis를 쓰는 두 가지 방식]] 로 이관하고 하단에 날짜 붙인 역링크 append(FE 의 민감도×수명 두 축이 서버에선 **"프로세스 밖에 둬야 하나"** 셋째 축이 붙어 MySQL→Redis→JVM 힙으로 반복된다 + §1 표에서 진짜 캐시는 TanStack Query 행 하나뿐이라는 대조). 이관 김에 **주변 서술의 거짓 1건 수리** — 옛 링크에 딸린 설명이 "FE는 토큰을 SecureStore에" 였는데 그 노트 §0·§1 이 **JWT 는 메모리, SecureStore 는 OAuth 크리덴셜**이라고 적고 있어 자기모순이었다. 걷어내고 수정 사유를 노트 하단에 남김. / [[쿠키·세션 vs 토큰 인증 — 왜 앱은 JWT인가]] ↔ 29 양방향 신설(29→그쪽은 §5 에 이미 있었고 역방향이 비어 있었다 — "JWT 가 세션 공유를 없앤 게 아니라 access 에서만 없애고 refresh 로 옮긴 것" 이라는 논지로 연결)
- [2026-08-27] update | R10 승격 — 29번을 `(집필 예정)` 평문으로 예고하던 자리 **7건** 위키링크 승격: 1`:301`·2`:234`·12`:274`·17`:385`·20`:759`(각주 목록)·25`:186`·28`:9`(머리 블록 ➡️다음). 이어서 **`kbap 백엔드 (old)` 21노트 + 옛 지도의 머리 배너 22건**도 전수 승격 — "지금 기준 노트 → 새 커리큘럼 N번 「…」(집필 예정) · 전체 목차 → 새 지도(집필 예정)" 이 이제 전부 실재하므로 `[[새 노트|N번 「…」]]` 별칭 링크 + 새 지도 링크로 갈아끼웠다. 이로써 **볼트 전체 `(집필 예정)` 0건**(작업 로그 제외)
- [2026-08-27] fix | R10 수리 2건 — ① 옛 14번(메뉴판 스캔 판정 흐름)의 **백틱에 갇힌 위키링크 11개** 해제(`` `[[…]]` `` → `[[…]]`). 전부 코드 예시가 아니라 실제 항해 링크였는데 백틱 때문에 죽어 있었다 — 옛 4·5·7·8·9·10·11·12·13번과 Expo 의 OCR 2노트로 가는 길이 되살아났다. ② 승격 작업 중 내가 넣은 공백 정리 정규식이 과했다 — old 폴더 22노트의 `A · B` 가운뎃점 앞 공백 **100군데**를 `A· B` 로 만들었고, 같은 회차에서 역변환으로 전수 복구·검증(`[^ ]· ` 잔여 0). 볼트 전역에 후행 공백이 0인 하우스 스타일이라 줄바꿈 쪽 피해는 없었음을 확인
- [2026-08-27] update | R10 등록 — 지도(`🗺️ kbap 백엔드 공부 지도 (2026-08)`) **완성 상태로 전환**: 1·2·3(R9 몫이 미등록으로 남아 있었다)·29 `[x]` + 위키링크 승격으로 **29/29 전부 체크·전부 링크**(평문 예고 0), 진도표 R9 `✅`/R10 `✅ 완주`, 완료 수 **25 → 29 / 29**, frontmatter `상태: 진행중` → **`완료`**. 낡은 "노트가 완성될 때마다 링크가 살아납니다" 안내를 걷어내고 **커리큘럼 완주 요약**(29노트·약 17,000줄·76만 자·5단계+확장, 옛 21노트 대비 2주제 폐기 8주제 신설)과 **읽는 순서 제안**(처음이면 1번부터 / 급하면 5→7→15 / 면접이면 9·13·23·29)으로 교체. [[🏠 홈]] kbap 줄 `(집필 중: 25/29)` → `29노트 완주`, 옛 지도 📦 보관 줄은 유지. 옛 지도 본문의 "새 지도로 이관 중" 도 완주 문구로 갱신
- [2026-08-27] lint | 린트 #20(R10, 볼트 전면 — kbap 폴더뿐 아니라 전 폴더): **79노트·위키링크 1,279건 전수대조**(NFC 정규화, 코드펜스·인라인코드 제외, `[[대상|별칭]]`·`#헤딩` 처리) — **라이브 노트 깨진 링크 0**(미해결 6건은 전부 `📜 작업 로그` 안: 2026-07-14 줄의 append-only 알려진 예외 5건 + 린트 #14 줄의 문법 설명 1건) / **유령 노드 0**(`(집필 예정)` 평문 0건) / **고아 0**(신규 29노트의 최소 인바운드는 8건) / **`(old)` 외부 인바운드 = 의도된 1건만**([[🏠 홈]]의 📦 보관 줄. 그 외 2건은 작업 로그의 과거 기록으로 append-only 불변) / **`상태: 진행중` 방치 0** / **홈 미등록 0**(29노트는 지도 경유, Claude Code 6노트는 그쪽 지도 경유 — 볼트 규칙이 허용한 방식) / **백틱 래핑 위키링크 = 문법 예시 6건만 잔존**(홈·볼트 규칙·작업 로그·Obsidian 활용법·옛 18번의 의도적 예시) / **상대링크 `](../` 0**(1건은 작업 로그 안 경로 표기) / frontmatter 누락 0 / 머리 블록 서식 이탈 0. 파일명≠H1 17건은 전부 **H1 에 부제를 더 붙이는 기존 하우스 스타일**(새 29노트는 전건 일치)이라 미수정
- [2026-08-27] setup | ⭐ **kbap 백엔드 재구축 완료 선언 (2026-08-26 ~ 08-27, R0~R10 11회차)** — 옛 21노트를 `kbap 백엔드 (old)/` 로 보관 이동(R0)하고 새 커리큘럼 **29노트·약 17,000줄**을 `kbap 백엔드 (new)/` 에 신규 집필. 사유는 레포 격변(모듈 8→3, UseCase 계층 소멸, JPA 연관관계 전면 금지, 배치 Spring Batch 재작성). **주제 2건 폐기**(옛 19 "조사 대기열 순회" 구조 제거 / 옛 20 LLM 스코어링 파이프라인 `kbap-langchain` 이관), **주제 8건 신설**(API 계약·ArchUnit·이미지 업로드·리뷰·주문·커뮤니티·아웃박스와 SQS·임베딩과 벡터 검색). 누계 — **기존 볼트 노트 수리 건수 약 20건**(거짓 서술 2·응답 봉투 필드명 1·"매 요청" 범위 1·클라이언트 저장소 SecureStore 1·백틱 갇힌 링크 11·가운뎃점 공백 일괄 1 등), **old→new 링크 이관 누계 3건**(Claude Code 노트 2 + 클라이언트 저장소 1)에 더해 **old 배너 22건 승격**. 재구축 시작 시점의 old 인바운드 4건이 이제 **의도된 1건**(홈 📦)만 남았다
- [2026-08-27] ingest | kbap 백엔드 R9(1단계 기초, 3노트 1,464줄 — **커리큘럼의 첫머리를 맨 마지막에 썼다**): [[1. 서버는 켜두는 프로그램 — 프로세스·포트·두 개의 실행 파일]](533줄. 프로세스·포트·바인딩·`main`·jar/bootJar 7개 용어만 쓰고 스프링·빈·계층은 한 번도 안 꺼낸다 / "켜 둔다"는 `while(true)` 가 아니라 **커널이 깨워 주기를 기다리는 대기**(CPU 0%)이고, `npx expo start` 의 멈춘 커서가 정확히 그 상태 / `main` 이 **두 개**(`KbapApiApplication.kt` · `KbapBatchApplication.kt`), 모듈은 3개인데 bootJar 는 2개(`:common` 에 `main` 없음) / **§7-1 부팅 거부 4종**(JWT 시크릿 32바이트 미만 → `require` 로 조립 실패 / 포트 점유 / `ddl-auto: validate` 스키마 불일치 / `.env` 없음)을 fail-fast 로 묶어 설명 / §9 에 요청 흐름 6단계 → 노트 번호 대응표) / [[2. 요청 하나에 스레드 하나 — JVM 동시성과 Node 이벤트루프]](371줄. 톰캣 워커 200개 vs 이벤트루프 1개, "JS 는 기다리지 않기로, JVM 은 기다리는 놈을 여러 명 두기로 동시성을 얻는다" / 대가 3종을 한 구조로 묶음 — 싱글턴 빈의 `var` 필드 · lost update · check-then-act / §8 `ThreadLocal` 이 트랜잭션·영속성 컨텍스트·MDC 요청ID 의 공통 바탕이라는 정리) / [[3. JVM 위의 Kotlin — 컴파일·널 안전·Gradle 3모듈]](565줄. TS 대비 축 7개 표 / **널 검사가 바이트코드에 박힌다**를 `javap` 실측으로 증명 — `OnboardingRequest` 생성자에 `checkNotNullParameter` 가 정확히 4번, 물음표 없던 필드 넷과 일치하고 `String?` 둘에는 없다 / `data class` 가 `component1()~component6()`·`copy`·`equals` 를 실제로 얻는 것도 같은 방식으로 확인 / Gradle↔npm 대응표, `implementation` vs `api`, buildSrc 컨벤션 플러그인 4개, `kotlin-spring`(all-open)·`kotlin-jpa`(no-arg)가 소스에 없는 `Food()` 생성자를 만들어 넣는 것을 `javap` 로 확인)
- [2026-08-27] ingest | R9 실측 요지 — ① **톰캣 동시 처리 상한 200**. 근거가 설정이 아니라 **부재**다: `server.tomcat.threads` 가 레포 전체 **0건**이라 Spring Boot 4.1.0 소스 `TomcatServerProperties.java` 의 기본값(`threads.max=200` · `min-spare=10` · `max-connections=8192` · `accept-count=100`)이 그대로 적용된다. 커넥션은 8192 개 **열어둘 수** 있어도 **동시에 일이 진행되는 건 200 건**이라, api 인스턴스 하나의 실질 상한은 "동시 접속 8192" 가 아니라 **"동시 처리 200"**. ② **가상 스레드 안 씀** — `spring.threads.virtual` 설정 0건, 레포 전체 `virtual` 문자열 **0건**(직접 재확인). Spring Boot 에서 가상 스레드는 명시적 opt-in 이고 프로퍼티 기본값이 `false` 라, Java 21 을 쓰면서도 **플랫폼 스레드 200개짜리 고전 톰캣 풀**로 돈다. ③ 스택 실측 **Kotlin 2.3.21 / Spring Boot 4.1.0 / Java 21 toolchain / Spring AI 2.0.0**(`gradle/libs.versions.toml:1-8`) · Gradle Wrapper 9.5.1 · JDK 는 foojay-resolver 가 자동 조달하므로 로컬 `JAVA_HOME` 과 무관. ④ 확장 함수: **프로덕션 코드에 최상위 확장 함수 정의 0건**(공통 로직은 `common.util` 의 객체로 모음). ⑤ 노트 1 §8 에 **정직하게 못 밝힌 것 1건** 명시 — readiness 프로브가 ECS 에서 실제로 200 을 주는지는 설정 배선만 읽었고 응답은 확인 못 했다(`management.endpoint.health.probes.enabled`·`spring.main.cloud-platform` 전수 grep 0건).
- [2026-08-27] fix | R9 **낡은 지식 폐기 — 옛 2번의 "가상 스레드 LLM fan-out" 은 사실이 아니다.** 옛 노트는 `newVirtualThreadPerTaskExecutor()` 로 3모델을 병렬 호출한다고 `LlmConfiguration.kt:71` 까지 짚어 가르쳤는데, 지금 `LlmConfiguration.kt` 는 **83줄에 `@Bean` 셋**(비전 추출기·이미지 배치 클라이언트·임베딩 클라이언트)뿐이고 **실행기 빈이 하나도 없다.** 3모델 병렬 스코어링 자체가 `kbap-langchain` 별도 레포로 이관된 결과다(R8 이 기록한 KB-301 흐름과 맞물림 — 다만 가상 스레드 제거를 명시한 티켓 번호는 레포 근거로 확인하지 못했다). 보관 노트 [[2. 요청 하나에 스레드 하나 — JVM 서버의 동시성과 Node 이벤트루프의 차이]] 의 해당 절 머리에 `> [!warning] 모순` 콜아웃을 달아 **"개념 설명으로만 읽고 kbap 의 현재 사실로 읽지 말 것"** 을 명시하고 현행 정본(새 2번 §6)으로 넘겼다. 폴더 전체의 📦 보관 배너는 구조 변경을 뭉뚱그려 경고할 뿐이라, **이렇게 특정 사실이 뒤집힌 자리는 그 절에 따로 표시해야 한다.**
- [2026-08-27] update | R9 복리 — **승격 16건**(기존 노트가 `제목(집필 예정)` 평문으로 예고하던 자리를 위키링크로). 1·2·3 이 R1~R8 내내 미집필이라 볼트 곳곳에 쌓여 있던 예고를 이번에 전수 해소했다: **→ 2번 8건**(17`:435`·20`:29`·20`:235`·20`:756`(각주 목록)·21`:64`·23`:68`·26`:347`·27`:529`, 24`:109` 는 번호 없이 제목만 쓴 형태라 `[[…|요청 하나에 스레드 하나]]` 별칭으로) · **→ 1번 1건**(11`:398`) · **→ 3번 1건**(4`:9` 머리 블록 ⬅️앞). 덤으로 **1·2·3 과 무관하게 낡아 있던 예고 5건**도 함께 승격했다 — 10`:9`→11번 / 14`:9`→15번 / 18`:9`→19번 / 12`:317`·19`:427`→21번(백틱으로 감싼 형태라 R1~R7 의 승격 스캔에 안 걸렸다. **`` `제목` ``(집필 예정) 형태도 승격 대상**이라는 걸 다음 회차 체크리스트에 넣을 것). 남은 `집필 예정` 은 전부 29번을 가리키는 7건뿐이고 R10 소관. ⚠️ 그중 1`:301` 은 `` `29. 캐싱과 키-값 저장소` `` 로 **제목을 줄여 적었다** — 파일명은 `29. 캐싱과 키-값 저장소 — kbap이 Redis를 쓰는 두 가지 방식` 이라 정규식 승격이 그냥은 안 걸린다.
- [2026-08-27] update | R9 복리 — **역링크 append 16건**(하단 `> [!note] 2026-08-27 역링크`, 본문 불변): [[4. IoC 컨테이너와 의존성 주입 — 부품을 직접 만들지 않는다]](1번이 `@SpringBootApplication` 을 표지판까지만 설명하고 넘긴 지점 + 2번이 싱글턴에 스레드 200개를 얹어 `var` 금지 결론을 내는 지점 + 3번의 런타임 리플렉션이 DI 의 근거라는 지점) / [[5. kbap의 계층 — 컨트롤러에서 리포지토리까지]] / [[6. 설정과 프로필, 트랜잭션 경계]]("설정에 없는 것도 정보" 읽는 법 + `@Transactional` 이 `ThreadLocal` 로 스레드에 묶이기에 §4 자기호출 함정이 생긴다는 인과) / [[7. API 계약의 모양 — 공통 봉투·에러 코드·헤더 버저닝]] / [[8. 영속성 컨텍스트와 엔티티 매핑 — 엔티티가 곧 도메인 모델]](1차 캐시 보장이 **내 요청 안에서만** 성립하는 이유) / [[9. 연관관계를 쓰지 않는 설계 — N+1을 애초에 만들지 않기]](N+1 이 **그 요청만** 늦추는 게 아니라 워커 스레드를 묶어 스레드 고갈로 번진다) / [[10. Flyway 마이그레이션 — 스키마를 코드로 관리하기]](마이그레이션은 잡일이 아니라 **부팅 선행 조건**) / [[11. 모듈러 모놀리스와 DDD — 3모듈과 도메인 패키지]] / [[12. 포트와 어댑터 — 외부 시스템을 갈아끼우는 자리]] / [[13. 경계를 지키는 테스트 — ArchUnit이 강제하는 규칙들]](컴파일 → ArchUnit → 테스트 순서) / [[15. 온보딩 프로필 저장 흐름 — 요청이 DB에 닿기까지]] / [[17. JWT 인증 필터와 세션 생명주기 — 매 요청 검증·재발급·로그아웃]] / [[19. 메뉴판 스캔 판정 흐름 — 메뉴명이 위험도가 되기까지]](`ScanService.kt:40-42` 의 `await` 없는 LLM 호출이 왜 정상인지) / [[26. 배치 앱의 골격 — Spring Batch 잡·트리거·스케줄]] / [[27. 아웃박스와 SQS — 다른 시스템에 일을 넘기는 법]]. **교차 주제 복리 1건**: [[Metro 연결 실패 — Mac·폰 IP 불일치 (link-local·서브넷)]] ↔ 1번 양방향 — 포트 점유(8081/8080)·폰 입장의 `localhost`·터미널 커서가 멈춘 대기 상태, 세 가지가 그대로 겹친다. **Metro 를 띄워 본 적이 있다면 이미 서버를 켜 본 것**이라는 게 1번 §2-2 의 논지라 FE 쪽 노트에서 건너오는 다리를 놨다.
- [2026-08-27] fix | R9 품질 검수 — 직접 수리 4건. ① **2번 §7-1 이 4번을 "…에서 봤듯" 이라고 과거로 가리켰다.** 4번은 두 칸 뒤 노트라 순서대로 읽는 독자는 아직 안 봤다 → 싱글턴 스코프를 **그 자리에서 한 문장으로 정의**하고 4번은 "정본은 다음다음 노트" 로 넘기게 고침. ② **3번 §2 의 "런타임 리플렉션" 이 정의 없이 등장** → 인라인 정의(JS 의 `Object.keys`/`obj[key]` 대비) + 정의가 있는 8번 §10 링크. ③ **3번 §9 의 "프록시" 가 정의 없이 등장** → "상속한 대역 객체" 로 풀고 6번 §4 링크(2026-07-08 이후로 세 번째 반복되는 유형이라 기록해 둔다 — **1~3번은 커리큘럼 앞머리라 뒤 노트의 용어를 빌려 쓰면 안 된다**). ④ 3번 머리 블록에 실측 기준 줄이 없어 2·4번 서식과 어긋남 → `기준:` 한 줄 추가. **반려 0건**(작성자에게 돌려보낼 구조적 문제는 없었다).
- [2026-08-27] update | R9 등록 — 착수 시점에 **지도·홈이 이미 R10 완주 상태로 갱신돼 있었다**(지도 12:03, 홈 11:54, 29번 노트 11:57 완성). 지시는 `28 / 29` 였으나 **되돌리면 사실과 어긋나므로 그대로 두었다** — 지도는 1·2·3 `[x]` + 위키링크, 진도표 R9 `✅`, 완료 `29 / 29`, 홈 kbap 줄 `29노트 완주 (2026-08-27)` 로 이미 정확하다. `kbap 백엔드 (old)/` 1·2·3 머리말의 `새 커리큘럼 N번(집필 예정)` 평문도 12:01 에 이미 위키링크로 승격돼 있었다(별칭 링크). **29번 노트 본문·복리는 손대지 않았다 — R10 소관.**
- [2026-08-27] lint | 린트 #21(R9 — 번호 주의: R9 keeper 가 R10 보다 **늦게 끝나** 이 줄부터 8줄이 R10 의 완료 선언 뒤에 붙었다. R10 의 린트 #20 은 R9 의 복리 작업(역링크 16건·승격 16건·모순 콜아웃 1건) **이전**에 측정된 값이므로, 지금 볼트의 현행 수치는 이 줄이다): 볼트 전역 **79노트·위키링크 1,319건 전수대조**(모든 편집 완료 후 재측정)(NFC 정규화 + 코드펜스·인라인코드 제외, `[[대상\|별칭]]` 파이프 이스케이프·`[[#앵커]]` 자기참조 처리) — **라이브 노트 깨진 링크 0**(미해결 6건은 전부 `📜 작업 로그` 안의 2026-07-14 줄 + 린트 #14 줄의 `[[wikilink]]` 문법 설명 텍스트, append-only 알려진 예외) / **유령 노드 0** — R8 이 인계한 "R9 대기 유령" 은 1·2·3 이 실파일로 생기며 **전부 해소**됐고, 29번을 가리키던 유령도 29번 파일이 생겨 해소됐다(남은 29번 언급 7건은 평문·백틱이라 애초에 노드를 만들지 않는다) / **고아 0** / 새 3노트 → `kbap 백엔드 (old)/` **유입 0** / **`(old)` 외부 인바운드 1건**(`🏠 홈` → 옛 지도, 📦 보관 등록이라 의도된 것). R8 이 남긴 잔여 1건([[클라이언트 저장소 — AsyncStorage·SecureStore·메모리 (K-Bap 실전)]] → 옛 21 캐싱)은 **R10 쪽에서 새 29번으로 갈아끼워 해소**됨(같은 수정에서 "FE 는 토큰을 SecureStore 에" 라는 거짓 설명도 걷어냄) / 새 폴더 30파일(29노트+지도) 전부 `상태: 완료` · 파일명 = H1 = 링크 대상 일치 / 새 노트 백틱 래핑 `` `[[…]]` `` **0** · 상대링크 `](../` **0**(볼트 전역 6·1건은 볼트 규칙·홈·Obsidian 노트의 문법 설명과 작업 로그의 경로 표기) / `상태: 진행중` 방치 0(템플릿 제외) / 1·2·3 인바운드 = **21·30·12건**.
- [2026-08-27] lint | R9 추가 점검(R8 인계 2건 대조) — ① **승격 후보 10곳 전건 대조 완료**: 26`:347`·27`:529`·11`:398`·20`:29`·20`:235`·20`:756`·21`:64`·23`:68`·24`:109`·4`:9` 전부 R9 승격 16건에 포함돼 처리됨(누락 0). 볼트 전역 `집필 예정` 평문 **0건**(작업 로그 제외) — 29번 잔여 7건은 R10 이 병행 처리. ② **응답 봉투 `data`/`error` 오기 3차 점검 — 1·2·3번 해당 없음(clean).** 세 노트에 응답 JSON 예시가 **하나도 없고**, 봉투는 이름만 언급한 뒤 전부 [[7. API 계약의 모양 — 공통 봉투·에러 코드·헤더 버저닝]] 으로 넘긴다(1번 §2-3·§9 표 6단계). 이게 정확한 처리다 — 1단계 기초 노트가 봉투 필드를 직접 적었다면 R6·R8 과 같은 오기가 세 번째로 났을 자리다. **앞머리 노트는 뒤 노트의 계약을 인용하지 말고 넘기는 게 오기 예방책**이기도 하다. 볼트 전역 `"data"`/`"error"` 잔여 오기 **0건**(13번 `:47` 은 ESLint 설정 예시라 무관).
- [2026-08-31] ingest | 신규 시리즈 **kbap PR 읽기** 시작(폴더 `kbap PR 읽기/`): [[PR 201 — 어드민 음식 상세·수정·재수집·삭제 REST]] — team-skyjs/kbap-server#201(KB-397, 어드민 리빌드 에픽 KB-384), 7파일 +789/−3. 구성: 제품 관점 → 파일 한눈에 표 → **파일별 역할 해설**(Api 문서 인터페이스·Controller 번역기 3역할·DTO 3종·Service·Repository·ErrorCode·테스트) → 동시 수정 3겹 방어 → 기존 노트 연결표 → 리뷰 질문 → 용어집. 파일명에 `#` 금지(옵시디언 위키링크의 헤딩 구분자라 링크가 깨짐) → `PR 201 — …` 규약
- [2026-08-31] update | PR 201 실측 확인 3건: **레포 첫 비관적 락**(`FoodJpaRepository.findByIdForUpdate`, `@Lock(PESSIMISTIC_WRITE)`) — 노트23이 "PESSIMISTIC·@Lock 0건"으로 실측했던 상태가 이 PR로 깨짐 / `Food.version` 은 `@jakarta.persistence.Version`(정규화 이름이라 `@Version` grep에 안 잡힘) → 컨트롤러의 `catch(OptimisticLockingFailureException)` 은 죽은 코드가 아니라 3겹째 방어 / 소프트삭제 자동 제외는 `BaseEntity` 의 `@SQLRestriction("status = 'ACTIVE'")` 때문
- [2026-08-31] update | PR 201 문서↔코드 불일치 1건: PR 본문 "변경 사항"은 `FOOD-005` 채번만 적었으나 코드엔 **`FOOD-006`(버전 충돌, 409)도 함께** 들어감(최신 커밋 `7dc66de7` 미반영). 노트는 코드 기준으로 쓰고 §3-6에 콜아웃. 홈에 `kbap PR 읽기` 섹션 신설·등록
- [2026-08-31] update | PR 201 노트 갱신(새 커밋 `ee3df844` 반영): 사용자가 §6 리뷰 질문 5건 중 **2건을 적용**해 재커밋 — ④version 필수화(`@field:NotNull`, 누락 시 400 COMMON-002, 테스트 시나리오 신설), ⑤PR 본문 전면 갱신(FOOD-006 누락 보완 + 에러 응답 요약표 + 리뷰 반영 이력). 노트의 낡은 서술 2곳 교정(§3-3 DTO에 `@NotNull` 표기, §4 "생략하면 무조건 덮어쓴다" → "REST 계약에서만 닫힘 — 서비스 기본값 `expectedVersion=null`은 유지돼 Thymeleaf 경로는 무영향") · §6에 ✅/⬜ 반영 표시 · **§9 변경 이력 신설**(커밋 해부 + 앞선 리뷰 반영 이력 4커밋 표 + 미반영 3건의 사유)
- [2026-08-31] update | PR 201 §9의 관찰: **3겹 방어가 한 번에 설계된 게 아니라 리뷰를 거치며 쌓였다** — ①비관적 락은 Codex P2(재수집 원자성) 고치다 유입, ②version 비교는 FE 요청, ③JPA 낙관 잠금은 ②의 경합 방지 벨트. 각각 다른 이유로 붙어 결과적으로 3겹. PR 본문이 "일괄 재수집 check-then-insert 경합은 감수(비치명)"로 **막지 않기로 한 판단도 기록**한 점을 좋은 예로 인용
- [2026-08-31] ingest | [[PR 209 — 어드민 벡터 아웃박스 조회·enqueue·재시도 REST]] — team-skyjs/kbap-server#209(KB-402, 같은 어드민 리빌드 에픽), 7파일 +448/−5. PR 201과 **파일 구성이 판박이**(Api·Controller·Response·Service·Repository·ErrorCode·테스트 7자리)라 "이 레포에서 REST 한 벌 추가 = 이 7자리"를 두 PR이 함께 보여줌. 핵심 서사: **"조용히 아무것도 안 하던 자리에 전부 결과값을 붙였다"**(`?: return` → `?: return NOT_FOUND`, enqueue Unit→Int, retry Unit→enum 4갈래)
- [2026-08-31] update | PR 209 실측 2건: **어드민은 offset 페이징, 사용자 API는 커서** — 사용자 쪽 `PageRequest.of(0, size+1)`은 페이징 도구가 아니라 **limit**으로 쓰인 것이고(진짜 커서는 `WHERE id < :cursor`), 어드민은 `PageRequest.of(page-1, 50)` + `totalCount`로 진짜 offset. 페이징 노트·9번 노트와 모순이 아니라 **소비자가 다른 것**임을 §4 표로 정리 / `displayName: String?`이 널이 되는 이유는 `@SQLRestriction`으로 소프트삭제 음식이 `findAllById`에 안 잡혀서 — 아웃박스 행은 남고 이름만 비는 상태가 운영 화면에 그대로 보임(§3-1)
- [2026-09-01] ingest | kbap PR 읽기 4노트 추가(작성자 rocher71의 **열린 PR 4건** — 사용자가 범위 정정: 전체 7건이 아니라 open 4건): [[PR 214 — 벡터 아웃박스 목록 검색 q 추가]](JPQL 상관 `exists` 로 연관관계 없이 타 테이블 검색 · 선택 파라미터 2개=분기 4갈래 · 센티널 `-1`) / [[PR 215 — 검수 비대상 거절에 전용 코드 FOOD-008 채번]](`require`=내부 버그 vs `BusinessException`=업무 규칙 분류 교정 · 도메인이 ErrorCode 참조하는 근거 3가지 · `@ApiErrors`+`OpenApiSnapshotTest` 로 문서를 테스트가 지킴) / [[PR 216 — 삭제 음식 조회·복원 REST와 벡터 큐 대칭]](native 쿼리는 `@SQLRestriction` 을 안 받는다 · 복원 시 반대편 PENDING 아웃박스 취소 = 비동기 큐의 되돌리기 함정) / [[PR 217 — 멤버 목록·상세·활동 3종과 대시보드 메트릭 REST]](앞 PR 손동작 3종 재사용 · 14일치 한 번에 긁어 7·7로 자르기 · 빈 날짜 0 채움 · 차트 계산은 클라이언트)
- [2026-09-01] update | PR 시리즈 6노트 완성(201·209·214·215·216·217) — 홈 `kbap PR 읽기` 섹션 등록, 링크 전수 검증 깨진 링크 0. 시리즈 전체를 관통하는 관찰: 어드민 리빌드 에픽(KB-384)에서 **REST 한 벌 추가 = `*Api`·`*Controller`·`*Response`·`*Service`·`*JpaRepository`·`ErrorCode`·테스트 7자리**가 반복되고, 멱등 어법(`skipped`/`retried`/`restored`=false)·`q` 검색 관례(부분일치+숫자면 id)·N+1 회피(`findAllById`+`associate`)가 PR을 거치며 관례로 굳어감
- [2026-09-01] update | [[PR 214 — 벡터 아웃박스 목록 검색 q 추가]] §6을 "읽으며 눈에 띈 것 → **리뷰 결과**"로 확장(커맨드 센터 세션 경유로 전달된 판정 반영): **수용 1**(LIKE 풀스캔 — 어드민 2인 저빈도 + 음식 목록 q도 동일 관례, FULLTEXT/prefix는 후보로만) · **수정 발주 2**(@Query와 규약형 메서드 이름 혼용 → 짧은 서술형 리네임 / 센티널 `-1` → `(:foodId is null or …)`) · **의도 확정 1**(exists 내 소프트삭제 필터 — FE가 검색창 힌트로 해소, P-A13). 발주 2건은 코드 발췌가 낡을 예정이라 `> [!warning]` 으로 명시하고, **실측으로 아직 미반영임을 확인**(`findByFoodDisplayNameContainingOrFoodId`·`NO_FOOD_ID` 그대로)해 시점을 박음
- [2026-09-01] update | [[PR 214 — 벡터 아웃박스 목록 검색 q 추가]] 코드 발췌 갱신(리팩터링 반영, 커밋 `381f991a` — **실측 확인 후 반영**): 메서드 `findByFoodDisplayNameContainingOrFoodId(AndOutboxStatus)` → **`searchByFoodKeyword`·`searchByFoodKeywordAndStatus`** / 센티널 `-1` → **`(:foodId is not null and o.foodId = :foodId)`**(파라미터 `Long` → `Long?`). §3 쿼리·§4 분기 발췌 교체, 센티널은 "왜 바뀌었나" 기록으로 콜아웃 보존, §6-2·6-3 판정을 "발주됨" → "반영 완료"로, 낡음 경고 `[!warning]` → `[!check]` 로 전환. 지적 → 수정 → 문서 갱신이 하루 안에 한 바퀴 돈 사례
- [2026-09-01] update | [[PR 215 — 검수 비대상 거절에 전용 코드 FOOD-008 채번]] §6에 리뷰 결과 반영(커맨드 센터 경유): **반영 2**(①상태값 소실 → 응답 메시지는 고정 유지하되 throw 직전 WARN 로그로 foodId·contentStatus — **응답=계약, 로그=진단** 역할 분리 ②가드 복붙 → `requireReviewable()` 추출, ①의 로그도 여기 들어가 두 지적이 한 수정으로 묶임) · **반려 2**(③READY 재승인 멱등 vs 비대상 예외 비대칭은 의도 — "원하는 결과 이미 달성"과 "운영자 실수 신호"는 다른 상황, **멱등은 모든 실패를 삼키는 게 아니다**, 주석으로 명시 ④별도 FOOD- 코드표는 드리프트 원천 — Swagger 에러 표가 정본이고 OpenApiSnapshotTest가 검증). **실측 확인: 1·2는 아직 PR에 미반영**(커밋 2개뿐, `Food.kt` if 중복 그대로)이라 `[!warning]`으로 시점 명시
- [2026-09-01] update | PR 노트 3건 리뷰 결과 반영(커맨드 센터 판정 일괄): **215** — 반영 2건 push 확인(`ce7121da`, 실측)해 §2 발췌를 최종형으로 교체(`requireReviewable()` 추출 + `log.warn(foodId, contentStatus)` + 비대칭 의도 주석), `[!warning] 미반영` → `[!check] 1·2·3 한 커밋으로 정리` / **216** — 반려 3(별칭 필드=크러프트·복원은 "삭제 취소" 의미론·취소행 비표시 감수) · 반영 1(`findAnyById` KDoc 경고, P-S10) · **조사 1**(콘텐츠 수집 아웃박스 대칭 미판정) / **217** — 반려 3(Clock 주입 과함·`sanctions` 자리표시 유지·LIKE는 214와 동일 판정) · 반영 1(`requireMemberExists` 이유 주석 — 내 결론이 정답으로 확인) · **조사 2**(DTO 제네릭화 의도·이메일 로그 유출)
- [2026-09-01] update | 리뷰 문답에서 **내 지적이 틀렸던 2건도 노트에 남김**(반박 근거가 배울 거리라): 217-2 `sanctions` — "빈 배열은 필드 이름만 약속하지 원소 모양까지 약속하지 않는다"로 **약속의 크기를 과대평가**했음 / 216-1 `deletedAt` — 소비자가 **FE 하나뿐**이라 동값 중복 필드가 크러프트가 됨(공개 API였다면 별칭이 정당했을 수 있음). 217-6(이메일 검색어 로그 유출)은 6건 중 **가장 실질적 지적**으로 채택돼 조사·조치 발주
- [2026-09-01] update | 조사 3건 결론 반영 — **리뷰 질문이 실제 버그 2건을 잡았다**. **216-5(커밋 `e80cd4c7`)**: 음식 삭제가 콘텐츠 아웃박스를 안 건드려 PENDING → SQS 발행 → kbap-langchain이 **삭제된 음식에 LLM 비용 지출** → ingest 콜백 FOOD-001 거절 → SENT 행 영구 표류 + 에러 노이즈. 수정=삭제 시 PENDING 콘텐츠 큐도 소프트삭제 취소(벡터 큐와 동일 패턴), SENT는 발행 완료라 취소 불가. **"큐가 둘인데 하나만 대칭"이라는 비대칭 자체가 신호였다** / **217-6(커밋 `d52a680c`)**: `RequestLoggingFilter`가 쿼리스트링을 원문 로깅(마스킹은 위경도뿐) → **어드민 이메일 검색어가 평문으로 로그에 축적**. 수정=마스킹 목록에 `q` 일괄 등록 + QueryMaskingTest. 트레이드오프(음식 검색어 로그도 가려짐)를 경로 분기 대신 단순성으로 선택
- [2026-09-01] update | 나머지 조사 2건: **216-4** → 주석이 아니라 **ArchUnit 규칙**으로 반영(`ModuleBoundaryTest` 기존 인프라 재사용 — 상태 무시 조회를 api.admin 밖 호출 금지, 메서드명 기반이라 217 멤버 쪽도 자동 커버). 13번 노트의 3층 구분에서 **"사람이 지키는 것" → "테스트가 막는 것"으로 한 층 상승** — 새 도구 도입은 과하지만 있는 도구 사용은 과하지 않다 / **217-3** → 반려·의도 확정(내 추정이 맞음): springdoc이 제네릭을 단일 스키마명으로 뭉개 FE 코드젠 타입 구분 소실 + 레포 관례가 기능별 명시 Response. **중복 제거와 계약 표현이 충돌할 때 계약을 택한 사례**
- [2026-09-01] fix | 오독 유발 서술 교정(사용자가 PR 216 §5-2를 "삭제 상태가 안 풀리나?"로 오독 — 커맨드 센터 경유 피드백): 원인은 **`status`(BaseEntity 소프트삭제 플래그, 복원 시 `active()`로 풀림)와 `contentStatus`(Food 콘텐츠 생명주기, 복원해도 보존)의 이름 유사성**. 조치 3곳 — ①**노트 8(소프트삭제 정본)에 세 status 구분표 신설**(`status`/`contentStatus`/`memberStatus`는 독립 축, `@SQLRestriction`이 거르는 건 첫 번째뿐, 복원 예시 포함) ②PR 216 §1에 경고 상자 + §5-2 제목·서두를 "삭제 상태는 당연히 풀린다"로 먼저 못 박고 §3 코드에 주석 2줄 + §6 체크리스트 0번 신설 ③PR 215 첫 등장부에 한 줄 구분 + 노트8 링크. 레포 관례(`status`는 소프트삭제 전용 예약, 도메인 상태는 컬럼명 분리)가 이미 노트8에 있었으므로 그 자리를 정본으로 확장
- [2026-09-02] ingest | [[PR 218 — 표시 이름 편집·어드민 반려 유형·삭제 시 이름 반납]] — team-skyjs/kbap-server#218(KB-406), 12파일 +124/−2. **코드는 작고 설계 밀도가 높은 PR**. 셋이 한 뿌리("이름과 상태를 어떻게 표기하나")임을 §3으로 정리: ①매치키(`korean_name` 유니크·NFC+한글만 정규화) vs 표시명(`display_name` 비유니크) **분리 편집** ②`contentFailureKind`에 `ADMIN_REJECTED` 추가로 **파이프라인 실패 vs 사람 반려** 구분 + 랭체인 콜백이 그 값을 못 쓰게 `@AssertTrue` 가드(**값 추가와 함께 쓰기 권한도 좁힘**) ③**실제 500 버그 수정** — 소프트삭제가 유니크 이름을 붙들어 동명 재등록이 `uq_food_korean_name` 위반(중복 검사는 `@SQLRestriction`으로 ACTIVE만 봐서 통과), 해법=삭제 시 매치키만 `원명_deleted_<epochMillis>`로 개명해 키 반납·복원 시 접미사 제거(그 사이 동명 신규면 409 FOOD-009 신규 채번)
- [2026-09-02] update | PR 218에서 잡은 연결 2건: **정규화가 배당금을 낸다** — 매치키가 `가`~`힣`만 남아 `_deleted_` 구분자와 절대 충돌 불가(19번 노트의 정규화 설계가 여기서 안전성 근거가 됨) / **PR 209 리뷰의 "MySQL은 부분 유니크 인덱스 미지원" 판정이 여기서 비용을 청구** — PostgreSQL이면 `WHERE status <> 'DELETED'` 부분 인덱스로 끝났을 문제. 개명 도입 전 삭제 행은 접미사가 없어 `substringBefore`가 원본 반환 → **마이그레이션 없이 하위호환**
- [2026-09-02] update | [[PR 218 — 표시 이름 편집·어드민 반려 유형·삭제 시 이름 반납]] §4를 "리뷰 결과"로 확장(커맨드 센터 판정): **반영 1**(currentTimeMillis 실충돌 불가 3겹 근거를 주석으로) · **반려 3**(복원+개명 통합=한 API 두 의미 지양·216의 "복원은 되돌리는 것" 답변과 동일 원칙 재확인 / 출처 필드 분리=YAGNI / ) · **조사 1**(매치키 문자열 조인 경로 — ScanHistory FK는 안전 확인, FoodService·ScanApi 확인 중) · **칭찬 1**(동명 재등록 성공까지 검증). **5번은 내 우려가 애초에 불가능**했음이 실코드로 확인 — `recordContentFailure`는 FAILED로 보내고 `reject()`의 `requireReviewable()`은 PENDING_REVIEW만 통과하므로 파이프라인 사유를 덮어쓸 경로가 없다
- [2026-09-02] update | PR 218 §4-5에서 건진 관찰: **[[PR 215 — 검수 비대상 거절에 전용 코드 FOOD-008 채번]]에서 "복붙 정리+로그 추가" 목적으로 뽑은 `requireReviewable()`가 여기선 데이터 무결성 가드로 일한다** — 좋은 가드는 만들 때 의도하지 않은 것까지 막아주고, **함수로 뽑혀 있을 때 그게 눈에 띈다**(if 두 개로 복붙돼 있었다면 "두 곳 다 그런가?"를 따로 확인해야 했음). 216·218에서 "복원은 되돌리는 것이지 바꾸는 게 아니다"가 두 번 반복돼 레포의 확립된 원칙으로 기록
- [2026-09-02] ingest | [[PR 219 — 콘텐츠 아웃박스·이미지 배치·LLM 비용 레포트 REST]] — #219(KB-407), 14파일 +643/−0. **"무엇을 만들까"가 아니라 "무엇이 빠졌나"** 로 접근한 PR(Thymeleaf 전수 대조 → 4종 중 2종은 이미 REST 존재 확인, 3종 신설). 핵심 관찰 3: ①콘텐츠 아웃박스는 `displayName`이 **자체 스냅샷 컬럼**이라 209의 배치 로딩·214의 `exists` 서브쿼리 복잡도가 통째로 없음(대신 낡을 수 있음 — "그때의 사실 vs 지금의 사실") ②**재시도 주체가 다르다** — 벡터는 FAILED에서 멈춰 사람이 retry, 콘텐츠는 PENDING을 배치가 자동 재발행 + recollect. **안 만든 것에 이유를 적으면 누락이 아니라 결정** ③214 리뷰 반영분(`searchByKeyword` 네이밍·널 파라미터)을 **처음부터** 적용 — 리뷰가 다음 PR의 출발점을 바꿈
- [2026-09-02] fix | **218·219 잠정 표시**(커맨드 센터 정정 — Codex 왕복 종결 전에 넘어온 건이라 내가 읽은 코드가 최종본 아님): 두 노트에 `⏳ 잠정` 배너 + frontmatter `상태: 진행중`, 홈에도 ⏳ 표기. 218은 P2 2건이 `70a5a805`로 반영돼 **§2-3의 `deletedKoreanNameOf` 발췌가 이미 낡음**(앞을 잘라 접미사 자리를 만들던 방식 → **`deleted_original_korean_name` 별도 컬럼에 원명 보존**, 잘림 원천 차단 + 복원 레이스 409 매핑, 마이그레이션 2건). **내가 놓친 지적** — §2-3에서 `take(MAX - suffix.length)`를 "길이 관리"로만 적고 원명 손상 가능성을 못 짚었다
- [2026-09-02] update | 219 §5를 리뷰 결과로 확장(**반영 5 · 반려 1**, 단 Codex 재리뷰 전이라 일부는 결정만 남고 코드 미반영): 1번이 특히 값짐 — **215가 정리한 `require` 분류에 세 번째 칸이 추가**됐다(프로그래머 실수=`require`/업무 규칙=`BusinessException`/**사용자 입력=`@Min`·`@Max` 검증**, Swagger 문서화 부수 이득). 3·5번은 서버 무변경 FE 처리(209·214와 같은 "판단 재료가 응답에 있으면 FE가 푼다" 계열), 4번은 **임시 분리에 만료일을 붙임**(217 머지 후 합류를 PR 본문에 명시), 6번은 커버리지 매트릭스 상시화
- [2026-09-02] update | **PR 218·219 최종 확정**(Codex 리뷰 클린 종결) — 잠정 배너 제거, `상태: 완료`, 홈 ⏳ 표기 해제. 218은 초판 대비 **+124 → +279 (커밋 6개, 마이그레이션 3건)** 로 리뷰가 코드를 두 배로 키움. §2-3 개명 발췌를 최종형으로 전면 교체: 접미사 `currentTimeMillis` → **`foodId`**(유니크·불변이라 절단돼도 충돌 불가), 원명은 **`deleted_original_korean_name` 별도 컬럼 보존**, 복원은 컬럼 우선·없으면 파싱 폴백(레거시) + **backfill 마이그레이션(멱등)**, 복원 레이스는 flush 유니크 위반을 **409 FOOD-009** 매핑
- [2026-09-02] update | PR 218에서 건진 일반 교훈을 노트에 명시: **문자열에 정보를 인코딩해 두고 나중에 파싱해 되찾는 방식은 길이 제한 앞에서 깨진다** — 되찾아야 할 값이면 따로 저장해야 한다. 초판이 `묘비명_deleted_<ms>`에서 `substringBefore`로 원명을 복구하는 구조였는데 255자 절단 시 **원명이 손상**됐다. id 접미사는 남았는데 그건 **되찾을 정보가 아니라 유일성만 만들면 되는 값**이라서. 부수: `matchKey` 응답 필드 신설(활성=koreanName, 삭제=deletedOriginalKoreanName 폴백)로 **묘비 문자열의 FE 누출 차단** — 폴백을 `from()` 한 곳에만 두고 노출 표면 6곳 전수 점검
- [2026-09-02] fix | PR 218 §2-3에 **"원래 요청은 타임스탬프였다"** 맥락 보강(사용자 확인 질문 — 초판 요청안과 최종 코드가 달라 혼동): 실코드 재검증 결과 최종은 `_deleted_<foodId>`이고 `currentTimeMillis` 잔존 0건. 바뀐 이유를 명시 — **255자 절단과 만나면 타임스탬프는 구멍**(잘린 앞부분이 같아진 두 음식이 같은 ms에 삭제되면 묘비 키 충돌, 확률 낮지만 0 아님)이고 **id는 접미사 자체가 유일**해 절단 무관. 덤: id는 불변이라 삭제/복원 반복에도 묘비 키가 동일. **"언제 지웠나"는 `updated_at`이 이미 갖고 있으므로 접미사는 유일성만 만들면 되는 자리**
- [2026-09-02] update | 노트2(스레드)에 **§5-1·§5-2 신설**(사용자 문답에서 나온 의문 — 노트를 읽으면 자연히 생기는데 답이 없던 자리): **§5-1 "이 200은 누가 정하나"** 3문답 — ①CPU 자동 아님(`TomcatServerProperties`에 박힌 고정값, 개발자 설정 > 기본값 순) ②서버 사양이 달라도 톰캣 풀은 200 고정이나 **코어 수를 보는 풀은 따로 있다**(GC·`ForkJoinPool.commonPool`·Netty — `availableProcessors()`, Java 10+는 컨테이너 인식) ③배정은 **3층 분담**(개수=애플리케이션 설정 / 생성=JVM이 OS에 요청, 플랫폼 스레드는 커널 스레드와 1:1 / 스케줄링=OS. 가상 스레드는 JVM이 M:N으로 직접) / **§5-2 "5,000,000으로 설정하면"** — 상한 검증 없음, 그러나 **부팅은 성공**(min-spare 10개만 만들고 lazy 증가, max는 천장), 무너지는 순서 2단계(①스택 1MB/개 + 컨텍스트 스위칭으로 **느려짐** ②`OutOfMemoryError: unable to create native thread` — 힙 아니라 OS 거절, `ulimit -u`·cgroup `pids.max`). **진짜 보호장치는 프레임워크가 아니라 커널**이고 `max-connections`(8192)가 우연한 방파제. 체크리스트 4항목 추가
- [2026-09-03] ingest | [[PR 224 — LLM 비용 레포트 컨트롤러 합류 (임시 분리 청산)]] — #224(KB-408), 4파일 **+36/−63 — 시리즈 최초로 코드가 줄어든 PR**. [[PR 219 — 콘텐츠 아웃박스·이미지 배치·LLM 비용 레포트 REST]] §5-4 리뷰("임시 분리에 만료일을 붙여라")의 **실제 이행**이라 두 노트를 상호 링크. 핵심 관찰: ①**URL 은 그대로인데 라우팅 조립만 바뀜** — 클래스 경로(`/dashboard/llm-costs`)+빈 `@GetMapping` → 클래스(`/dashboard`)+`@GetMapping("/llm-costs")`, 스프링은 두 경로를 이어 붙일 뿐이라 어디서 끊어도 결과 동일 ②**테스트 무수정 그린이 곧 계약 무변 증명** — 테스트가 구현(클래스)이 아니라 계약(URL·응답)에 붙어 있어서 가능 ③219 리뷰 반영분 `@Min/@Max` 가 이사에도 보존됨(리팩터에서 흔히 새는 자리)
- [2026-09-03] update | PR 224에서 일반화한 교훈: **임시 조치가 영구가 되는 건 "임시"라고 안 적어서가 아니라 회수 조건과 담당을 안 정해서다.** 219는 ①TODO 주석에 조건 명시("#217 머지 후") ②PR 본문에 계획 기재 ③후속 이슈 등록 — 셋을 남겼고 조건이 충족되자 224가 회수. 리뷰 질문 5건 제시(서비스는 왜 안 합쳤나 · 테스트 파일명이 낡음 · DTO가 서비스 파일에 얹혀 있는 배치 · 남은 청산 대상 TODO 훑기 · 리팩터를 기능 PR과 분리한 것은 잘한 판단)
- [2026-09-03] update | [[PR 224 — LLM 비용 레포트 컨트롤러 합류 (임시 분리 청산)]] §5를 리뷰 결과로 확장(**반려 1 · 후속 정리 2 · 채택 1 · 칭찬 1**). **1번 판정이 이 PR 전체를 한 줄로 설명** — "컨트롤러는 합쳤는데 서비스는 왜 안 합쳤나"에 대한 답이 **축이 두 개다: 컨트롤러=URL 축(공통 접두사로 묶음), 서비스=도메인 축(관심사로 묶음)**. 접두사가 같다고 서비스까지 합치면 무관한 로직이 모이는 **god service**가 된다. 컨트롤러 하나가 서비스 둘을 주입받는 건 정상이고, 5번 노트의 "컨트롤러는 번역기"의 자연스러운 귀결
- [2026-09-03] update | PR 224 §5-2·5-3을 **후속 정리 항목**으로 노트에 명시(커맨드 센터가 나중에 발주할 때 근거로 쓰겠다고 함): 테스트 파일명(`AdminLlmReportControllerTest` — 그 컨트롤러는 삭제됨, 단 이번 PR에서 안 고친 건 **무수정 그린이 계약 무변의 증거**였으므로 옳은 판단) · DTO 배치(`*Response.kt` 분리 관례 이탈) · KDoc 한 줄(축 두 개 설명). 4번(TODO 스윕)은 **가장 값진 지적으로 채택** — BE 세션에 읽기 전용 스윕 발주, 에픽 종료 체크리스트로 사용 예정. 교훈: **임시 조치는 만든 사람만 알고 있을 때 영구가 된다 — 목록이 되는 순간 팀의 것이 되고 종료 조건에 들어간다**
- [2026-09-03] fix | **커리큘럼 순서 결함 발견·보완**(사용자가 노트2 §7-1에서 막힘 — "bean·singleton 진짜 모르겠다"): 노트2 §7이 **스프링 빈 개념을 전제**하는데 빈의 정본은 **4번**이라 아직 안 나온 상태였다. 노트 스스로도 *"왜 그런지는 다음다음 노트 4번이 정본"*이라고 적어 미래 참조를 인정하고 있었음. 조치 3곳 — ①§7-1에 **🧩 30초 상자**(빈=스프링이 대신 만들어 들고 있는 객체 / 싱글턴=앱 전체에 하나 / **JS 모듈 최상단 `export const`를 여기저기서 import 하는 것**에 비유 / "객체는 하나인데 스레드는 200개"로 절의 핵심 연결) ②§7 머리에 **읽는 순서 안내**(막히면 4번→3번 후 복귀, §7~§9는 §1~§6과 독립) ③**지도 1단계에 같은 안내** 추가
- [2026-09-03] ingest | PR 노트 3건(커맨드 센터 일괄 요청, 셋 다 Codex 종결·클린): **[[PR 225 — 멤버 주문 항목·합계 추가 (Int 오버플로)]]**(+91/−3) — dogfooding 발 건. 핵심은 **JVM 정수 오버플로가 조용하다**(예외 없이 음수로 돌아감, `2_000 × 2_000_000 = 40억` > Int 21억)와 **`.toLong()`은 곱셈 앞에**. 도메인 헬퍼(`totalPriceOf: Int`)를 안 고친 이유 = **앱 경로 공유**(출시된 계약), 어드민만 Long. **Int→Long 확장이 하위호환인 이유 3가지**(필드명 동일·JSON엔 정수/실수 구분 없음·값이 JS 안전범위 2⁵³ 아래) / **[[PR 223 — 음식 목록 3필터·LIKE 이스케이프·READY 직행 가드]]**(+238/−13) — `(:p IS NULL OR 조건)`으로 조합 폭발 회피(214의 `is not null and`와 **AND/OR 문맥에 따라 뒤집히는 관용구**), **LIKE 와일드카드 이스케이프 누락**(`q=%` 하나로 전체 매칭 — 인젝션 아님, 값은 안전한데 패턴이 샌 것), READY 직행 가드(**올리는 건 막고 내리는 건 연다**) / **[[PR 230 — 이미지 갤러리 P1 (비정규화 캐시와 불변식)]]**(+273/−14, 30파일)
- [2026-09-03] update | PR 223에서 건진 일반 교훈: **파생 쿼리(`Containing`)는 스프링이 자동 이스케이프하는데 native 쿼리만 구멍이었다** — 216에서 `@SQLRestriction` 우회하려고 native를 도입한 대가가 여기서 청구됨. **프레임워크가 대신 해주던 일은 그 밖으로 나가는 순간 내 몫이 되는데, 대신 해주던 일은 원래 안 보이니까 빠뜨리기 쉽다** / PR 230의 핵심 3: ①`food.image_ref`를 **비정규화 캐시로 남겨** 전 공개 API·앱 무변경(219 스냅샷과 달리 **절대 낡으면 안 되는 사본**) ②"음식당 대표 1개"를 **생성 컬럼 `IF(is_primary,food_id,NULL)` + 유니크**로 DB가 보장(MySQL 부분 유니크 부재 우회, `image_batch_item.pending_food_id` 선례 재사용) ③**강등을 먼저 실행**해야 하는 이유 — 하이버네이트 flush가 INSERT를 UPDATE보다 먼저 내보내 유니크 위반이 나므로 `@Modifying(flushAutomatically=true)`로 순서 강제. **코드 순서 ≠ SQL 순서**
- [2026-09-03] update | PR 225·223·230 §를 리뷰 결과로 확장(판정 15건). **⭐ 223-1이 시리즈 최초로 "노트 리뷰가 실제 버그를 잡은" 사례 — `KB-416` 발주**: `escapeLikeWildcards`가 이 서비스에만 있고 214(아웃박스)·217(멤버) 검색에 **같은 구멍 2곳**. 조치는 한 곳 더 고치기가 아니라 **`common` 유틸 승격 + 네이티브 LIKE 전수 확인을 DoD에** — 원인이 파생/native 비대칭이라 공통화가 근본 수정. 노트에 관찰 추가: **`q=%` 전체 매칭과 AND/OR 관용구 뒤집기는 둘 다 에러 없이 조용히 틀린 결과(무증상 오작동)라 테스트로만 잡힌다**
- [2026-09-03] update | **230-4에서 이 시리즈의 중요한 구분이 확정**됐다 — **임시 스캐폴딩 vs 의도된 중복**. `food.image_ref`는 224 계열(회수 조건 필요)이 아니라 **영구 유지 + 불변식으로 정합 보장**이 정답이다(앱이 `images[]`를 읽어도 썸네일 1장은 계속 필요). 시리즈가 계속 "임시엔 회수 조건을 붙여라"를 강조했기 때문에 **중복이 보이면 반사적으로 회수 대상으로 오해하기 쉬운 자리**라 표로 명시 — **중복이라고 다 빚이 아니다**. / 나머지 판정: 225-2 채택(`totalPriceLongOf()`를 도메인에 — "널은 0" 규칙 단일화) · 225-3 불필요(FE에 이미 "가격 미인식" 칩) · 223-2 채택(`/deleted` 회수 조건 명시) · 223-3 보류하되 **동적 정렬은 화이트리스트가 정석**(문자열 조립=진짜 인젝션)을 노트에 남김 · 230-1 가드 불필요(어드민 UI에 이미지 편집 입력 자체가 없어 도달 경로가 수동 API 호출뿐) · 230-2 `source` enum 유지(**자리표시가 아니라 현재 사실의 기록**이라 217 `sanctions`와 성격이 다름)
