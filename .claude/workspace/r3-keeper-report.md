# R3 장부정리 보고 (keeper-r3, 2026-08-26)

## ① Claude Code 노트 2개 수리 (거짓 서술)
- **전**: 두 노트 머리말 🔗줄이 `[[12. SDD와 TDD…]]`(old 폴더)를 가리키며 "kbap 이 test-writer→implementer 팀으로 TDD 하네스를 굴린다"고 서술.
- **후**: 링크를 `[[14. SDD와 TDD — 스펙과 테스트로 개발하기]]`로 갱신 + 하단에 날짜 붙인 `> [!note]` append(본문 나머지 불변). 삭제가 아니라 **교체 + 이력 보존** — 무엇이 언제(2026-08-14) 왜(느려짐 + 과한 코드) 걷혔는지, 지금은 메인 세션 직접 구현 + 리뷰 스킬 명시 호출, 원칙 I 은 유효.
- 남긴 교훈 — 노트2: 서브에이전트 맥락 격리의 대가 + **강제 수단(하네스)과 규칙(헌법)을 분리해 뒀기에 수단만 갈아치울 수 있었다** / 노트5: 남이 쓴 테스트를 받는 분업이 Green 의 '최소' 판단을 막는다 + **"나눌 수 있나"가 아니라 "나눠서 뭘 버나"**.

## ② 복리 (전부 하단 append·본문 불변, 양방향)
- `백엔드/S3 presigned URL과 클라이언트 시크릿` ↔ 12 — 서명하는 코드가 레포에서 파일 하나로 갇혀 있다(갈아끼울 때 고칠 파일 수를 미리 정해 두는 설계).
- `인증·보안/쿠키·세션 vs 토큰 인증 — 왜 앱은 JWT인가` ↔ 12 — 계약이 규정한 건 저장소가 아니라 **조회+삭제 원자성**이고 회전의 재사용 구멍이 거기 달렸다(노트 12 §6-1 에도 역방향 한 문장 추가).
- CC `2. 행동을 다듬는 장치…` · `5. 여러 에이전트의 협업` ↔ 14 (①과 한 블록).
- 외부 인바운드 전수 확인 결과 11·13 이 닿는 볼트 밖 기존 노트는 없음 — 억지 링크 만들지 않음.

## ③ 지도·홈
- 지도: 11·12·13·14 `[x]` + 위키링크 승격 / 진도표 R2 ✅·**R3 ✅ 방금**·R4 ⬅ 다음 / 완료 `7 / 29` → **`11 / 29`**. 미집필 18개(1~3·15~29)는 일반 텍스트 유지.
- 홈: kbap 줄 `(집필 중: 7/29)` → **`(집필 중: 11/29)`**. 다른 줄 불변.

## ④ 작업 로그
5줄 append (`ingest` R3 4노트 / `fix` CC 2노트 수리 / `update` 실측 정정 2건 / `update` 복리·등록 / `lint` #14). 102줄 → 107줄.

## ⑤ 린트 수치
- 라이브 노트 깨진 링크 **0** (미해결 6건은 전부 작업 로그의 append-only 예외 3건 + `[[링크]]`·`[[홈]]`·`[[wikilink]]` 문법 텍스트)
- 새 4노트·CC 2노트 → `kbap 백엔드 (old)/` 유입 **0** (CC 2건은 14번으로 이설. 다른 폴더 → old 인바운드 11건은 대체 노트 미집필이라 R4·R5 몫)
- 유령 노드 **0** (지도 포함 — 위키링크는 4~14 + 홈뿐)
- 백틱 래핑 위키링크 **0** / 상대링크 `](../` **0**
- 머리말 두 줄 고정 서식 11~14 **전부 준수**(직접 수정 0) · frontmatter `상태: 완료` 4/4 · 파일명 = H1 = 링크 대상 일치 · 고아 0(인바운드 11:4 · 12:6 · 13:4 · 14:4)

## ⑥ 품질 검수 (직접 수리 1 / 지목 3)
- **수리** — 13 §4-13 "리플렉션"이 정의 없이 등장 → 인라인 정의 + 정의가 있는 `[[8. 영속성 컨텍스트와 엔티티 매핑…]]` 링크.
- **지목1(구조)** — 14 만 머리 블록이 다르다. 11·12·13 은 `> 작성 · 맥락 / > 목표` + `## 0. 한 줄 요약`인데 14 는 그 블록 없이 인라인 `> **한 줄 요약**` + 절 번호가 1부터. 폴더에서 다른 시리즈처럼 보인다(머리말 콜아웃 서식 자체는 준수).
- **지목2(정합)** — 13 이 같은 규칙을 두 좌표로 인용: §4-5 제목 `:105-138`, §5-1 표 `:125-138`. 실측상 `given` 105 · 맵 108-123 · 검사 125-138 이라 둘 다 거짓은 아니나 노트 안에서 갈린다.
- **지목3(눈높이)** — 12 의 `seam` 첫 등장이 §3-2 표(134줄)인데 정의는 §4 표(202줄) "계약(seam)". 개념표가 없는 노트라 첫 등장 자리에 한 마디가 필요.
- **중복·상충 없음** — ADR-0018 을 11(모듈 궤적)·12(3분할 규약)·13(§4-11 규칙)이 각각 다루되 서로를 "정본"으로 지목해 층이 갈려 있다. 13 이 규칙 나열에 그치지 않는지 확인: 13종 전부 **무엇을/왜/어기면** 3단 + 3층 강제표 + 실패 메시지 읽는 법까지 있다.
