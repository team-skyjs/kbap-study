# R5 장부정리 (keeper-r5) — 2026-08-27

**① OCR 노트 2개 이관·검증 (R4 잔여 3건 중 2건 상환)**

| 노트 | 줄 | 처리 |
|---|---|---|
| `Expo & React Native/OCR 메뉴 스캔 — 박스 그룹핑.md` | 10 | 옛 14 → **새 19 + 20 병기**, `POST /api/v1/scans` → **`/api/scans`** 교정, "이건 v1 경로" 명시 |
| `Expo & React Native/OCR 라인 분류 — 휴리스틱의 한계와 대안.md` | 11 | 옛 14 → **새 19 + 20 병기**, "FE 가 OCR 결과를 보내는 건 v1" 명시. §5 제목 `(BE 작업 중)` → `(2026-07 예고안 — 실제 구현은 다르게 풀렸다)` |

**서술 검증 — 어긋난 곳 3종, 두 노트 하단에 날짜 붙인 `> [!note]` 로 남김.**
① 경로·필드명(`itemId`(FE) = `idx`(서버 DTO)). ② **서버가 사진을 비전 LLM 으로 다시 읽는다**(kb-320) — FE OCR 은 힌트일 뿐, 응답 기준은 비전 추출 메뉴라 **응답 길이 ≠ 요청 items 길이, `idx` 로 짝을 맞춰야 한다**. ③ **라인 분류 §5 가 예고한 카탈로그 퍼지 매칭(Levenshtein 3단계)은 채택되지 않았다** — 서버는 `matchKey`(NFC + `가`~`힣` 만) 세 줄로 정확 일치만 본다. "매칭을 똑똑하게" 대신 **"입력을 깨끗하게"**(정제를 비전 단계로 이동)로 풀렸고, 그 노트의 결론("카탈로그 매칭 = 최고의 분류기")은 살아남았다. 덧붙여 그 노트가 정의한 **false-safe 의 변종이 서버에 남아 있다** — 기피 성분 미등록 회원은 전 항목이 `SAFE`.

**② 복리 3짝** (하단 append·본문 불변, 양방향 완성)
- `백엔드/S3 presigned URL과 클라이언트 시크릿` ↔ **21** — 그 노트가 *"발급 API 미배포"* 로 끝나 있던 API 가 배포됐다. 설계가 코드에서 달라진 곳 하나: **2단계가 아니라 3단계**(확정 신고가 소유 증명을 만든다).
- `인증·보안/쿠키·세션 vs 토큰 인증 — 왜 앱은 JWT인가` ↔ **20** — JWT 를 "너 누구냐" 말고 **"이거 해도 되냐"**(자원 사용권)로 쓰는 두 번째 용례. 실패가 401 이 아니라 **400** 인 이유까지.
- `Expo & React Native/iOS 앱 용량 — 네이티브 의존성과 ML Kit 모델` ↔ **20** — v2 는 앱이 글자를 안 읽으니 **450MB 사건의 뿌리가 사라진다.** 대신 오프라인 불가 + 호출당 비용 — **앱 용량을 서버 비용과 맞바꾼 것**.

**③ 지도·홈** — 지도 19·20·21 `[x]` + 위키링크 승격 / 진도표 R4 `✅`·**R5 `✅ 방금`**·R6 `⬅ 다음` / 완료 `15 / 29` → **`18 / 29`**. 홈 kbap 줄 `(집필 중: 15/29)` → **`(집필 중: 18/29)`**. 미집필 1~3·22~29 일반 텍스트 유지.

**④ 작업 로그 — 6줄 append**: `ingest`(R5 3노트) · `move`(이관 2건) · `fix`(FE 서술 3종) · `update`(복리 3짝+지도·홈) · `update`(실측 6건) · `lint`(#16).

**⑤ 린트 #16** — 볼트 68노트 / 위키링크 833건 NFC 정규화 후 전수대조.
- **깨진 링크 0** (미해결 3건 = 작업 로그 2026-07-14 줄 `:45`·`:53`·`:56`, append-only 알려진 예외) / **새 3노트 → old 유입 0** / 유령 노드 **0**(지도 위키링크는 4~21 + 홈뿐) / 백틱 래핑 **0** / 상대링크 `](../` **0**
- **old 로 들어가는 외부 인바운드 잔여 = 1건** ✅ 예상대로. `클라이언트 저장소 — AsyncStorage·SecureStore·메모리 (K-Bap 실전)` → 옛 `21. 캐싱과 키-값 저장소`(**새 29번, R10 대기**). 홈 → 옛 지도는 의도된 📦보관이라 제외. R4 의 3건 → **1건**.
- **고아 0** — 인바운드 19:4노트 · 20:7노트 · 21:3노트
- 머리말 두 줄 서식 + 머리 블록 19~21 **전부 준수, 직접 수정 0건** / frontmatter `상태: 완료` 3/3 · 파일명 = H1 = 링크 대상 일치(지도만 `진행중`, 18/29 라 정상)

**⑥ 품질 검수** — 직접 수리 **1건**: 노트20 용어집의 `degraded` 가 살아 있는 플래그처럼 적혀 노트19 §8-3("하드코딩된 죽은 필드, 항상 `false`")과 어긋남 → 실측대로 고치고 19번을 정본으로 링크.

- **19↔20 경계는 깨끗하다 — 상충 0.** "v1 도 비전 LLM 을 부른다"·`requireDetectedMenu` 플래그가 v1/v2 를 가른다·판정 엔진(`ScanService.scan`)은 공유가 양쪽 동일 서술이고, 19 는 자기 표·치트시트마다 "v1", 20 은 §10 에 13행 대비표를 둬 **독자가 어느 버전을 읽는지 늘 안다.**
- **20번 754줄은 쪼갤 값이 없다.** 18개 절 중 최대가 91줄이고, "LLM 호출은 돈이다 → 몇 번까지·동시에 두 번·터졌는데 깎이면" 이라는 한 줄기로 읽힌다. 용어(Lua·원자성·check-then-act·jti·멱등·ZSET)는 §1 개념표에 FE 대응물과 함께 **선정의**돼 있어 눈높이 문제 없음.
- **지목 3건**(구조적, 작성자 판단):
  1. **`ScanService.kt:40-51` 코드 블록이 19 §5-1 과 20 §9 에 통째로 중복.** 19 는 2갈래, 20 은 3갈래(`SCAN-003` 포함)로 읽는다 — 상충은 아니나 **정본이 갈린다.** `requireDetectedMenu` 를 다루는 20 이 정본이므로 19 는 발췌를 줄이고 링크로 넘기는 편이 낫다.
  2. **`SCAN-001`(이미지 소유 검증 부재)이 세 노트에 다 실렸다** — 19 §8-3, 20 §13-③, 21 §10. 서술은 일치하나 3중 서술이다. 소유 검증의 전모를 가진 **21 을 정본**으로 두고 19·20 은 한 줄 + 링크 권장.
  3. **20 §12(테스트) 71줄이 19(21줄)·21(17줄) 대비 유독 두껍다.** Redis 원자성 단위 테스트는 §6 의 증명이므로 §6 으로 올리고 §12 는 통합 시나리오만 남기면 흐름이 붙는다.
