# R7 장부정리 결과 (24·25)

## ① 복리 + 승격
- **복리 1짝** — `백엔드/페이징 — Offset vs No-Offset(Keyset·Cursor).md` 하단에 2026-08-27 역링크 append(본문 불변). R6 역링크가 8절 5번 "복합 정렬 → tie-breaker" 의 실물이었다면 이번은 **반대편**: 주문·커뮤니티는 정렬이 `id` 하나뿐이라 동점이 없어 커서가 숫자 하나로 끝난다 + 원리 노트에 없던 셋(정렬 방향 뒤집히면 커서 부등호도 뒤집힘 / `size` 는 400 아니라 `coerceIn(1,30)` 조용한 클램프 / `totalCount` 부재가 FE 계약으로 샘). 24·25 의 외부 아웃바운드는 이 노트 **하나뿐**이라 억지 복리 없음.
- **승격 5건** — 노트17 `:207`(게스트 댓글 401 콜아웃 → 25 §7) · 노트19 `:395` · 노트22 `:649-650`(2줄) · 노트23 `:9`(머리 블록) · 노트9 `:447`(24 §8 + 25 §6-3 추가). 잔여 텍스트 예고 0건.

## ② 지도·홈
- 지도: 24·25 `[x]` + 위키링크 승격, 진도표 R6 ✅ / **R7 ✅ 방금** / R8 ⬅ 다음, 완료 **20 → 22 / 29**. 미집필 1~3·26~29 는 일반 텍스트 유지.
- `🏠 홈.md:25` — `(집필 중: 20/29)` → **`(집필 중: 22/29)`**.

## ③ 로그
`📜 작업 로그.md` 에 **6줄 append**: `ingest`(R7 2노트) · `update`(게스트 401 조사 종결) · `update`(실측 8건) · `update`(복리·승격·등록) · `fix`(QA 수리 2건) · `lint`(#18).

## ④ 린트 (#18)
볼트 72노트 · 위키링크 **1,001건** 전수대조(NFC + 코드펜스 제외) — **라이브 깨진 링크 0**(미해결은 작업로그 2026-07-14 알려진 예외 3건 + `[[링크]]`류 문법 설명 텍스트) / 새 2노트 → old **유입 0** / **old 외부 인바운드 잔여 1건 유지**: `클라이언트 저장소…:10` → 옛 21 캐싱(새 29번, R10 대기). 홈 `:26` → 옛 지도는 의도된 📦보관 / **유령 노드 0**(지도 링크 4~25 + 홈) / 백틱 래핑 **0** / 상대링크 **0** / 머리말 두 줄 + 머리 블록 24·25 준수 — **서식 수정 0건** / frontmatter 둘 다 `상태: 완료`, 파일명 = H1 = 링크 대상 일치 / **고아 0**(24: 7노트 · 25: 6노트 인바운드).

## ⑤ 품질 지적
- **22↔24↔25 커서 중복 0** — 원리는 페이징 노트, 복합 keyset 정본 22 §7-2, 단순 id 정본 24 §7-1, 25 §4 는 "커뮤니티에서만 다른 두 가지"만 남기고 넘긴다. 중복 설명 0줄.
- **24 = 9번의 실전판 성립** — §8 이 주문→항목→음식 3단 배치 로딩을 코드로 보여주고 `loadFoodsById` 네 줄을 9 §3-1 에 줄 단위로 대응시킨다. 상세·목록 모두 쿼리 3번(연관관계였으면 181번).
- **25 대댓글 트리 N+1 없음** — 쿼리 3번. 단 **대댓글은 페이징하지 않아** 최상위 하나에 300개 달리면 300개가 다 온다(노트가 스스로 의도적 단순화로 밝힘).
- **봉투 필드명 `payload` 정상** — R6 노트22 `data` 오기 전례가 있어 `BaseResponse.kt:5` 재확인 후 24 §9-1~9-3 · 25 §8 전수 대조, 오기 **0건**.
- **직접 수리 2건**: ① 24 §12 비교표의 커뮤니티 칸 `조합 = 뷰어별 차단 필터` → 25 §5 실측(차단 필터 없음)과 상충하여 교정 ② 같은 뿌리를 22 `:645` 에서도 잘라내고 "리뷰에만 있다" 명시.
- **지목(수정 안 함)**: 25 에는 `## 1. 개념표` 가 없다(18~24 일곱 노트 연속 관행이 끊김). §12 용어집 4항목 + 인라인·링크로 커버돼 읽는 데 지장은 없어 반려하지 않음. 정의 없이 튀어나온 용어·근거 없는 단정 **0건**, 24↔25 상충은 위 ①로 해소.
- **작성자 반려 0건.**

## ⑥ 작성자 실측 요약 (8건, 노트에 반영됨)
① 주문 상세·목록 **각 쿼리 3번 고정**(`publicImageUrlOf` 가 DB 를 안 가는 순수 문자열 함수라야 성립) ② **`saveAll` ≠ 배치 INSERT** — `hibernate.jdbc.batch_size` 설정 0건 + `IDENTITY` 키 전략이라 행마다 INSERT ③ 중복 주문 두 겹의 역할 분리 — 앱 검사는 정중한 에러용, 진짜 보증은 유니크 제약. 그래서 **`saveAndFlush`** 여야 `try` 안에서 잡혀 409 로 번역된다 ④ 그 제약 이름이 **SQL `:16` 과 `OrderService.kt:140` 두 곳 하드코딩** — 예외 원인 체인 문자열 매칭이라 이름 바꾸면 실전 더블탭만 500 ⑤ 소프트삭제(`ACTIVE` 만)와 유니크(`DELETED` 까지)가 **다른 세계를 본다** — 주문 삭제 API 가 없어 지금은 잠재적 ⑥ **준비중 음식도 주문된다**(`a2c339f9`, `ORDER-001` 소멸) — 대신 사진만 기본 대체(`f5fd3c6e`) ⑦ 남의 주문은 **403 아닌 404**(존재 여부 비노출) ⑧ **커뮤니티에 차단 필터 없음** — 자리는 있으나 `viewerMemberId` 가 `assemble` 에 전달조차 안 되고 `MemberBlockService` 미주입. 차단·신고는 리뷰뿐(`ReviewService.kt:177-183`).

**게스트 댓글 401 조사 종결** — 누락이 아니라 **스펙 명시 의도**. 근거 6개(`spec.md:89` FR-010 · `:46` · `contracts/comments-api.md:5`·`:67` "피드와 달리 게스트 첫 페이지도 불가" · swagger · 테스트 고정)를 25 §7 이 표로 못 박음. 의도는 "보이되 못 읽는다". 남는 옥에 티는 **에러 코드 불일치**(피드 2p = `COMMUNITY-005` 서비스 판정 / 댓글 = `AUTH-003` 필터). 교훈: **코드만으로는 "버그처럼 보이는 의도"를 구분할 수 없다.**
