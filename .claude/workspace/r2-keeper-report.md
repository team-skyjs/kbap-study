# R2 vault-keeper 보고 (2026-08-26)

## ① 페이징 노트 수리 (`백엔드/페이징 — Offset vs No-Offset(Keyset·Cursor).md:9`)
- **전**: `🔗 kbap 실전: [[8. LAZY 로딩과 N+1 문제]] — … 실제 백엔드에선 fetch join과 충돌해, "커서로 ID만 페이징 → 그 ID로 join fetch" 2단계로 결합된다(FoodRepositoryAdapter.findFoodPage, kb-63).`
- **후**: `🔗 kbap 실전: [[9. 연관관계를 쓰지 않는 설계 — N+1을 애초에 만들지 않기]] — … "커서로 id만 페이징(findFoodPageIds) → 그 id로 본체 조회(findByIdIn)" 2단계로 돈다(common/src/main/kotlin/com/kbap/common/domain/food/FoodJpaRepository.kt:107-117, kb-63). 조인이 아니라 id 목록을 거치는 2단계다.`
- 하단에 날짜 붙인 `> [!note]` 추가 — 옛 문장 원문 인용 + 왜 거짓인지(`join fetch` 0건 / `FoodRepositoryAdapter` 소멸 / 연관관계 0개라 애초에 쓸 일 없음) + **"2단계라는 뼈대는 그대로 맞다, 틀린 건 2단계째의 정체와 클래스 이름뿐"**. 본문 2~9절 불변.
- 부수: 노트 9의 §4-2 경고 콜아웃이 "지금도 그렇게 적혀 있다"는 현재형이라 수리 후 스스로 낡음 → 과거형 + "수리 완료"로 정정.

## ② 복리 (전수 확인: 8·9·10의 외부 링크는 아래 2건이 전부)
- `백엔드/페이징 — …` ↔ 9 — ①과 한 블록으로 처리(양방향).
- `Expo & React Native/클라이언트 저장소 — AsyncStorage·SecureStore·메모리 (K-Bap 실전)` ↔ 10 — 하단 append. 접점: AsyncStorage 구조 변경과 스키마 마이그레이션의 갈림길은 **되돌리기**(폰은 사용자마다 한 벌, DB는 서버 전체가 공유하는 딱 한 벌).

## ③ 지도·홈
- 지도: 8·9·10 → `[x]` + 위키링크 승격. 진도표 R1 ✅ / **R2 ✅ 방금** / R3 ⬅ 다음. `완료: 4 / 29` → `7 / 29`. 미집필 22개(1~3·11~29)는 `- [ ]` 일반 텍스트 그대로.
- 홈: kbap 줄 `(집필 중: 4/29)` → `(집필 중: 7/29)`. 다른 줄 불변.

## ④ 작업 로그
append 5줄 (ingest R2 / fix 페이징 수리 / fix QA 1건 / update 복리·등록 / lint #13). 기존 줄 수정 0.

## ⑤ 린트
| 항목 | 수치 |
|---|---|
| 볼트 전역 깨진 링크(라이브 노트) | **0** — 미해결 13건은 전부 `[[링크]]`·`[[대상]]`·`[[대괄호]]`·`[[...]]` 문법 설명 텍스트 + 작업 로그 3건(append-only 알려진 예외) |
| 새 노트 → `kbap 백엔드 (old)/` 유입 | **0** |
| 유령 노드(지도 포함) | **0** — 지도 위키링크는 4~10 + 홈뿐 |
| 백틱 래핑 위키링크 / 상대링크 `](../` | **0 / 0** |
부수: frontmatter 3노트 `상태: 완료` · 파일명 = H1 = 링크 대상 일치 · 동명 파일 0 · 고아 0(인바운드 8:6·9:9·10:5) · 홈/지도 등록 확인.

## ⑥ 품질 검수
- **직접 고침(1건)** — 노트 8 §9 치트시트 `@Version // (kbap 은 Food 만)` → `Food·Review 둘뿐`. 같은 노트 §8이 `Food.kt:73-75`·`Review.kt:42-44` 둘을 들고 있어 노트 안 모순이었다(실측 확인).
- **지목(3건, 미수정)**
  1. **노트 8 §7 — "리플렉션"이 정의 없이 3회 등장**(7-1·7-2). FE 개발자에게 가장 낯선 용어인데 §10 용어집에도 없다. 한 줄 풀이나 용어집 항목 필요.
  2. **노트 10 §6 — 인용한 마이그레이션 SQL 주석에 `:app:batch`** 라는 지금 없는 모듈 좌표가 그대로 들어 있다(레포 원문이라 편집은 부적절). 바로 위에서 "off는 설정이 아니라 의존성 부재"를 바로잡았듯, `:app:batch`가 지금의 `:batch`임을 한 줄 덧붙이면 좋겠다 — 노트 3·5가 "모듈 3개"를 가르친 직후라 독자가 걸린다.
  3. **머리말 서식이 폴더 안에서 두 갈래**다. 8·9는 `📂 주제: [[지도]]` + `⬅️ 앞 / ➡️ 다음` + `작성:·목표:` 두 줄, 10(과 R1의 7)은 `🗺️ [[지도]] · 📂 kbap 백엔드 (new)` + `앞:`만 있고 `작성:·맥락:` 줄이 없다. 폴더 전체 통일 여부는 결정 사항이라 손대지 않았다.
- **중복·상충**: 8↔9↔10 사이 상충 **0**. `ddl-auto: validate`가 8 §5-5와 10 §7에 겹치나 관점이 다르고 상호 링크가 있어 허용 수준. 소프트삭제·JSON 컬럼·2단계 페이징은 정본이 하나씩으로 잘 갈렸다.
