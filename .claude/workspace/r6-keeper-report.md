# R6 장부정리 (keeper-r6) — 2026-08-27

## ① 복리 — 3짝 양방향 + 승격 3건
- `백엔드/페이징 — Offset vs No-Offset(Keyset·Cursor).md` ↔ 22 — 8절 5번 "복합 정렬이면 커서에 tie-breaker(id) 포함"의 실물. 커서 인코딩이 정렬마다 갈려 **정렬 바꾸면 커서를 버려야 한다**는, 원리 노트에 없던 운영 제약.
- `20. 스캔 v2…` ↔ 23 — 같은 동시성 문제의 반대편 해법. Lua(값을 보고 판단) vs 원자 UPDATE(값을 볼 필요 없음).
- `21. 이미지 업로드…` ↔ 22 — `complete` 한 이미지의 소비처(`verifyImageOwnership` → `REVIEW-003`), 고아 객체의 실물.
- 승격: 20 `:400`·`:759`, 21 `:9` 의 `22. …(집필 예정)` → 위키링크. 20 흐름 노트 줄에 23 추가.
- 억지 링크 없음 — 인증·보안·Expo 쪽은 접점 실측 0.

## ② 지도·홈
지도 22·23 `[x]` + 위키링크 승격 / 진도표 R5 ✅ · **R6 ✅ 방금** · R7 ⬅ 다음 / 완료 `18 / 29` → **`20 / 29`**. 미집필 1~3·24~29 는 일반 텍스트 유지. 홈 kbap 줄 `(집필 중: 18/29)` → **`(집필 중: 20/29)`**.

## ③ 로그 — 6줄 append (ingest 1 · update 3 · fix 1 · lint 1)

## ④ 린트 (린트 #17)
70노트 · 위키링크 918건 전수대조(NFC + 코드펜스 제외). **라이브 깨진 링크 0**(작업 로그 2026-07-14 3건은 알려진 예외) / 새 2노트 → old 유입 **0** / **old 외부 인바운드 잔여 1건** — `클라이언트 저장소` → 옛 21 캐싱(새 29번 R10 대기, R5 대비 변동 없음. 홈→옛 지도는 의도된 📦보관) / 유령 노드 **0** / 새 노트 백틱 래핑 **0** · `](../` **0** / 머리말·머리 블록 22·23 준수(**서식 수정 0건**) / frontmatter `상태: 완료` · 파일명=H1 일치 / 고아 0(22 인바운드 5노트 · 23 3노트).

## ⑤ 품질 — 직접 수리 2건, 반려 0건
- **[수리·중요] 노트22 응답 봉투 필드명이 틀렸다** — §9-1·§9-2 JSON 이 `"data"` 였다. 실제는 `"payload"`(`BaseResponse.kt:5` 재확인), 노트7 정본·노트23 §11 과도 어긋남. FE 가 베끼면 `undefined` 가 나오는 종류라 2곳 교정.
- **[수리] 노트22 §6-4 `TOCTOU`** 가 정의 없이 괄호로만 첫 등장(용어집 정의는 440줄 뒤) → 인라인 풀이 삽입.
- **22↔23 경계 깔끔** — 원장 적재 = 22 §6 정본, 점수 계산·"왜 원장이 필요한가" = 23 §8. 서로를 정본으로 명시해 넘긴다. `uq_member_ranking_event` 도 중복이 아니라 각도가 다름(22: TOCTOU·3겹 방어 / 23: 원자성 ≠ 멱등).
- **20↔23 상충 0** — 양쪽 다 "비관 락은 수 초짜리 LLM 호출을 잠금 안에 가둬 기각" 동일 서술. 23 §7 의 7행 대비표가 정본.
- 용어(lost update·멱등·원장·낙관/비관 락·값 객체·check-then-act)는 전부 §1 개념표에 FE 대응물과 함께 선정의됨.
- **22 의 737줄은 분할 불필요** — 16절 중 최대 90줄대. 신고·차단이 §8 한 절로 눌려 있고 "그 행이 남의 조회 결과를 바꾼다"가 동거 이유. 쪼개면 `GET /api/reviews` 제외 목록 설명이 두 노트로 갈린다.
- 23 실측 단정 5건 레포 재확인 OK: 마이그레이션 49개에 `score`·`tier` 컬럼 0 · `leaderboard` 0건 · `PESSIMISTIC`/`@Lock` 0건 · `@Version` 은 `Review`·`Food` 둘뿐 · 원장 리포지토리 메서드 1개.

## ⑥ 랭킹 점수 기여 요소 — 실측 결론 (옛 노트 대비 무엇이 바뀌었나)
옛 노트(`old/18. 회원 랭킹 흐름`)·KB-123 스펙: *"리뷰 도메인이 없어 reviews·diversity 는 항상 0, 점수는 스캔만"*. **지금은 3요소 전부 가동이다.** `ReviewService.createReview` `:62 increaseReviewCount`·`:80 increaseUniqueReviewedFoodCount`, `deleteReview` `:119`·`:122 decrease*` — 레포에서 줄 번호까지 확인. 가중치 `Ranking.kt:21-23` = **리뷰 10 · 다양성 5 · 스캔 2**, 등급 7단계. 스캔은 `ScanFacade.kt:32`(v1)·`:49`(v2)로 계속 유입. **낡은 건 문서뿐, 코드는 정상** — 낡은 자리 3곳(① `MemberApi.kt:167` springdoc → **Swagger 에 그대로 노출**, 문장 실물 확인 ② kb-123 `spec.md` FR-011·Edge Cases ③ 같은 스펙 contract 의 `/api/v1/...` 경로, 실제는 버전리스)을 노트23 §13 이 표로 못 박음. FR-011 이 *"리뷰가 추가되면 산정 규칙을 안 고쳐도 반영돼야 한다"* 를 요구했고 실제로 `Ranking` 은 **한 줄도 안 고쳐졌다** — 호출자만 늘었다.

부수 실측: **리뷰 자격 조건**(`kb-369`, 2026-08-21) — 본인 스캔 이력 필수, 없으면 `REVIEW-004`/**403**. 음식 매칭 실패·삭제된 스캔 이력도 자격 없음. **`PATCH` 수정은 재검증 안 함**(작성 후 이력이 지워져도 수정 가능해야 하므로). / **삭제 시 원장 처리 = 삭제가 아니라 상쇄(보상) 이벤트** — `REVIEW_CREATED +1/+1` 은 남기고 `REVIEW_DELETED -1/0` 행을 **추가**. 리뷰 본체도 소프트 삭제. / **`uq_member_ranking_event` 는 원자성과 별개의 멱등 장치** — `SET c = c + 1` 이 완벽히 원자적이어도 **두 번 실행되면** 틀린다. `> 0` 하한 가드도 못 막는다(리뷰 5개 회원의 삭제 2회 반영 → 3, 음수가 아니라 통과. 틀렸지만 그럴듯한 값). `event` 를 키에 넣은 건 막을 대상이 "같은 리뷰"가 아니라 "**같은 사건**"이라서.
