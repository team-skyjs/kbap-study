# R4 장부정리 (keeper-r4) — 2026-08-27

**① 링크 이관 9건 / 5노트** — 매핑 옛13→새15 · 옛15→새16 · 옛16→새17 · 옛17→새18(외부 위키링크 원래 0건). 대상 4파일 존재 검증 완료.

| 노트 | 줄 | 전 → 후 |
|---|---|---|
| `인증·보안/OAuth 2.0과 OIDC — 소셜 로그인의 원리.md` | 182 | `15. 소셜…` → `16. 소셜…` |
| `인증·보안/Google vs Apple 소셜 로그인 비교.md` | 130 | `15. 소셜…` → `16. 소셜…` |
| `인증·보안/쿠키·세션 vs 토큰 인증 — 왜 앱은 JWT인가.md` | 143 / 144 | `15. 소셜…`→`16. 소셜…` / `16. JWT 인증 필터…`→`17. JWT 인증 필터…` |
| `Expo & React Native/Android 구글 로그인 — SHA-1·OAuth 클라이언트·DEVELOPER_ERROR.md` | 10, 61 | `15. 소셜…` → `16. 소셜…` |
| `백엔드/S3 presigned URL과 클라이언트 시크릿.md` | 9 / 10 / 87 | `15. 소셜…`→`16. 소셜…` / `13. 온보딩…`→`15. 온보딩…` / `16. JWT…`→`17. JWT…` |

**거짓 서술 수리 1건**: `쿠키·세션…:144` 의 "**매 요청** 필터가 검증" → "**보호 경로마다**"(필터는 19개 패턴에만 붙음). 원문 수정 + 날짜 `> [!note]` 병기.

**② 복리 5짝** (하단 append·본문 불변, 양방향 완성) — `OAuth 2.0과 OIDC` ↔ 16 (검증 성공한 토큰을 왜 안 쓰나: memberId 내장·수명 통제·강제 로그아웃) · `Google vs Apple` ↔ 16 (복합 식별 원리가 `uk_member_provider_uid` DDL + `"firebase-uid-should-not-be-stored"` 테스트로) · `쿠키·세션 vs 토큰` ↔ 17 (필터 비전역 정정 + 재사용 탐지라는 미구현 절반) · `Android 구글 로그인` ↔ 16 (**이음매** — 앱이 idToken 을 쥐는 종착점이 16의 첫 줄. 서버로 가는 건 구글 것이 아니라 Firebase 재포장본) · `S3 presigned URL` ↔ 15 (`validatedImagePath` 가 `http(s)://` 거절 → objectKey 만 저장, 조회 때 `ImageUrls.resolve`)

**③ 지도·홈** — 지도 15·16·17·18 `[x]` + 위키링크 승격 / 진도표 R3 `✅`·R4 `✅ 방금`·R5 `⬅ 다음` / 완료 `11 / 29` → **`15 / 29`**. 홈 kbap 줄 `(집필 중: 11/29)` → **`(집필 중: 15/29)`**. 미집필 1~3·19~29 일반 텍스트 유지.

**④ 작업 로그** — `📜 작업 로그.md` 에 **6줄 append**: `ingest`(R4 4노트) · `move`(이관 9건) · `fix`(거짓 서술 1건) · `update`(복리 5짝+지도·홈) · `update`(실측 정정 5건) · `lint`(#15).

**⑤ 린트 #15** — 볼트 65노트 / 위키링크 719건 NFC 정규화 후 전수대조.
- **깨진 링크 0**(미해결 3건 = 작업 로그 `:45`·`:53`·`:56`, append-only 알려진 예외) / **새 4노트 → old 유입 0** / 유령 노드 **0** / 백틱 래핑 **0** / 상대링크 `](../` **0**
- **고아 0** — 인바운드 15:6회·4노트, 16:13회·8노트, 17:13회·6노트, 18:4회·3노트
- 머리말 두 줄 서식 + 머리 블록(`> 작성·맥락`/`> 목표` → `---` → `## 0. 한 줄 요약`) **15~18 전부 준수, 직접 수정 0건** / frontmatter `상태: 완료` 4/4 · 파일명 = H1 = 링크 대상 일치(지도만 `진행중`, 15/29 라 정상)

**old 로 들어가는 외부 인바운드 잔여 4건** (R3 의 11 → 4):

| 출처 | → 옛 노트 | 대기 |
|---|---|---|
| `Expo & React Native/OCR 메뉴 스캔 — 박스 그룹핑` | `14. 메뉴판 스캔 판정 흐름` | 새 19 (R5) |
| `Expo & React Native/OCR 라인 분류 — 휴리스틱의 한계와 대안` | `14. 메뉴판 스캔 판정 흐름` | 새 19 (R5) |
| `Expo & React Native/클라이언트 저장소 — AsyncStorage·SecureStore·메모리 (K-Bap 실전)` | `21. 캐싱과 키-값 저장소` | 새 29 (R10) |
| `🏠 홈` | `🗺️ kbap 백엔드 공부 지도` | **의도된 📦보관 등록 — 이관 대상 아님** |

**⑥ 품질 검수** — 직접 수리 2건: 노트15 흐름표의 `MDC` 가 정의 없이 첫 등장 → 인라인 풀이 + 17번 정본 표시 / 노트18 참고 URL 4개를 폴더 관행대로 `<>` 로 감쌈. 지목 3건(구조적, 작성자 판단):
1. **AUTH-007 이 17번 에러 코드 표에 없다.** 16번 §8 은 탈퇴 실패를 `SOCIAL_ACCOUNT_DELETE_FAILED`(AUTH-007, 500)로 명시하는데 17번 §11 표(`ErrorCode.kt:14-21`)는 001~006·008 만 싣는다. 17이 인증 에러 코드 정본을 자처하니 한 줄 구멍.
2. **17↔18 코드 발췌 중복.** `AuthMemberIdOrNullArgumentResolver` 같은 5줄이 17 §5(정본)·18 §9 양쪽에, "`parseAccessToken` 은 예외를 던진다" 서술도 두 곳. 18이 게스트 3갈래 표를 위해 필요하고 17로 링크도 걸려 모순은 아니나 정본이 갈릴 위험.
3. **노트15 안에서 같은 파일을 두 좌표로 인용.** `AuthMemberIdArgumentResolver` 를 흐름표는 `:26`, 3-1절은 `:24-27`(17번은 `:18`/`:14-28`). 실코드 확인 후 통일 권장.

**상충 없음 확인** — `@AuthMemberId` 메커니즘은 15·18 이 모두 17을 정본으로 명시하고 본문 중복 설명 없음. "홈은 필터를 안 탄다"는 17 §4 와 18 §9 note 가 동일 근거(`WebConfig.kt:80-100`)로 일치. `@AuthMemberIdOrNull` 이 만료·위조에 예외를 던진다는 서술도 17·18 일치.
