---
tags: [meta, 규칙, schema]
생성일: 2026-07-08
상태: 완료
---

# 📐 볼트 규칙 (Schema)

> 이 볼트의 **운영 설계도**. 사람이든 LLM이든(클로드 코드 포함) 이 볼트를 만질 땐 이 규칙을 따른다.
> 참고: [카파시 LLM Wiki](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)의 Schema 층에 해당.

---

## 1. 위치 (절대 헷갈리지 말 것)
- **정식 볼트 (여기에 쓴다)**: `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/study` — Mac·아이폰 iCloud 동기화.
- **백업 (읽기 전용)**: `~/dev/local-handoff/study` — 구 원본. **사용자가 명시적으로 허락하기 전까지 삭제·수정 금지.**

## 2. 구조
```
study/
├── 🏠 홈.md          ← index. 모든 노트가 여기 등록됨 (묶음 노트는 주제 지도 경유 가능)
├── 📐 볼트 규칙.md    ← 이 문서 (schema)
├── 📜 작업 로그.md    ← append-only 작업 일지 (log)
├── _templates/       ← 새 노트 양식
└── <주제 폴더>/       ← 주제당 폴더 하나. 큰 주제는 🗺️ 지도(MOC) 노트를 둠
```

## 3. 노트 작성 절차 (= ingest, 복리의 핵심)
새 노트 하나를 만들 때 **⑥까지가 한 세트**다. ④를 건너뛰면 볼트는 덧셈으로만 자란다.

1. **접수**: 시드 노트 또는 사용자의 주제 요청.
2. **리서치**: 공식 문서·신뢰 소스 우선. 참고 링크를 노트 하단에 남김.
3. **작성**: [[📝 공부노트 템플릿]] 구조(한 줄 요약 → 개념표 → 원리 → 비유 → 주의점 → 용어집 → 체크리스트). 쉬운 한국어 + 비유 1개 이상.
4. **⭐ 복리 단계 (의무)**: 관련 기존 노트를 검색해서 —
   - 새 노트 ↔ 기존 노트 **양방향 링크** 추가
   - 기존 노트에 보강할 내용이 있으면 **직접 갱신**
   - 새 지식이 기존 노트와 **모순**되면 해당 노트에 `> [!warning] 모순` 콜아웃으로 플래그
5. **등록**: [[🏠 홈]]에 `[[링크]] — 한 줄 요약` 추가. **링크는 파일명과 글자까지 정확히 일치** (2026-07-08 깨진 링크 사고 참조).
6. **기록**: [[📜 작업 로그]]에 한 줄 append.

## 4. 시드 워크플로우 (코딩 세션 ↔ 공부 세션)
- 코딩 세션(kbap-fe 등)은 dev 폴더에 `status: seed` 노트를 떨굴 수 있음 — 겪은 사실 + 리서치 질문만.
- **`_briefs/` 변형 (2026-07-09~)**: spec 세션이 `dev .../study/_briefs/YYYY-MM-DD-주제.md` 형식의 브리프를 떨구기도 함 — 시드보다 정교 (완성 노트 위치·연관 노트·리서치 키워드·재현 코드 지정). 처리 절차는 시드와 동일.
- 공부 세션이 시드/브리프를 읽고 → 리서치 → **iCloud 볼트에 완성본** 작성(위 절차) → 확인 후 dev 원본 삭제.
- 브리프가 프로젝트 코드를 인용하면 **가능한 실제 레포 코드를 열어 검증** (브리프 작성 시점 이후 코드가 진화했을 수 있음 — 실제로 KB-72 패치가 브리프엔 "예정", 코드엔 적용돼 있었음).

## 5. 파일·표기 규약
- 파일명 = 노트 제목 = 링크 대상. 읽기 좋은 한국어 제목 (대시 나열식 ✗).
- frontmatter: `tags / 생성일 / 상태(진행중·완료)`. 시드는 `status: seed`.
- `상태: 보관` — 사실이 낡아 더는 현행이 아니지만 기록으로 남기는 노트(2026-08-26 추가). 최상단에 `> [!warning] 📦 보관 노트` 배너로 낡은 이유와 대체 노트를 밝힌다. 홈에서 빼지 않고 "📦 보관" 표시를 달아 남긴다(고아 방지).
- 표 안의 별칭 링크는 `[[대상\|별칭]]` (파이프 이스케이프 — Obsidian 표준).
- 문법 예시용 가짜 링크는 반드시 백틱으로 감싼다: `[[예시]]` ← 이렇게. (유령 노드 방지)

## 6. 린트 (주기 점검)
사용자가 "린트 해줘" 하면 볼트 전체를 검사:
- [ ] 깨진 링크 (링크 대상 ↔ 실제 파일명 대조)
- [ ] 고아 노트 (인바운드 링크 0개)
- [ ] `상태: 진행중` 방치 (시드·템플릿 제외. `상태: 보관` 은 의도된 종착 상태이므로 제외)
- [ ] 홈 미등록 노트
- [ ] 노트 간 모순 (발견 시 플래그)
- 결과를 [[📜 작업 로그]]에 `lint |` 로 기록.

## 7. 역할 분담
- **사용자**: 주제 선정, 질문, 시드 투척, 최종 판단.
- **클로드 코드**: 리서치, 작성, 링크·홈·로그 장부정리, 린트. (= "장부정리는 LLM이" — 카파시)
